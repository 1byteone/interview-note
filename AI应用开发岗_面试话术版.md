# AI 应用开发岗 · 面试话术版（可直接背诵）

> 适用于临场快速回顾，侧重“自我介绍 + 项目亮点 + 高频追问应答”。

---

## 一、30s 自我介绍（通用）

我是 Java 后端方向、兼具 AI 应用开发经验的候选人。  
实习阶段参与了 AI 商品搜索和 RAG 知识库问答支持，完成接口联调、高并发幂等、搜索召回优化和流式交互验证。  
同时在项目中落地过 Spring Boot + Redis + ES + SSE 的稳定链路，擅长把业务问题拆解成可实现、可验证的工程方案。

---

## 二、项目一句话定位

1) AI 商品搜索与 RAG 知识库（实习）  
   - 关键词：接口开发、幂等防重、搜索召回提升、SSE 流式体验、异常兜底。  
2) 养老护理系统（团队）  
   - 关键词：健康评估优化、IoT 增量同步、小程序无感登录、并发支撑。  
3) 灵犀智能写作（个人）  
   - 关键词：StateGraph 编排、SSE 流式输出、结构化输出、多源配图降级。

---

## 三、高频问答模板

### Q1：你如何保证接口幂等？
我会在关键链路使用唯一幂等键，结合 Redis + Redisson 分布式锁做并发控制，  
同时在失败时通过 RocketMQ 做重试和补偿，最后用压测验证重复提交与超卖风险是否收敛。

### Q2：搜索召回为什么能提升 35%？
通过 IK 分词覆盖长尾表达，配合 DSL 多条件筛选与排序策略，  
再接入语义召回能力做混合检索，在内部测试集上做前后对比验证。

### Q3：RAG 场景如何保证输出稳定？
我会做三件事：明确工具边界、增加来源约束和结构化输出，  
再配置兜底策略，比如字段默认值与重试降级，避免模型波动影响业务链路。

### Q4：为什么用 SSE？
SSE 协议轻量、浏览器支持好，适合 AI 逐 token 输出；  
配合异步任务解耦和异常中断恢复，可以显著提升交互体验和接口吞吐。

### Q5：设备同步延迟怎么解决？
通过异步线程池增量同步，避免同步阻塞；  
同时设计设备与位置绑定规则，保证数据一致性，并监控同步耗时持续优化。

---

## 四、项目难点话术（可背诵）

这个项目难点不只是功能实现，而是稳定性保障。  
我会从缓存、异步、重试、降级四个层面组合优化，  
把问题从“能跑通”推进到“可压测、可观测、可恢复”，并把联调记录和排查步骤沉淀成文档，方便团队复用。

---

## 五、结尾收束话术

我更看重把 AI 能力稳定落到业务链路里，  
不仅要效果好，还要延迟可控、异常可兜底、问题可追溯。  
这也是我后续持续强化的方向：工程稳定性 + AI 应用落地能力。
