# 技术经理面试 100 问 · QA 全集（关键点详解版）

> 基于 `简历_模板覆盖版.docx`，从技术经理/技术面试官视角设计。
> 每题包含：**问题 → 参考回答 → 关键点解析（为什么问 / 怎么答 / 深挖方向）→ 常见扣分项**。

---

## 模块一：自我介绍 & 职业动机（1-10）

### Q1：请用 1 分钟做一个自我介绍。
**参考回答：** 我是软件工程专业应届生，方向是 Java 后端 + AI 应用开发。核心经历两段：一是传习教育的 AI 工程实习，参与商品搜索接口开发、RAG 知识库治理、SSE 流式交互，召回效果提升 35%；二是独立完成灵犀智能写作平台，基于 Spring AI Alibaba 的 StateGraph 编排多智能体，SSE 毫秒级首字上屏，生成成功率 99%。技术上以 Spring Boot + Redis + MySQL 为底座，叠加 RAG / Agent / SSE 等 AI 能力。我的特点是能把模型能力稳定地接到业务链路里。

**关键点解析：**
- **面试官意图**：技术经理第一题是在 1 分钟内判断三点——你有没有技术主线、表达能力、以及是否和岗位匹配。他不期待你背稿，而期待听到「你是谁 + 你做过什么 + 你的特点」三段式。
- **怎么答**：按「背景一句话 → 两段核心经历各带一个量化数字 → 技术底座 → 个人定位」组织。每个项目只给一个数字（35%、99%），面试官自然会顺着数字追问，这就等于你引导了面试节奏，把对话带到你准备好的深水区。
- **深挖方向**：面试官大概率会追「35% 怎么来的」「99% 怎么测的」「StateGraph 为什么能提升扩展性」，所以自我介绍里出现的每个数字和名词都必须能在后续 2-3 分钟内展开，否则等于给自己埋雷。

**扣分项：** 超时 >2min；背诵感强；只说技术和框架名没有数据；没有个人定位句。

### Q2：你为什么选择 AI 应用开发方向？
**参考回答：** AI 落地不只是调 API，核心难点在于检索质量、输出稳定性、延迟控制和异常兜底。这些恰好和我的 Java 工程背景高度契合。我希望成为"懂业务的 AI 工程师"，而不是只写 prompt 的 API 使用者。实习中我验证了这条路径的可行性——通过工程手段把 RAG 召回从不稳定提升到 35% 以上改善。

**关键点解析：**
- **面试官意图**：验证你的动机是否真实、是否经过思考，而不是「AI 火所以跟风」。技术经理见过太多嘴上说热爱 AI、实际只调过 API 的候选人。
- **怎么答**：用「AI 落地的工程难点（检索质量/输出稳定性/延迟/异常兜底）正好匹配我的 Java 工程能力」这个逻辑链，然后把「现实证据」（实习中召回提升 35%）摆出来。关键是让面试官听到你理解「模型能力 ≠ 产品能力」。
- **深挖方向**：他可能追问「你说的工程难点具体指什么」「调 API 和做 AI 应用区别在哪」，准备好"检索召回、结构化输出、SSE 断流恢复"三个具体例子即可。

**扣分项：** 回答 "AI 热门 / 薪资高所以选"——完全没有个人判断。

### Q3：你觉得自己最大的优势和不足分别是什么？
**参考回答：** 优势是"全链路意识"——不只写功能，还关注测试、异常、文档和可维护性。不足是：实习深度有限，某些中间件（如 RocketMQ）停留在使用层面，底层原理还需加强。目前在系统学习源码级知识。

**关键点解析：**
- **面试官意图**：优势看「是否与岗位强相关且可验证」，不足看「是否真实、是否在补」。这是技术经理快速筛选"自我认知清晰度"的题。
- **怎么答**：优势必须给实例（全链路意识 → 实习里做了压测、文档、复盘），不足必须是「真实的、可改进的、正在改进的」三要素齐全：承认 RocketMQ 停留在使用层 → 正在学源码。可以顺势说已经在看 commitlog/consumequeue 这些源码概念，体现出行动力。
- **深挖方向**：他可能追问 "RocketMQ 源码你学到哪了"——这是送分题，提前准备「NameServer/Broker/CommitLog/ConsumeQueue 四件套 + 消息存储流程」就能漂亮收尾。

**扣分项：** "我没什么不足"；不足说成优点（"我太追求完美"）；不足与岗位核心能力冲突（"我不擅长沟通"）。

### Q4：你的职业规划是什么？
**参考回答：** 短期（1-2年）：在 AI 应用开发岗位上深耕，补齐中间件底层原理和分布式系统设计短板。中期（3-5年）：成为能独立负责 AI 系统架构的技术骨干，具备从需求分析到性能调优的全栈能力。长期：成为连接 AI 和业务的技术桥梁。

**关键点解析：**
- **面试官意图**：判断稳定性（你是否会干一年就走）、目标是否可落地、以及你是否把成长寄托在公司平台上。
- **怎么答**：按「短期补短板 → 中期独当一面 → 长期技术+业务桥梁」三段阶梯回答，重点是让面试官看到你的成长路径「绑定在这个岗位上」（AI 应用开发），而不是「有计划跳槽」。
- **深挖方向**：他可能问 "具体怎么补分布式短板"，答「读完 RocketMQ 存储源码 + 做 1-2 个分布式事务/缓存一致性实验 + 参与开源」即可，体现规划的具体性。

**扣分项：** "想当架构师"（空泛）；"想创业"（不安定信号）。

### Q5：你对加班怎么看？
**参考回答：** 我关注的是"产出"而非"工时"。项目攻坚期主动加班是应该的；但日常我会用工程手段（自动化、文档化、测试覆盖）减少返工，本质上也是提升效率。

**关键点解析：**
- **面试官意图**：确认你对高强度工作的接受度，同时看你的工作价值观。技术经理并不想听"我能无限加班"，而想听"你有责任感且有方法"。
- **怎么答**：两段式——先表态「攻坚期没问题」，再展示「我用效率思维减少加班」的工程手段（测试覆盖防回归、文档减少沟通成本、自动化解放重复劳动）。这样既显责任心又显专业性。
- **深挖方向**：可能追问 "你实际加过班吗"，可以答实习期版本迭代上线前的联调加班，落点放在「保证交付质量」。

**扣分项：** 只答 "接受" 无实质内容；"不接受加班" 过于生硬。

### Q6：期望薪资是多少？
**参考回答：** 我对行业薪资有一定了解，更看重岗位成长空间和技术方向匹配度。薪资方面我持开放态度，愿意参考公司体系和岗位级别来协商。

**关键点解析：**
- **面试官意图**：初面阶段技术经理问薪资更多是走流程，真正定薪在 HR 环节。他期待你给一个「合理的、可协商的区间」，而不是立刻咬死数字。
- **怎么答**：不急着报数字，先强调「成长空间和技术方向匹配」是首要考虑，再表达「参考公司体系和岗位职级来协商」的开放性。如果对方非要数字，给一个窄区间（如 8-10K 应届参考区间），并说明区间按面试定级浮动。
- **深挖方向**：HR 环节会再谈，保持口径一致即可；不要在此环节陷入讨价还价。

**扣分项：** 报价过高（显得不近市场）或过低（显得没信心）；"都行"（显得没底线）。

### Q7：你还投了哪些公司/岗位？
**参考回答：** 主要投递 Java 后端和 AI 应用开发方向，集中在能做 AI 工程落地的团队。相比"面广"，我更倾向"面深"——选一个技术方向对口、团队氛围好的地方长期发展。

**关键点解析：**
- **面试官意图**：判断你的求职定位是否清晰、是否是海投党。海投容易被认为「拿到 offer 就骑驴找马」。
- **怎么答**：强调「方向聚焦」（Java 后端 + AI 应用开发双主线，本质是同一类能力），以及「选团队标准」（技术对口、能长期发展）。不要说具体公司名（避免显得脚踏多条船），也不要答"就投了你们一家"（不真实）。
- **深挖方向**：可能追问 "如果同时拿到多个 offer 你怎么选"，答「看技术栈匹配度、团队成长性、业务前景」即显理性。

**扣分项：** 报出一堆完全不相关的岗位；贬低其他公司抬高当前公司（油腻）。

### Q8：你有什么想问我们的？
**参考回答：** 会结合岗位预期提问，例如：① 团队目前 AI 项目的技术栈和架构是怎样的？ ② 对这个岗位的新人，前 3 个月的核心期望是什么？ ③ 团队的技术分享和成长机制是怎样的？

**关键点解析：**
- **面试官意图**：提问环节是加分的黄金窗口——它体现你的求职诚意、思考深度，以及对岗位的真实兴趣。技术经理会通过你的问题反推「你关心什么」。
- **怎么答**：准备 2-3 个「让面试官愿意展开说」的问题：问团队技术栈（显兴趣）、问新人期望（显执行力）、问成长机制（显长期主义）。把对方引到"他熟悉的领域"，还能顺便收集信息判断团队是否适合你。
- **深挖方向**：如果面试官回答很简短，可用「刚才您提到 XX，能再展开讲讲吗」做二次追问，把互动变深。

**扣分项：** "没有问题"（兴趣缺失）；一上来就问薪资福利年假（初面丢好感）。

### Q9：如果给你一个完全陌生的技术栈，你怎么做？
**参考回答：** 三步走：① 快速通读官方文档（入门+核心概念），② 跑通一个最小 Demo，③ 带着业务场景深入关键模块。同时用公司内已有代码做参照，避免从零踩坑。实习中用 LangChain 和 Spring AI 都是这么上手的。

**关键点解析：**
- **面试官意图**：考察"学习能力"和"可迁移能力"——技术天天变，能否快速上手比"用过什么"更重要。技术经理愿意招"会学习的人"。
- **怎么答**：给清晰的「文档 → Demo → 业务场景」三步方法论，并强调「向老代码、老同事、已有规范学习」——这三点展示的不是技术而是"融入团队的方式"，很加分。
- **深挖方向**：可能追问 "遇到文档没有的坑怎么办"，答「看源码/提 issue/翻社区踩坑文章/主动问同事」，体现多路径解决能力。

**扣分项：** "百度一下就行"（太随意）；只说学不会就换（没有韧性）。

### Q10：你平时怎么学习新技术？
**参考回答：** 分层学习：① 先看官方文档建立全景认知；② 找一个真实场景做 Demo 验证；③ 写笔记/博客沉淀；④ 在项目中应用并收集反馈。比如 RAG 技术，我先通读 LangChain 文档，再在实习中落地，最后整理了排查记录。

**关键点解析：**
- **面试官意图**：区分「被动学习者」（看视频、收藏夹吃灰）和「主动学习者」（有输入-实践-输出闭环）。这也是技术经理判断你成长速度的题。
- **怎么答**：突出「输出闭环」——写笔记、沉淀文档、整理排查记录，这比"爱看视频"高一个档次。最好提到你确实有笔记仓库或博客（简历里有 GitHub 链接是加分证据）。
- **深挖方向**：可以主动出示证据（"我的面试准备文档/学习笔记在 GitHub 上"），把学习能力变成可验证的事实。

**扣分项：** "刷 B 站教程"（被动）；只输入不输出（学了就忘）。

---

## 模块二：项目深挖 — 养老系统（11-25）

### Q11：养老系统整体架构是什么样的？你在其中负责什么？
**参考回答：** 系统由 Vue3 + Element Plus 的 B 端、微信小程序 C 端和 Spring Boot 后端组成，后端通过 MyBatis-Plus 访问 MySQL，Redis 承担热点缓存，IoTDA 负责设备接入，千帆模型负责健康报告语义分析。我负责健康评估、IoT 增量同步和统一鉴权，边界是把外部能力封装成稳定的业务接口。

**关键点解析：**
- **面试官意图**：第一道项目题，判断你对系统全貌的掌控度（不是只会自己那摊）、技术选型理由、以及个人边界是否清晰。
- **怎么答**：先给「端 - 后端 - 存储/缓存 - 外部服务」的全景，再收敛到「我负责的三个模块 + 我的边界」。关键词是"边界"——面试官很反感候选人把团队成果说成自己的，主动划清边界反而显诚实。
- **深挖方向**：他会顺着你负责的模块往下问细节（Q12-Q25），所以全景部分控制在 30 秒，把时间留给深水区。

**扣分项：** 背架构图背得细但不理解职责；把团队成果全部揽到自己身上。

### Q12：健康评估从 8 秒降到 1 秒，具体做了哪些优化？
**参考回答：** 先拆分耗时：数据库查询、模型调用和结果组装。对重复的健康建议按老人/评估版本建立缓存，优先命中本地缓存，再查 Redis，未命中才调用模型；同时限制模型调用超时并返回可用兜底。8 秒到 1 秒主要来自热点结果复用，而不是单纯调大线程池。

**关键点解析：**
- **面试官意图**：这是对你简历上"8s→1s"的验证题。技术经理有三层考察：① 你说的是不是真的；② 你懂不懂性能优化的方法论（先测量再优化）；③ 你是否诚实（有没有虚报）。
- **怎么答**：按「先测量拆分耗时 → 定位瓶颈（AI 调用） → 用缓存消除重复计算 → 超时/降级兜底」逻辑链回答。强调"不是调大线程池"是在展示你懂「优化要从算法/结构层面下手，而不是堆资源」。
- **深挖方向**：可能追问"缓存命中率多少""怎么评估优化效果"，提前准备「压测对比 + 缓存命中率统计」两个数据。

**扣分项：** 答 "我加了 Redis 缓存所以快了"（太浅）；说"换了个更强的机器"（不懂优化本质）。

### Q13：Redis 多级缓存怎么设计的？为什么选多级而不是单级？
**参考回答：** 一级是进程内缓存，适合热点数据，访问最快；二级是 Redis，供多实例共享；最后回源 MySQL 或重新调用模型。读路径按 L1→L2→DB，写路径更新 DB 后删除缓存并设置 TTL。多级缓存是用少量本地内存换取低延迟，同时保留分布式共享能力。

**关键点解析：**
- **面试官意图**：考察缓存架构的完整思考——不只知道 Redis，还要知道一级缓存、二级缓存各自的定位差异和一致性成本。
- **怎么答**：核心是「L1 快但不共享，L2 共享但稍慢，DB 兜底」的权衡逻辑。务必讲清**读写路径**（读：L1→L2→DB；写：先 DB 后删缓存），以及「为什么两级而不是一级」（本地缓存命中在 ns 级，避免每次走网络）。
- **深挖方向**：必追问「两级缓存一致性问题怎么处理」，准备好「本地缓存短 TTL + Redis 主动失效广播/版本号」的方案，承认"多级缓存会放大一致性问题，所以只用在容忍最终一致的数据上"是加分认知。

**扣分项：** 只答"我用了 Caffeine+Redis"没有权衡；不知道多级缓存会引入更多一致性问题。

### Q14：缓存和数据库不一致怎么处理？
**参考回答：** 对健康建议这类允许最终一致的数据，采用"更新 DB 后删除缓存"，删除失败进入重试或延迟双删；热点数据设置 TTL。强一致场景不依赖缓存直接返回，或使用锁/版本号控制。关键是先定义一致性要求，再选择方案。

**关键点解析：**
- **面试官意图**：这是 Java 后端必考题。技术经理想看你是不是"背了延迟双删八股"，还是真的理解「一致性是分等级的，方案要按等级选」。
- **怎么答**：先给「最终一致 → 更新 DB + 删缓存 + TTL」的常规做法，再补「删除失败怎么办（重试/延迟双删/订阅 binlog）」，最后强调「强一致场景别用缓存或加锁」。最后这句是区分度所在。
- **深挖方向**：必追问「先删缓存还是先更新 DB」，答「先更新 DB 再删缓存，避免并发下旧值覆盖缓存；删除用延迟双删解决读旧值窗口」，并主动承认「没有绝对正确的方案，只有符合一致性要求的方案」。

**扣分项：** 只答"延迟双删"说不清为什么；把延迟双删说成绝对可靠。

### Q15：百度千帆大模型怎么集成的？
**参考回答：** 在 Spring Boot 中封装独立的 AI Client，负责鉴权、请求参数、模型选择、响应解析和错误映射；业务层只传递结构化输入。外层配置连接/读取超时、有限重试和日志 traceId，密钥放配置中心或环境变量，不写入代码。

**关键点解析：**
- **面试官意图**：验证你真的集成过外部 AI 服务，而不是道听途说。重点看封装意识、安全意识和错误处理。
- **怎么答**：突出「独立封装 Client + 业务层解耦」的设计，讲清"鉴权/请求/解析/异常映射都收敛在一个类里"的好处（可测试、可替换模型、密钥不外泄）。traceId 和密钥管理是小加分项。
- **深挖方向**：可能问"换模型怎么办"，答「面向接口编程 + 模型适配层，换模型只改 Client 内部实现」，再问 "怎么测试" 答「Mock HTTP + 录制的响应做单测」。

**扣分项：** 说不出鉴权流程；密钥写死在代码里（安全意识差）；"直接在 controller 里调 API"（毫无封装）。

### Q16：AI 调用超时或返回异常怎么办？
**参考回答：** 先设置基于业务 SLA 的读取超时；仅对连接失败或临时错误做有限次指数退避重试，避免重复提交型请求被盲目重试；模型不可用时优先返回已有缓存或规则兜底，并记录模型、耗时、错误码和 traceId，便于告警和复盘。

**关键点解析：**
- **面试官意图**：考察第三方依赖的「容错设计」——AI 调用是最不稳定的一环，技术经理想看你有没有「超时-重试-降级-可观测」四件套思维。
- **怎么答**：四个层次都要覆盖：① 超时（区分 connect/read）；② 重试（有条件、有限次、指数退避，且幂等才重试）；③ 降级（缓存/规则兜底）；④ 可观测（日志带模型名、耗时、错误码、traceId）。每个层次一句话即可，组合起来就是完整方案。
- **深挖方向**：必追问 "哪些情况不能重试"，答「非幂等操作（如已扣款但响应丢失）不能盲目重试，需要人工或对账兜底」，这是高级认知。

**扣分项：** 只答"超时了我就重试"（没有条件判断）；完全不提降级和日志。

### Q17：IoTDA 设备同步是怎么做的？为什么选增量同步？
**参考回答：** 本地记录上次同步游标或时间点，按分页拉取 IoTDA 的变更数据，只处理新增、更新和下线设备；线程池负责有限并发，避免一次任务占满资源。同步结果记录成功/失败状态，失败重试，定期全量对账修复遗漏。500+ 设备时增量同步能显著减少请求和写库量。

**关键点解析：**
- **面试官意图**：考察「集成场景的数据同步设计」——面试官期待听到游标、分页、并发控制、失败重试、全量对账这一整套，而不是"我调了个接口把数据拉下来"。
- **怎么答**：按「增量依据（游标/时间戳）→ 拉取方式（分页）→ 执行方式（有限并发线程池）→ 可靠性（失败重试 + 全量对账兜底）」四段展开。强调"增量 + 对账"双保险，体现数据一致性意识。
- **深挖方向**：追问 "对账发现不一致怎么办"，答「标记差异 + 按设备维度重新拉取修复 + 记录对账日志」，再问 "为什么不用全量"，答「500+ 设备全量请求量大、慢，增量只拉变更更高效」。

**扣分项：** 说不出增量依据（游标/时间戳）；没有失败重试和全量对账的意识。

### Q18：异步线程池怎么配置的？为什么不用 @Async？
**参考回答：** 我会按 IO 密集型任务配置有界队列，设置核心/最大线程、存活时间、命名线程工厂和拒绝策略，并监控活跃数和队列长度。@Async 本身可以用，但默认配置容易失控；自定义线程池能做到任务隔离、参数可调和指标可观测，实际不是"绝不用 @Async"。

**关键点解析：**
- **面试官意图**：考察并发基础和工程化意识。重点不是"用没用 @Async"，而是你懂不懂线程池参数的意义、队列有界无界的风险、以及任务隔离。
- **怎么答**：先展示参数设计（IO 密集 core 偏大、有界队列防 OOM、命名工厂便于排查、拒绝策略选型），再讲「为什么自定义」——默认 @Async 单例线程池被各种任务共享容易互相阻塞。最后补充「不是不用 @Async，而是用自定义的 Executor 配合 @Async」——这句是对"为什么不用"的精确修正。
- **深挖方向**：追问 "队列满了怎么办"，答「按拒绝策略处理：核心任务 CallerRuns 降速、非核心丢弃并告警或落库补偿」——体现场景化选择而非背策略。

**扣分项：** "我就用默认 @Async 没啥问题"（深度不足）；说不出核心/最大/队列三个参数的关系。

### Q19：「设备-位置」绑定逻辑怎么保证数据一致？
**参考回答：** 设备 ID 和位置关系建立唯一约束，写入采用事务和版本号；更新时校验设备是否已绑定、位置是否被占用，避免并发覆盖。同步失败保留待处理状态，配合定时对账和人工补偿处理异常。

**关键点解析：**
- **面试官意图**：考察「关系数据的并发一致性设计」——这是后端基础能力，面试官想看数据库约束意识 + 并发控制 + 异常兜底的组合。
- **怎么答**：三层：① 数据库层用唯一约束兜底（设备-位置唯一，防重复绑定）；② 应用层用事务 + 乐观锁/版本号防并发覆盖；③ 失败状态留痕 + 对账/人工补偿。强调"数据库约束是第一道防线，应用逻辑是第二道"。
- **深挖方向**：追问 "并发同时绑定同一位置怎么办"，答「唯一约束会让一个失败，失败方提示位置被占用并重试」；追问"位置换绑"，答「事务内先校验新位置空闲，再更新绑定关系」。

**扣分项：** 只答"我在代码里判断了一下"（没有数据库约束概念）。

### Q20：ThreadLocal 在认证体系中怎么用的？如何防止内存泄漏？
**参考回答：** 拦截器校验 token 后，把用户上下文放入 ThreadLocal，Controller/Service 在同一次请求内读取；请求结束无论成功失败都在 finally 中 remove。在线程池中不能直接假设上下文会自动传递，应优先显式传参，必要时使用 TTL 并清理快照。

**关键点解析：**
- **面试官意图**：ThreadLocal 是 Java 面试高频题，技术经理重点考察：① 你用过且知道生命周期；② 你知道内存泄漏根因；③ 你知道线程池下不能直接传递。
- **怎么答**：分三句：① 怎么用（拦截器 set、业务读、finally remove）；② 为什么泄漏（ThreadLocalMap 的 key 是弱引用，value 是强引用，线程复用 + 不 remove → value 永远残留）；③ 线程池下怎么办（InheritableThreadLocal 只在新建线程时复制一次，池化复用会串号；生产用 TTL 或显式传参）。
- **深挖方向**：追问 "为什么 key 设计成弱引用"，答「弱引用 + 每次 get/set 时清理空 key，只是优化而非根治；正确 remove 才是根治」，体现你读过源码。

**扣分项：** 只答"我用 ThreadLocal 存用户"；不知道 remove 和泄漏根因。

### Q21：微信小程序无感登录流程？
**参考回答：** 小程序调用 wx.login 得到 code，后端服务端调用微信接口换取 openid，创建或查询用户后签发短期 access token 和可轮换 refresh token；后续请求由拦截器校验 token 并写入上下文。session_key 只在服务端保存或使用，不能下发前端。

**关键点解析：**
- **面试官意图**：登录鉴权是常见项目题。重点考察「code 换取身份的流程是否标准」「token 安全设计」「敏感信息是否妥善处理」。
- **怎么答**：按「wx.login → code → 服务端换 openid/session_key → 签发 token → 拦截器校验」流程讲，并强调两个关键点：① code 有有效期且一次性，必须服务端换；② session_key 是解密敏感数据的密钥，绝不能下发前端。
- **深挖方向**：追问 "token 过期了怎么办"，答「短期 accessToken + 可轮换 refreshToken，refreshToken 失效则重新登录，且 refreshToken 轮换防重放」；追问 "怎么防止 token 被盗"，答「https + 短有效期 + 敏感操作二次校验」。

**扣分项：** 不知道 code 要服务端换；把 session_key 当作普通用户信息下发前端。

### Q22：并发压测怎么做？压出了什么问题？
**参考回答：** 先准备可重复的库存和用户数据，再用 JMeter/Locust压测重复提交、库存扣减和查询热点，观察 QPS、P95/P99、错误率、数据库连接池和 Redis 命中率。重点验证库存最终不为负、同一请求只生成一笔订单，并根据日志定位重复写入和锁竞争。

**关键点解析：**
- **面试官意图**：验证「压测解决真实问题」的经验——技术经理想听的不是"我会用 JMeter"，而是"我通过压测发现了什么、怎么修复的、效果如何"。
- **怎么答**：完整链条：准备数据 → 设计场景（重复提交/库存/热点）→ 观察指标（吞吐 + 分位延迟 + 错误率 + 连接池）→ 验证业务正确性（库存不为负、订单不重复）→ 定位修复 → 复测对比。把「业务正确性」列入指标是亮点，因为很多人只压性能不压正确性。
- **深挖方向**：追问 "QPS 和并发数什么关系"，答「QPS = 并发数 / 平均响应时间，压测要用逐步加压找到拐点而不是一味拉高并发」。

**扣分项：** 只答"我用 JMeter 压了一下"；没有提到发现的业务问题。

### Q23：Redisson 分布式锁的原理？和原生 SETNX 区别？
**参考回答：** Redisson 用 Redis 键记录持有者和重入次数，通过 Lua 保证加锁、续期和释放的原子性，并提供看门狗续期。SETNX 只解决一次加锁，业务必须自己处理唯一 owner、过期时间和安全释放。锁不是库存一致性的全部，数据库条件更新和幂等仍要保留。

**关键点解析：**
- **面试官意图**：考察分布式锁的底层理解。技术经理期待听到"Redisson 相比 SETNX 多了什么"（重入、看门狗、Lua 原子、owner 验证），而不是背条条。
- **怎么答**：四层对比：① 原子性（Lua 脚本 vs 多条命令）；② owner 验证（释放前校验 value 防误删他人锁）；③ 看门狗续期（避免业务没跑完锁过期）；④ 可重入。最后补一句「分布式锁解决互斥，但数据一致性还要靠幂等/条件更新兜底」——这句是高级认知，说明你不迷信锁。
- **深挖方向**：追问 "看门狗原理"，答「锁默认 30s，客户端后台定时（每 1/3 周期）续期，业务结束或客户端挂掉时停止续期，Redis 到期自动释放」；追问 "锁过期但业务没完怎么办"，正是看门狗解决的问题。

**扣分项：** 只答 "Redisson 好用"；不知道看门狗；把锁当成库存一致性的全部方案。

### Q24：订单链路中 RocketMQ 做了什么？
**参考回答：** 将非核心或可异步流程从同步请求中拆出，通过消息削峰和解耦；消费者处理库存、索引或通知。消费失败返回重试，超过次数进入死信，消费端用业务唯一键实现幂等。下单核心结果仍需数据库事务确认，不能把 MQ 当作事务万能替代品。

**关键点解析：**
- **面试官意图**：考察对 MQ 三大价值（异步/解耦/削峰）的真实理解 + 可靠性闭环（重试/死信/幂等）是否完整。
- **怎么答**：先说「为什么用 MQ」——下单后的非核心动作（库存、通知、搜索索引）异步化，削峰（高峰期请求先入队）。再说「可靠性怎么保证」——消费失败重试、超次进死信人工处理、业务唯一键做幂等。最后强调边界：核心下单结果靠 DB 事务，MQ 只承载可异步的部分。
- **深挖方向**：追问 "消息重复消费了怎么办"，答「消费端幂等：唯一业务键 + 状态表先查后插 / 主键冲突跳过」；追问 "消息堆积怎么办"，答「先扩容消费者 + 分析消费慢的根因（SQL/锁）」。

**扣分项：** 只答 "用来异步"；没有消费幂等和死信的概念。

### Q25：MySQL 事务在这个项目中用到了吗？遇到过死锁吗？
**参考回答：** 下单、库存扣减和绑定关系更新需要事务，保证一次业务操作内部的原子性。事务尽量短，按固定顺序访问资源并依赖合适索引减少锁范围。若发生死锁，查看 InnoDB 状态和慢日志，确认互相等待的 SQL，统一加锁顺序并允许安全重试。

**关键点解析：**
- **面试官意图**：考察事务级别的实际使用 + 死锁排查能力。技术经理想知道你是否只在"背概念"，遇到真实死锁会不会查。
- **怎么答**：先说事务怎么用的（原子性、边界清晰、尽量短），再说「如何降低死锁概率」（固定加锁顺序、索引缩小锁范围、事务短），最后给「真遇到怎么办」（SHOW ENGINE INNODB STATUS 看 latest deadlock、分析两条 SQL、加锁顺序冲突 → 统一顺序 + 业务侧捕获死锁重试一次）。
- **深挖方向**：追问 "间隙锁是什么"，答「RR 隔离级别下 InnoDB 对范围查询加的锁，锁住区间防止幻读，也是死锁常见来源」，体现你有深入。

**扣分项：** 答 "没遇到过死锁" 且说不出如何预防；把 @Transactional 背成万能。

---

## 模块三：项目深挖 — 灵犀写作（26-40）

### Q26：StateGraph 是什么？和普通的 if-else 编排有什么区别？
**参考回答：** StateGraph 可以把 AI 应用建模成"状态 + 节点 + 边"的有向图。节点负责一个明确步骤，边负责顺序、条件或并行关系，状态负责在节点间传递中间结果。if-else 适合简单流程，但复杂后容易把业务、模型调用和异常分支揉在一起；图模型更便于扩展、测试和追踪。

**关键点解析：**
- **面试官意图**：考察你对「工作流编排」的理解深度——这是灵犀项目最亮的部分，技术经理想确认你不是"抄了个 StateGraph"而是真懂它的价值。
- **怎么答**：先给三个核心概念（State 共享状态 / Node 节点 / Edge 边），再用「为什么图优于 if-else」论证：① 复杂流程可拆分可复用节点；② 加新流程不破坏旧代码；③ 每个节点可单独测试；④ 执行链路可视化可追踪。每个论点一句话即可。
- **深挖方向**：追问 "State 怎么设计的，节点间怎么传数据"，答「定义统一的 WorkflowState（标题/大纲/正文/配图等字段），节点读写 State，边决定流转」；追问 "失败节点怎么办"，答「图支持条件边：失败走重试节点或错误汇聚节点」。

**扣分项：** 只能说"它是编排框架"；说不出 State/Node/Edge 或图 vs if-else 的实质差异。

### Q27：多智能体怎么编排？串行和并行怎么选？
**参考回答：** 我按任务依赖关系决定。标题生成、大纲生成、正文生成存在上下游依赖，采用串行；配图搜索和部分内容生成互不依赖，可以并行。并行后要在汇聚节点统一合并结果，并设置超时、失败降级和取消策略，不能只追求并发。

**关键点解析：**
- **面试官意图**：考察工作流设计的"以依赖关系为中心"的思考方式——这是编排系统的核心能力，而不是背"什么能并行"。
- **怎么答**：核心是「依赖分析」：有依赖 → 串行；无依赖 → 并行；并行后必须「汇聚节点统一合并 + 超时 + 失败降级」。强调"不是能并行就并行，并行会引入结果合并和部分失败的新复杂度"。
- **深挖方向**：追问 "并行的任务一个成功了另一个失败了怎么办"，答「汇聚节点处理部分成功：成功的保留，失败的走降级（如配图源失败用默认图）或整体回滚重试」，这是编排系统核心考点。

**扣分项：** 只说"能并行的就并行"；说不出并行带来的合并与失败处理复杂度。

### Q28：新增一种生成模式，只需要配置新节点——这是什么意思？
**参考回答：** 把每种能力封装成独立节点，把流程连接关系从节点内部抽离。新增模式时实现或注册新节点，再配置入口、边和输出映射，不修改已有节点。这个说法的前提是节点契约统一，否则配置化只是把复杂度转移到配置文件。

**关键点解析：**
- **面试官意图**：验证「扩展性"分工合理，以确认你的核心节点可复用、改已有节点不受影响」。
- **怎么答**：① 统一节点接口（输入/输出契约一致）；② 流程定义与节点实现分离（通过配置声明编排）；③ 新增模式 = 注册节点 + 配置边。并主动补一句"前提是节点契约统一，否则配置化只是把复杂度转移到配置文件"——这句话防止面试官觉得你夸大其词，展现工程判断力。
- **深挖方向**：追问 "如果新增模式要改现有节点的逻辑怎么办"，答「优先新增节点而不是改旧节点，遵循开闭原则；确实需要改时先加测试再改」。

**扣分项：** 说"加一个节点就行"但说不清节点接口和流程配置怎么分离。

### Q29：SSE 流式输出的技术细节？
**参考回答：** SSE 是基于 HTTP 的服务端单向事件流，响应类型为 text/event-stream，服务端按 data、event、id 等字段发送事件，浏览器可用 EventSource 接收。AI 场景中模型产生一个 token 或一个增量片段就发送一次，结束时发送明确的完成事件，异常时发送错误事件并关闭连接。

**关键点解析：**
- **面试官意图**：考察流式输出的底层细节是否真懂——能否说出协议格式、事件字段、连接生命周期，而不仅是"我用了 SSE"。
- **怎么答**：三个层次：① 协议层（content-type、data/event/id/retry 字段、事件间空行分隔）；② 生命周期（建立 → 逐段推送 → 完成事件/错误事件 → 关闭）；③ 与普通响应的区别（不是一次返回 JSON，而是持续连接推送）。加上"结束时发完成事件、异常发错误事件"体现工程细节。
- **深挖方向**：追问 "断线了怎么办"，答「EventSource 原生支持自动重连，配合 Last-Event-ID 实现断点续传」；追问 "为什么不用普通轮询"，答「逐 token 推 30 次 vs 轮询 30 次请求，SSE 一次连接完成」。

**扣分项：** 说不出 text/event-stream 和事件字段；"前端轮询"混淆。

### Q30：毫秒级首字上屏怎么实现的？
**参考回答：** 请求到达后先完成鉴权和参数校验，立即建立 SSE 连接；后台异步任务调用模型的流式接口，拿到首个增量片段后马上写入事件流，而不是等待整篇文章生成。这里的"毫秒级"应明确是前端展示链路的首字延迟指标，不能等同于完整请求耗时。

**关键点解析：**
- **面试官意图**：验证你「指标口径是否诚实」+「流式链路是否真懂」。技术经理非常在意候选人夸大指标——"毫秒级首字上屏"如果解释不清会扣分。
- **怎么答**：拆解链路：请求先建 SSE 流（毫秒级）→ 后台调模型流式接口 → 首 token 到达立即推送。强调「首字延迟 = 建连 + 模型首 token 时间，不是整个生成时间」，主动澄清口径=最高级的诚实展示。
- **深挖方向**：追问 "模型首 token 慢怎么办"，答「模型选型（首 token 快的）＋ 预热连接 + 前端先渲染占位框架，首 token 到了再填充」，体现端到端优化思维。

**扣分项：** 把 1 秒级别的体验说成"毫秒级"而不解释口径（夸大嫌疑）。

### Q31：异步任务体系怎么设计的？任务状态 tracking 怎么做？
**参考回答：** 接口层创建任务并返回 taskId，后台线程池执行生成，任务状态至少包括 PENDING、RUNNING、SUCCESS、FAILED、CANCELLED。状态和结果可以放 Redis 或数据库，更新时带版本号防止旧任务覆盖新状态；SSE 负责实时进度，查询接口负责断线后的补偿读取。

**关键点解析：**
- **面试官意图**：考察「异步任务体系」设计的完整性——taskId、状态机、状态存储、进度通知、断线补偿，缺一不可。
- **怎么答**：五件套：① taskId 标识；② 状态机（至少 5 态：待执行/执行中/成功/失败/取消）；③ 状态存储（Redis 高频读写 or DB 持久化）；④ 进度通知（SSE 实时）；⑤ 断线补偿（查询接口按 taskId 兜底）。再加「版本号防旧任务覆盖新状态」= 并发细节加分。
- **深挖方向**：追问 "状态和结果存 Redis 还是 DB"，答「高频状态存 Redis，最终结果落 DB；Redis 丢失可接受则只存 Redis，不可接受则 DB + Redis 双写并异步回补」；追问 "服务重启任务怎么办"，答「DB 持久化 + 启动时扫描未完成任务标记为失败或重跑」。

**扣分项：** 只有"前端轮询一下状态"；没有状态机和持久化概念。

### Q32：吞吐提升 50% 是怎么衡量的？
**参考回答：** 在相同机器、相同模型、相同输入集和相同并发度下，对同步版本和异步版本分别压测，比较吞吐、P95/P99、错误率和线程池队列长度。异步主要改善线程占用和请求排队，不能简单说"异步一定让模型生成更快"，所以我会同时报告完整任务耗时和接口响应耗时。

**关键点解析：**
- **面试官意图**：验证量化指标的严谨性。技术经理最烦"提升 50%"这种没有对照实验的说法，他期待听到"控制变量 + 指标口径"。
- **怎么答**：关键词是「控制变量」（相同的机器/模型/输入/并发度）+「指标分层」（接口响应耗时提升 vs 模型生成耗时不变）。主动说"异步改善的是吞吐和响应，不是模型本身的生成速度"= 极其加分，表明你理解指标的物理含义。
- **深挖方向**：追问 "哪些指标没变"，答「单任务模型耗时基本不变，变的是线程占用和接口排队」；追问 "怎么防止并发高时任务堆积"，答「有界队列 + 拒绝策略 + 队列长度监控」。

**扣分项：** "就是快了 50%"没有对照说明；把模型生成速度也说成提升（不严谨）。

### Q33：Prompt 模板引擎怎么封装的？
**参考回答：** 将系统提示词、用户提示词和变量定义分离，模板只声明主题、风格、字数、受众等变量，运行时做参数校验和安全转义。模板按版本管理，调用日志记录模板版本和模型版本，便于回滚和效果对比。

**关键点解析：**
- **面试官意图**：Prompt 工程是 AI 应用核心能力。技术经理想看封装思维（模板与代码解耦、变量化、版本化），而不是"prompt 写在代码字符串里"。
- **怎么答**：三个设计点：① 变量化（模板含占位符，运行时注入）；② 校验与安全（参数校验 + 转义防注入）；③ 版本化管理（模板版本 + 模型版本一起记录，出问题可回滚、可对比）。每点一句话。
- **深挖方向**：追问 "模板怎么存储"，答「配置文件/数据库/独立文件，避免硬编码在代码里」；追问 "不同模型 prompt 风格不同怎么办"，答「按模型体系分模板目录，统一 PIPELINE 接口」。

**扣分项：** "我就是 String 拼接的"（毫无封装）；没有版本管理意识。

### Q34：StructuredOutputConverter 是什么？怎么保证输出格式？
**参考回答：** 它根据目标 Java 类型生成格式提示，并尝试把模型文本转换为结构化对象。它是"尽力而为"的转换，不代表模型一定遵守格式。因此我会组合使用结构化 schema、System Prompt、解析校验、有限重试和兜底对象；关键字段缺失时不能直接入库。

**关键点解析：**
- **面试官意图**：考察对"LLM 输出是概率性问题"的理解。技术经理最看重你是否知道「结构化输出是尽力而为，必须校验兜底」——这决定你能不能做生产系统。
- **怎么答**：三个层次：① 它做什么（schema + 转换）；② 它不能保证什么（LLM 可能不遵守格式）——这句是核心；③ 完整方案 = schema + prompt + 校验 + 有限重试 + 兜底对象，关键字段缺失必须标记失败而不是静默入库。
- **深挖方向**：追问 "模型返回了不合法 JSON 怎么办"，答「解析失败 → 提示模型纠正重试 1-2 次 → 仍失败返回默认结构并标记兜底」；追问 "如何提高遵守率"，答「few-shot 示例 + 强约束 prompt + 选支持 structured output 的模型」。

**扣分项：** 以为转换器是"格式化保证"（不懂概率性）；没有校验兜底概念。

### Q35：策略模式做多源配图，具体怎么实现？
**参考回答：** 定义统一的 ImageProvider 接口，每个数据源实现自己的搜索、下载和结果转换逻辑；由工厂或注册表根据配置得到策略列表，再按优先级或健康状态选择。调用方只依赖接口，不依赖 Pexels 或 SVG 的具体实现。

**关键点解析：**
- **面试官意图**：考察设计模式的实际应用——是否真用了策略模式而不是只会背定义。技术经理想听接口抽象、策略注册、运行时切换。
- **怎么答**：三个要素：① 统一接口 ImageProvider（搜索/结果转换/健康状态）；② 策略注册（工厂 Map<源, Provider> 或 Spring 注入 List<Provider>）；③ 运行时选择（按优先级 or 失败后切换下一个）。强调「调用方只依赖接口」= 解耦的本质。
- **深挖方向**：追问 "和工厂模式什么关系"，答「策略模式解决『选哪个算法』，工厂解决『怎么创建对象』，这里用工厂+注册表把 Provider 装配起来」；追问 "新增一个图源要改什么"，答「实现接口 + 注册，调用方零改动」。

**扣分项：** 背"策略模式定义"；说不出接口抽象和注册机制。

### Q36：配图源失败自动降级——降级逻辑怎么做？
**参考回答：** 对可重试的网络错误做有限重试和退避，对鉴权失败或参数错误直接切换策略；记录每个图源的耗时和失败原因，连续失败触发熔断。所有图源不可用时返回默认占位图或仅保留文章文本，并把任务标记为"部分成功"，不能假装完整成功。

**关键点解析：**
- **面试官意图**：考察「第三方依赖容错」的成熟度——错误分类、重试边界、熔断、降级语义，这是生产级 AI 系统的关键能力。
- **怎么答**：五个层次：① 错误分类（可重试：网络/429；不可重试：鉴权/参数 → 直接切换）；② 有限重试 + 退避；③ 连续失败熔断（短期不再尝试该源）；④ 最终降级（占位图/纯文本）；⑤ 语义诚实（标记"部分成功"，不能假装完整成功）。最后一句是认知亮点。
- **深挖方向**：追问 "熔断怎么做"，答「记录连续失败次数，超过阈值进入 open 状态，经过冷却窗口 half-open 试探恢复」——给出标准熔断状态机即过关。

**扣分项：** "try-catch 换个源"一句话（没有错误分类和熔断）。

### Q37：生成成功率 99% 是怎么测的？
**参考回答：** 先定义成功标准：结构化字段完整、正文可读、图片链接有效、任务状态正确。使用覆盖不同题材和长度的测试集，统计端到端成功任务数/总任务数，并区分首次成功、重试成功和降级成功。报告指标时说明样本量、时间范围和是否包含降级结果。

**关键点解析：**
- **面试官意图**：验证量化指标的严谨性。技术经理会追问"成功率怎么定义的""分母是什么""是否包含降级"。答得好 = 数据可信。
- **怎么答**：四要素：① 明确定义成功标准（四条件）；② 测试集有代表性（不同题材/长度）；③ 指标分层（首次/重试/降级分开统计）；④ 口径透明（样本量、时间范围、是否含降级）。"我自己先信这个数，才能让面试官信"。
- **深挖方向**：追问 "1% 失败是什么原因"，答「模型超时和格式异常为主，通过重试和降级挽回一部分，剩余进入失败任务清单人工/自动复查」。

**扣分项：** "大概 99%"（说不清分母）；不区分重试和降级（虚高成功率）。

### Q38：Redis 在灵犀项目中做什么？
**参考回答：** 主要用于缓存热门模板和任务状态，降低重复查询；也可以用分布式锁限制同一用户或同一任务的重复生成。任务状态若只放 Redis，要接受重启丢失风险，生产应使用数据库或持久化方案，并通过 TTL 和容量策略控制内存。

**关键点解析：**
- **面试官意图**：考察你使用 Redis 的目的性和边界感——技术经理想知道你是否清楚 Redis 快在哪、适合什么、不适合什么。
- **怎么答**：三个用途（缓存模板、任务状态、分布式锁），并主动讲清边界：「任务状态放 Redis 有重启丢失风险」→「生产用 DB 持久化或 Redis+DB 双写」→「用 TTL 和容量策略控制内存」。讲边界比讲用途更显水平。
- **深挖方向**：追问 "任务状态为什么不用 DB 直接存"，答「高频轮询/更新放 Redis 减少 DB 压力，最终状态落 DB 保持久，读多写少用缓存是经典组合」。

**扣分项：** "什么都能用 Redis"（没有边界意识）；不知道 Redis 是非持久化/易失的。

### Q39：MyBatis Flex 和 MyBatis-Plus 区别？为什么选 Flex？
**参考回答：** 两者都增强了 MyBatis 的开发效率，提供条件构造和通用能力，但 API、代码生成、插件和生态习惯不同。个人项目选择 Flex 主要考虑查询表达力、开发效率和当时的技术实践；如果团队已有 MyBatis-Plus 规范，我会优先遵循团队统一栈，而不是为个人偏好增加维护成本。

**关键点解析：**
- **面试官意图**：考察技术选型的成熟度。技术经理想确认：你知道两者差异、你能给出选型理由、且你懂"团队规范优先于个人偏好"。
- **怎么答**：先说两者本质相似（MyBatis 增强、条件构造器），再说个人项目选 Flex 的理由（能力匹配当时需求），最后补关键句——"团队已有规范则跟随团队"，这句话把"个人偏好"升华成"工程协作意识"，直接加分。
- **深挖方向**：追问 "如果二者功能差不多你怎么权衡"，答「看团队已沉淀的基建（代码生成/插件/文档）和团队熟悉度，选型贴齐团队，少一个学习成本和维护面」。

**扣分项：** 贬低 MyBatis-Plus 吹 Flex（显得不成熟）；说不出理由就说"Flex 更好"。

### Q40：如果让你重新做灵犀项目，你会改进什么？
**参考回答：** 第一，将任务状态从纯内存迁移到持久化存储，支持服务重启恢复；第二，增加 prompt、模型、节点和 token 的可观测性；第三，为外部图源增加超时、熔断和配额控制；第四，建立离线评测集，分别评估结构完整性、内容质量、首字延迟和成本。

**关键点解析：**
- **面试官意图**：考察「复盘与反思」能力——技术经理录取标准之一就是"知道自己方案的边界"。这道题答得好，之前所有"坑"都能转化为亮点。
- **怎么答**：按「可靠性 → 可观测性 → 容错 → 评测」四个维度各给一条改进：持久化（重启不丢）、可观测（prompt/模型/token 追踪）、图源熔断配额、离线评测集。每条必须对应一个"当前方案的已知短板"，说明你清楚边界。
- **深挖方向**：可能会被追问 "如果让你现在就改，优先级排序"，答「先做持久化（影响数据可靠性）→ 再评测集（影响优化方向）→ 再可观测和熔断」，体现优先级判断。

**扣分项：** "我觉得挺完美的"（零反思）；改进方向空洞（"加更多功能"）。

---

## 模块四：实习深挖（41-50）

### Q41：实习期间做的最重要的事是什么？
**参考回答：** 我最看重的是打通"搜索 + RAG + SSE"的完整链路。面对关键词召回不稳、模型输出不可控、流式交互易断线三个问题，我从数据分块、检索融合、结构化输出和流式异常恢复四个点推进，最后在固定测试集上把召回提升约 35%，并把排查过程沉淀成文档。

**关键点解析：**
- **面试官意图**：考察你「述职能力」——能不能从一堆琐碎工作中提炼出有技术深度、有结果、有方法论的一件事。
- **怎么答**：选深度最大的那一件事，用「三个问题 → 四个技术点 → 一个结果 → 一个沉淀」结构讲。关键数据 35% 必须有支撑，并落到"沉淀成文档"体现工程习惯。
- **深挖方向**：面试官会顺着这个主线展开 Q42-Q50，所以选材时选你最有把握展开的方向（搜索+RAG+SSE 正好对应实习四大块内容）。

**扣分项：** 选一件"打杂"的事且说不出技术细节；数据 35% 支撑不住被追问露怯。

### Q42：AI 商品搜索的完整链路是什么？
**参考回答：** 用户输入先做预处理和查询改写，再进行多路召回——ES 负责关键词，语义向量负责近义表达；两路结果做融合排序后过滤、分页返回，同时记录查询和点击日志供效果评估。不同场景配置不同权重，避免"所有查询走同一套逻辑"。

**关键点解析：**
- **面试官意图**：考察检索系统的完整认知——召回、排序、过滤、日志评估全链路，而不是只懂"调一个接口"。
- **怎么答**：按「查询预处理/改写 → 多路召回（ES 关键词 + 向量语义）→ 融合排序 → 过滤分页 → 日志评估」五段讲，每段一句话。最后一句"不同场景不同权重"是画龙点睛，展示你懂规则可配置。
- **深挖方向**：追问 "查询改写怎么做的"，答「纠错、同义词扩展、去停用词，可以规则化也可以小模型改写」；追问 "融合排序用什么"，答「RRF 或加权求和，权重按场景配置」。

**扣分项：** 只说"ES 查一下"；缺日志评估环节。

### Q43：接口契约是怎么定的？你怎么保证和前端/其他服务对齐？
**参考回答：** 先定义数据结构和语义（字段类型、空值含义、错误码、分页规则），用 OpenAPI 或接口文档作为单一事实来源，联调前通过评审，联调时以文档为准。发现语义分歧时先改契约再改代码，并补充测试用例，而不是临时加判断。

**关键点解析：**
- **面试官意图**：考察协作工程化意识——接口设计不只是"写出来"，而是"定契约-评审-联调-变更管理"的流程。
- **怎么答**：重点词是「契约先行」：OpenAPI 文档作为单一事实来源 → 联调前评审 → 分歧时先改契约再改代码 → 补测试用例防回归。这句"先改契约再改代码"是专业度的体现。
- **深挖方向**：追问 "契约变更了怎么通知各方"，答「版本化接口（v1/v2）+ 变更说明 + 协作群公告 + 契约测试」。

**扣分项：** "跟前端说一声就行"（无流程）；没有文档和版本意识。

### Q44：高并发压测具体怎么做？用了什么工具？
**参考回答：** 我先明确压测目标，是看接口吞吐、链路延迟还是数据正确性；准备可重复的测试数据和隔离环境，用 JMeter 或 Locust 设计并发场景，逐步加压观察 QPS、P50/P95/P99、错误率、线程池、连接池和 GC；压测不止看峰值，还要看压测后的资源回收和数据一致性。

**关键点解析：**
- **面试官意图**：考察压测方法论。技术经理想听的顺序是「目标 → 环境 → 工具 → 指标 → 结论」，而不是"我点了下开始"。
- **怎么答**：五步：① 定目标（吞吐/延迟/正确性）；② 准备隔离数据（可重复、不污染生产）；③ 选工具设计场景（JMeter/Locust）；④ 监控指标（QPS、分位延迟、错误率、线程池、连接池、GC）；⑤ 分析结论 + 观察恢复（资源回收、数据一致）。"压测后的恢复观察"是少有人注意的亮点。
- **深挖方向**：追问 "发现瓶颈怎么定位"，答「逐层排查：先看负载指标（CPU/内存/网络）→ 再看连接池和线程池 → 再进慢查询/GC 日志」。

**扣分项：** "用了 JMeter"一句话结束；不知道 P95/P99 口径。

### Q45：ES + IK 分词 + DSL 多条件筛选具体怎么实现？
**参考回答：** 先设计索引：需要全文检索的字段用 text，需要精确匹配和排序的字段用 keyword；中文用 IK 分词，索引侧用更细的 ik_max_word，查询侧按召回需要选择。查询用 bool 组合 must（关键词）、filter（价格/品类等精确条件）和 sort 排序，避免深分页改用 search_after。

**关键点解析：**
- **面试官意图**：ES 项目题的标准考法。技术经理想听到"索引设计 → 分词策略 → 查询 DSL → 性能陷阱"的完整链条。
- **怎么答**：四段：① 字段映射（text vs keyword 的选择逻辑）；② IK 分词（索引层 ik_max_word 更细召回更高）；③ DSL 结构（bool + must 关键词 + filter 精确过滤——filter 不进相关性打分且可缓存，这是关键性能点）；④ 分页（避免深分页用 search_after + 游标）。
- **深挖方向**：追问 "为什么 filter 比 must 快"，答「filter 只过滤不打分，结果可缓存」；追问 "高亮怎么做"，答「ES highlight 配置在查询时指定字段」。

**扣分项：** 分不清 text 和 keyword；不知道 filter 缓存特性。

### Q46：语义召回怎么接入的？和 ES 如何协同？
**参考回答：** 文本先经过 embedding 模型转成向量，写入向量索引；查询时把用户问题向量化后做近似近邻检索。ES 负责关键词精确匹配和过滤，语义召回负责近义表达；两路结果用 RRF 或加权融合，再按分数过滤。纯向量方案在精确 ID、品类、价格过滤上并不占优，所以采用双路。

**关键点解析：**
- **面试官意图**：考察「混合检索」的选型判断力。技术经理最在意你是否理解"为什么不能只走向量"——这体现你对两种检索优缺点的真实认知。
- **怎么答**：① 嵌入+向量检索的基本流程；② ES 与向量各自擅长什么（精确 vs 语义）；③ 融合方式（RRF/加权）；④ 选型理由（纯向量在精确过滤场景不占优——这句话直接展示深度）。
- **深挖方向**：追问 "RRF 是什么"，答「Reciprocal Rank Fusion：按排名位次融合多路结果，不需要统一分数尺度」；追问 "向量检索用的什么库/索引"，答「HNSW 近似近邻」——把 HNSW 记牢。

**扣分项：** 只讲"embedding + 向量库"不提 ES 协同；说不出为什么要双路。

### Q47：RAG 知识库的文档分块策略？
**参考回答：** 优先按标题层级和语义段落切块，而不是固定字符数硬切；必要时保留少量 overlap 避免上下文被截断，并在块上保留来源、章节、更新时间等元数据用于引用和过滤。分块大小和 overlap 需要根据模型上下文和检索效果在测试集上调，不是一次定死。

**关键点解析：**
- **面试官意图**：分块是 RAG 质量和召回的关键环节。技术经理想听"为什么按语义切而不是按字数切"和"元数据意识"。
- **怎么答**：四个要点：① 按标题层级/语义段落切（保持章节语义完整）；② overlap 防止边界截断；③ 元数据（来源/章节/时间）用于引用和过滤；④ 参数需用测试集调优而非拍脑袋。最后一点体现工程化。
- **深挖方向**：追问 "overlap 设多少"，答「与切块大小相关（如块 500 字 overlap 50-100），需在测试集上验证召回效果」，追问 "怎么处理表格图片类 PDF"，答「表格转 markdown 结构、图片 OCR 提取文字成块」。

**扣分项：** "用固定字符切"（基础错误）；没有元数据意识。

### Q48：LangChain 和 LCEL 在实习中怎么用的？
**参考回答：** LangChain 提供组件和编排能力，我用它定义模型、提示词、工具和检索器，并用 LCEL 的管道语法把它们串联成可复用链；同时用结构化输出解析和回调记录调用。关键是理解"为什么这么组链、异常时链怎么降级"，而不只是能调通。

**关键点解析：**
- **面试官意图**：考察对 AI 编排框架的理解深度——LCEL 的"组件可组合"思想，以及工程意识（结构化输出、回调、降级）。
- **怎么答**：两个层次：① 组件面（model/prompt/tool/retriever 怎么定义）；② 组合面（LCEL 管道 → 可复用链；结构化输出解析；回调做监控）。最后强调"理解为何这么组 + 异常怎么降级"，把"会用"升到"懂设计"。
- **深挖方向**：追问 "LCEL 相比普通函数调用优势"，答「声明式组合、流式/批量/异步统一接口、可序列化可观测」；追问 "怎么调试链"，答「LangSmith/Langfuse 记录调用」。

**扣分项：** 只答"我用 LCEL 拼 prompt 和模型"；说不出可观测性。

### Q49：20 余组并发测试测出了什么？7 类问题分别是？
**参考回答：** 我按功能做并发验证，覆盖检索、工具调用、会话、流式输出等链路，集中暴露了检索超时、工具调用偶发失败、会话状态在并发下互相污染、SSE 偶发断流、重复消息消费、缓存失效瞬间穿透、模型输出格式异常七类。每类问题我定位到具体代码和日志，修复后补充测试和降级策略，避免同类型问题回归。

**关键点解析：**
- **面试官意图**：验证 7 类问题真实性 + 你的「问题定位-修复-防回归」闭环能力。技术经理会随机挑一类让你展开。
- **怎么答**：先分类列出 7 类（能说出具体类型本身就说明真实），再强调"每类定位到代码日志、修复后补测试和降级、防回归"。建议预先为每一类准备一个「症状-定位-修复」小故事（其中 3 个必准备：会话污染、缓存穿透、SSE 断流）。
- **深挖方向**：面试官必挑一类追问细节，比如"会话状态并发污染怎么定位的"，提前准备"日志里发现 A 会话夹带了 B 用户上下文 → 定位到 ThreadLocal/共享缓存未按 thread_id 隔离 → 修复为 key 加入 thread_id"的完整链条。

**扣分项：** 列不出具体问题类型；说不清任何一类的定位过程（数字遭质疑）。

### Q50：实习中最大的成长是什么？
**参考回答：** 最大的转变是从"能写代码"到"关注系统能不能稳定运行"。实习让我理解模型返回不可控、并发会放大问题、日志和可观测性决定排查效率，也让我建立了"测试 → 修复 → 文档 → 复盘"的习惯。这直接影响我现在设计接口时会主动考虑超时、幂等、降级和成本。

**关键点解析：**
- **面试官意图**：考察反思深度和成长轨迹。这道题答好能收束整个实习模块，把之前所有技术点升华成"认知成长"。
- **怎么答**：用「认知转变 + 具体证据 + 行为改变」三段：转变（能写代码 → 系统能稳定运行）、证据（模型不可控/并发放大/可观测性决定排查效率）、行为改变（现在设计接口主动加超时幂等降级成本）。"对现在的影响"是点睛。
- **深挖方向**：可主动举个例子连接 Q22（压测发现问题），证明成长不是空话。

**扣分项：** 泛泛而谈"我成长了很多"；只讲技能不讲认知转变。# 技术经理面试 100 问 · QA 全集（关键点详解版 · Part 2）

## 模块五：Java 基础 & 并发（51-65）

### Q51：HashMap 底层原理？JDK7 和 JDK8 区别？
**参考回答：** JDK7 用数组+链表，头插法，多线程扩容时可能产生环形链表导致死循环。JDK8 改为数组+链表+红黑树，链表长度超过 8 且数组长度≥64 时转红黑树，尾插法避免死循环，扩容时高低位拆分提高效率。

**关键点解析：**
- **面试官意图**：HashMap 是 Java 面试的"入场券"。技术经理通过它快速判断你 Java 基础是否扎实、是否读过源码。他能接受你说记不全细节，但不能接受你连数据结构都说不清。
- **怎么答**：必须覆盖三层：① 数据结构（数组+链表，JDK8 加红黑树）；② 扩容机制（负载因子 0.75、2 的幂、rehash）；③ 线程不安全（JDK7 死循环、JDK8 丢数据）。若主动补充"树化阈值 8 + 数组长度 ≥64 双条件"是明显加分。
- **深挖方向**：必追问"为什么用红黑树""为什么容量是 2 的幂"。"2 的幂"答「hash & (cap-1) 代替取模，位运算更快且分布均匀」是关键概念，务必记住。

**扣分项：** 只说"数组+链表"；不知道 JDK8 红黑树和尾插法；说不出为什么 2 的幂。

### Q52：ConcurrentHashMap 怎么保证线程安全？
**参考回答：** JDK7 用分段锁（Segment 数组，每段一把锁）降低锁粒度。JDK8 放弃分段，改用「CAS + synchronized」：空桶直接 CAS 写入，非空桶对桶头节点加 synchronized，锁粒度细化到单个桶；配合 volatile 修饰 Node 和 sizeCtl 保证可见性。扩容不阻塞读，读取无锁。

**关键点解析：**
- **面试官意图**：考察并发进阶深度。技术经理期待听到"锁粒度随版本演进"的趋势分析，而不是背结论。
- **怎么答**：核心是「为什么 JDK8 放弃分段锁」：分段锁粒度还是大、且代码复杂，JDK8 的「CAS 空桶 + synchronized 头节点」把锁粒度降到最小（单桶）。再补 volatile 可见性和无锁读，就可以自证"读过源码"。
- **深挖方向**：追问 "size() 怎么统计"（baseCount + CounterCell 数组近似值）；追问 "扩容时读线程怎么办"（ForwardingNode 转发到新数组）。这两点都答出 = 稳过。

**扣分项：** "它用了分段锁"一句话；不知道 JDK8 的 CAS+synchronized 演进。

### Q53：线程池核心参数有哪些？怎么选型？
**参考回答：** 六大参数：corePoolSize（核心）、maximumPoolSize（最大）、workQueue（任务队列）、keepAliveTime（非核心存活时间）、threadFactory（线程工厂）、handler（拒绝策略）。选型：CPU 密集 core = N+1；IO 密集 core = 2N 左右，**队列必须用有界队列**（ArrayBlockingQueue）防止任务堆积 OOM。

**关键点解析：**
- **面试官意图**：线程池是并发核心题。技术经理考察要点是「参数会背但会不会选型」——他会追问题的配置、队列有界无界、拒绝策略。
- **怎么答**：先背全六大参数，重点讲"队列必须有界"（无界队列下 maximumPoolSize 形同虚设，任务无限堆积 OOM——这是最常见的生产事故）。选型公式只说结论（N+1/2N）即可，重点是逻辑。
- **深挖方向**：必追问"任务提交流程"（核心满→进队列→队列满→扩到最大→再满走拒绝策略——四步流程要背熟）；追问"IO 密集为什么 2N"（IO 等待让出 CPU）。

**扣分项：** 参数背不全；用无界队列还不自知；听不懂"队列满才扩线程"的流程。

### Q54：线程池的拒绝策略有哪些？你们项目用的哪个？
**参考回答：** 四种：AbortPolicy（默认，抛异常）、CallerRunsPolicy（调用方线程执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢弃队列最老任务）。项目用 CallerRunsPolicy——不丢任务，让调用方（通常是主请求线程）执行，形成天然背压降速，比抛异常更平滑。

**关键点解析：**
- **面试官意图**：考察拒绝策略的「场景化选择」——面试官期待你不仅背出四种，还要说出"什么时候选哪种、你的项目为什么选它"。
- **怎么答**：四种一句话即够，重点讲你的选型理由：CallerRuns 的语义是"谁提交谁执行"，等于把压力回传给调用方形成背压，任务不丢失只是变慢——适合任务不能丢的场景；并说明另一种常用组合（非核心任务用 DiscardOldest + 告警监控）。
- **深挖方向**：追问 "如果调用方线程也忙怎么办"，答「CallerRuns 只是压给调用方，调用方线程本身忙导致整体变慢——这正是背压想要的效果；要看业务是否能承受，不能承受就告警落库做补偿」。

**扣分项：** 只知道 AbortPolicy；说不出为什么选某项（背策略名）。

### Q55：synchronized 和 ReentrantLock 区别？
**参考回答：** synchronized 是 JVM 层面实现、自动释放锁、非公平、不可中断；ReentrantLock 是 JDK API，需手动 unlock（放 finally）、支持公平/非公平、可中断、可超时、可绑定多个 Condition 条件队列。性能上 JDK6 后两者差距很小。

**关键点解析：**
- **面试官意图**：经典对比题。技术经理期待的是「从实现层面和功能层面两个维度」的结构化对比，而不是零散罗列。
- **怎么答**：两个维度各两条：① 实现/使用（JVM vs API、自动 vs 手动）；② 功能（公平性、可中断、可超时、多个 Condition）——这四项是 ReentrantLock 的独有能力。最后补"JDK6 后性能差距不大"是严谨性体现。
- **深挖方向**：追问 "Condition 有什么用"，答「实现精确唤醒，如生产者-消费者里分别阻塞/唤醒不同角色；synchronized 只能 wait/notifyAll 全体」；追问 "锁的底层"，答「synchronized 用 Monitor 对象头标志位，ReentrantLock 用 AQS+CAS」。

**扣分项：** "synchronized 快 ReentrantLock 慢"（过时认知）；说不出可中断/可超时/公平性。

### Q56：volatile 关键字的作用？
**参考回答：** 两个作用：① 保证可见性——写操作立即刷回主存，读操作强制从主存读；② 禁止指令重排——通过内存屏障保证有序性。但它不保证原子性——volatile 变量的 i++ 仍是线程不安全的，需要 AtomicInteger 或锁。

**关键点解析：**
- **面试官意图**：判断题。技术经理常用"volatile 能解决 i++ 并发问题吗"来筛人——很多人栽在"知道可见性、不知道不保证原子性"。
- **怎么答**：先说两个作用（可见性 + 禁止重排），必须强调「不保证原子性」。可以举项目例子：单例模式的双重检查锁 + volatile 防止"半初始化对象被读到"——这是最经典的应用，体现实用性。
- **深挖方向**：追问 "什么是内存屏障"，答「volatile 写后插入 StoreStore/StoreLoad 屏障，读前插入 LoadLoad/LoadStore 屏障，保证 CPU 层面顺序」——能说出口即超出多数候选人；追问 "为什么双重检查锁要 volatile"，正是半初始化问题。

**扣分项：** 忘了"不保证原子性"；把 volatile 说成能解决 i++。

### Q57：ThreadLocal 原理？为什么线程池下会出问题？
**参考回答：** 每个 Thread 持有一个 ThreadLocalMap，key 是 ThreadLocal 对象（弱引用），value 是用户数据（强引用）。线程池复用线程，若 ThreadLocal 不再被引用，key 变 null 但 value 还挂在 map 上无法被回收 → 内存泄漏。正确做法：finally 中 remove；跨线程传递用 TTL（TransmittableThreadLocal）。

**关键点解析：**
- **面试官意图**：ThreadLocal 有两个深度考点：内存泄漏（弱引用 key）和线程池传递（串号）。技术经理会先考一个，再顺着考另一个。
- **怎么答**：三段：① 结构（Thread→ThreadLocalMap→弱引用 key + 强引用 value）；② 泄漏链条（线程复用 + key 变 null + value 残留 → 内存泄漏）；③ 解决（finally remove + TTL/显式传参）。务必用 Q20 项目里的实例（拦截器 set + finally remove）呼应。
- **深挖方向**：追问 "为什么 key 要设计成弱引用"，答「弱引用 + 每次 get/set 顺带清理 null key，是折中优化，但 remove 才根治」——这个"两种方案都理解"的回答层次最高。

**扣分项：** 不知道弱引用 key；只说"用完 remove"但讲不清泄漏根因。

### Q58：AQS 原理了解吗？
**参考回答：** AbstractQueuedSynchronizer 是 JUC 的核心基类。核心两个要素：① state 状态变量（volatile）；② CLH 变体双向队列存放等待线程。独占模式（ReentrantLock：state 表示重入次数）和共享模式（Semaphore：state 表示剩余许可）。获取失败入队自旋/阻塞，释放时唤醒队头。

**关键点解析：**
- **面试官意图**：这道题是并发能力的"分水岭"——背下来的和平时刻关注过源码的一听便知。技术经理用它判断你能否深入底层。
- **怎么答**：五要素：① 定位（JUC 基类，ReentrantLock/CountDownLatch/Semaphore 都基于它）；② state；③ CLH 队列；④ 独占/共享；⑤ 获取-释放流程（tryAcquire 失败入队 → park → 释放唤醒）。能讲清"tryAcquire 留子类实现 + acquire 模板方法"就是读过源码的证据。
- **深挖方向**：追问 "公平锁和非公平锁在 AQS 的差别"，答「非公平：新线程先 CAS 抢一次；公平：直接看队列是否有前驱（hasQueuedPredecessors）」；追问 "为什么叫 CLH"，能说出"基于前驱节点的自旋锁"概念。

**扣分项：** 只答"AQS 是抽象队列同步器"；说不出 state 和独占/共享模式。

### Q59：乐观锁和悲观锁怎么选？
**参考回答：** 悲观锁假设冲突多，先加锁再操作（synchronized、Redisson）；乐观锁假设冲突少，操作时校验版本（CAS、版本号）。选型看冲突概率：写多读少、冲突频繁用悲观锁；读多写少、冲突少用乐观锁 + 自旋。项目中库存扣减用悲观锁保证确定性，普通更新用乐观锁避免长等待。

**关键点解析：**
- **面试官意图**：考察「锁的选型思维」——技术经理期待你给出"什么时候选什么"的标准，而不是只背定义。
- **怎么答**：核心是「冲突概率决定选型」：冲突高 → 悲观（自旋浪费 CPU）；冲突低 → 乐观（无锁开销）。结合项目例子（扣库存用悲观锁 + 普通更新用乐观锁），并补一句"乐观锁需要重试机制处理 CAS 失败"。
- **深挖方向**：追问 "CAS 失败怎么办"，答「自旋重试有限次或版本号 + 报错让用户重试」；追问 "乐观锁在数据库层怎么实现"，答「UPDATE ... SET version=version+1 WHERE version=?，影响行数为 0 即冲突」。

**扣分项：** 分不清两种锁的开销假设；没有项目实例。

### Q60：CompletableFuture 怎么用？
**参考回答：** 异步编排工具。supplyAsync 创建异步任务，thenApply/thenCompose 做依赖转换，thenAccept/thenCombine 消费或合并结果，allOf 等待多个并行任务，exceptionally/handle 处理异常，cancel/complete 手动完成。它可以替代回调地狱，把"多个下游调用并行 + 汇总"写得很清晰。

**关键点解析：**
- **面试官意图**：考察异步编程能力。技术经理期待有真实使用场景（并行调用 + 汇总），而不是背方法名。
- **怎么答**：一两个方法组 + 一个真实场景就够：如"页面详情 = 用户信息 + 订单列表 + 商品推荐三个服务并行调用，allOf + thenApply 合并，exceptionally 设置兜底"。强调「并行调用显著降低 P95」。
- **深挖方向**：追问 "和 Future/线程池什么关系"，答「底层用 ForkJoinPool.commonPool 或自定义 Executor；不指定线程池时注意公共池被阻塞」；追问 "异常怎么传播"，答「exceptionally/handle 捕获，whenComplete 不吞异常」。

**扣分项：** 只背方法名无场景；答"和 Future 差不多"（没理解组合编排本质）。

### Q61：JVM 内存结构说一下？
**参考回答：** 五大区域：① 堆——对象实例、GC 主战场，分新生代（Eden/S0/S1）和老年代；② 方法区/元空间——类元数据、常量池，JDK8 后元空间用本地内存；③ 虚拟机栈——栈帧（局部变量表、操作数栈、动态链接、返回地址）；④ 本地方法栈——native 方法；⑤ 程序计数器——当前执行字节码行号，唯一不会 OOM 的区域。

**关键点解析：**
- **面试官意图**：JVM 基础必考题。技术经理期待「五大区域 + 堆分代 + 元空间变化」完整，并能区分"线程私有 vs 线程共享"。
- **怎么答**：按「共享 vs 私有」分类讲：共享 = 堆 + 方法区；私有 = 栈 + 本地方法栈 + 程序计数器。这个分类能展示结构感，也让"哪些区域会 OOM"顺理成章（栈 OOM=StackOverflow、堆/元空间 OOM）。
- **深挖方向**：必追问 "JDK8 方法区变化"，答「永久代 → 元空间（本地内存），字符串常量池移到堆，极大减少 PermGen OOM」；追问 "栈里有什么"，答「局部变量表、操作数栈、动态链接、返回地址」。

**扣分项：** 漏掉分代或元空间；分不清哪些区域线程私有。

### Q62：垃圾回收算法有哪些？
**参考回答：** 四种基础算法：① 标记-清除——有碎片；② 标记-整理——无碎片但慢，适合老年代；③ 复制——新生代用（Eden/S0/S1），浪费空间可容忍；④ 分代收集——新生代复制 + 老年代标记整理组合。收集器演进：Serial → Parallel → CMS（标记清除）→ G1（Region 分块 + 可预测停顿）→ ZGC（着色指针 + 读屏障，亚毫秒停顿）。

**关键点解析：**
- **面试官意图**：考察 GC 知识框架的完整性。技术经理期待"算法 → 收集器演进 → 为什么演进"的链条，而不是零散名词。
- **怎么答**：先四种算法各一句（含适用代），再点出「分代收集是组合拳」，最后收集器演进一句话（G1 用 Region + 可预测停顿目标是亮点）。不用展开每个收集器细节，别被带进 CMS 细节的大坑。
- **深挖方向**：追问 "为什么新生代用复制"，答「存活对象少，复制代价低；标记整理要全量扫描」；追问 "G1 和 CMS 区别"，答「CMS 标记清除有碎片 + 并发失败退化；G1 用 Region 局部回收 + 停顿可预测」。

**扣分项：** 把算法和收集器混为一谈；说 CMS 是"最快"的（过时）。

### Q63：线上 OOM 怎么排查？
**参考回答：** 完整链路：① 先看监控和 GC 日志，确认是堆还是元空间 OOM；② jmap -heap 看堆概况，必要时 -dump 生成转储文件；③ 用 MAT/VisualVM 分析大对象、GC Roots 引用链；④ jstack 看线程（死锁、线程数暴涨）；⑤ 代码层常见原因：大集合未释放、流未关闭、ThreadLocal 泄漏、缓存无上限。定位后结合代码修复并加告警。

**关键点解析：**
- **面试官意图**：考察实战排查能力——技术经理要"能救火"的人。期待完整工具链而不是"我加内存了"。
- **怎么答**：按「确认类型 → 抓证据（dump）→ 分析（MAT）→ 定位代码 → 修复 + 防复发」五步走。主动提"ThreadLocal 泄漏"把它和 Q57 呼应起来，展示知识串联。强调「加 -Xmx 只是治标，定位大对象才是治本」是有水平的收尾。
- **深挖方向**：追问 "dump 文件很大怎么分析快"，答「先看 MAT 的 Leak Suspects + 支配树排序大对象，别全量扫」；追问 "内存涨而不崩怎么排查"，答「定时 dump 对比堆变化 + 看存活对象增长」。

**扣分项：** "重启就好了"（灾难性回答）；只会 jmap 不会分析。

### Q64：JVM 调优做过吗？
**参考回答：** 有基本实践：堆大小 -Xms=-Xmx 避免运行时扩缩堆（也避免启动后 GC 抖动）、按对象特征调 NewRatio 控制新生代比例、大堆场景用 G1 并参考 GC 日志调 MaxGCPauseMillis。更深入的调优（Region 划分、ZGC 实战）还在积累中——应届生阶段会基于 GC 日志做参数调整，而不是背调优参数表。

**关键点解析：**
- **面试官意图**：这是"诚实度 + 实战度"考题。技术经理很容易识破"背参数表"，但也接受应届生坦诚实践有限——前提是动手调过并知道为什么。
- **怎么答**：给 2-3 个真实调过的参数 + 理由（-Xms/-Xmx 相等防抖动、NewRatio 调新生代、GC 日志驱动决策），结尾坦诚"深入学习中"并给出下一步路线（读丁雪峰《深入理解JVM》/看 G1 数据）——诚实 + 行动 = 高印象分。
- **深挖方向**：追问 "怎么判断要不要调"，答「先看 GC 日志：Full GC 频率、停顿时长、吞吐比，有指标才调」——"以指标驱动调优"这句话是核心。

**扣分项：** 背一堆参数说不出理由（穿帮）；"没调过"且无补充（错失展示机会）。

### Q65：Java 8 的新特性用过哪些？
**参考回答：** 用过的：Lambda 表达式、Stream API（集合过滤/分组/聚合）、Optional（空值防御）、CompletableFuture（异步）、接口默认方法。项目里 Stream 用在列表筛选聚合，Optional 用在接口返回避免 NPE，方法引用让代码更简洁。

**关键点解析：**
- **面试官意图**：检查你是否真的在代码里用 Java 8 特性，而不是"知道功能"——技术经理通过项目例子验证。
- **怎么答**：每个特性必须挂一个项目里的真实用法（Stream 过滤分组、Optional 防空），重点强调"Optional 是用来返回值防 NPE 的，不是当参数或字段"——这句细节最能区分使用深度。可以顺势提"Spring Boot 3 已基于 Java 17"衔接项目版本认知。
- **深挖方向**：追问 "Stream 的中间/终止操作区别"，答「中间操作惰性、可链式，终止操作触发执行（collect/forEach）」；追问 "Optional 怎么用优雅"，答「orElseGet 延迟取值、orElseThrow 抛业务异常」。

**扣分项：** 只会背特性不知道用法；把 Optional 当参数/字段（使用反模式）。

---

## 模块六：Spring & MyBatis-Plus（66-78）

### Q66：Spring IOC 和 AOP 分别是什么？
**参考回答：** IOC（控制反转）：对象创建和依赖关系交给容器管理，类之间通过接口依赖，容器在运行时注入，达到解耦和易测试；最常见的注入方式是构造器注入。AOP（面向切面）：把日志、事务、校验、耗时统计等横切逻辑从业务代码抽离，运行期通过 JDK 动态代理或 CGLIB 生成代理增强。

**关键点解析：**
- **面试官意图**：Spring 两大核心思想。技术经理期待「为什么用 IOC（解耦/可测试）、AOP 解决了什么（横切关注点）」的"目的"理解，而非名词定义。
- **怎么答**：IOC 讲"控制从'自己 new'反转给容器，好处是解耦 + 易测试"；AOP 讲"横切关注点 + 代理实现"；两者各带一个项目应用（依赖注入 / 事务+日志切面）。若能区分构造器 vs 字段注入（构造器保证不可变和必填校验）是加分细节。
- **深挖方向**：追问 "IOC 和 DI 区别"，答「IOC 是思想，DI 是落地方式（注入就是容器把依赖给进来）」；追问 "AOP 有哪些通知类型"，答「Before/After/AfterReturning/AfterThrowing/Around」。

**扣分项：** 只背缩写；说不清 AOP 解决什么问题。

### Q67：Spring Bean 的生命周期？
**参考回答：** 实例化 → 属性填充（依赖注入）→ Aware 回调（BeanName/BeanFactory/ApplicationContext）→ BeanPostProcessor 前置 → 初始化（@PostConstruct / InitializingBean / init-method）→ BeanPostProcessor 后置 → 使用 → 容器关闭销毁（@PreDestroy / DisposableBean / destroy-method）。

**关键点解析：**
- **面试官意图**：Spring 核心细节题。技术经理期待你能记住"AOP 代理生成在 BeanPostProcessor 后置阶段"这个关键连接——这直接关系到循环依赖和事务失效的理解。
- **怎么答**：按「实例化 → 注入 → 初始化 → 使用 → 销毁」五段背熟，其中 Aware 回调和 BeanPostProcessor 前后置是重点标记。结尾补一句"AOP 代理对象在 BeanPostProcessor#postProcessAfterInitialization 阶段创建"——这句把生命周期和代理联系起来了。
- **深挖方向**：追问 "InitializingBean 和 @PostConstruct 哪个先执行"，答「@PostConstruct（CommonAnnotationBeanPostProcessor 在 BeanPostProcessor 前置阶段）→ InitializingBean → init-method」；追问 "为什么需要 Aware"（让 Bean 拿到容器资源）。

**扣分项：** 顺序记错；不知道 BeanPostProcessor 是关键扩展点。

### Q68：Spring 循环依赖怎么解决？
**参考回答：** 通过三级缓存：一级存完整 Bean，二级存提前暴露的半成品，三级存 ObjectFactory 工厂。A 依赖 B、B 依赖 A 时：A 实例化后把 ObjectFactory 放三级缓存，需要注入 B 时先尝试创建 B，B 又依赖 A——此时从三级缓存拿到 A 的早期引用（提前暴露），B 完成后 A 再注入 B。只有单例 + setter/字段注入可解决，构造器注入会在实例化前就互相等不到对方。

**关键点解析：**
- **面试官意图**：并发/Spring 高频压轴题。技术经理考察你「三级缓存各存什么 + 为什么需要三级 + 什么场景解决不了」的完整链条。
- **怎么答**：讲清三个关键点：① 三级缓存各自的职责（成品/半成品/工厂）；② 为什么三级而不是两级——需要 ObjectFactory 在提前暴露时支持 AOP 代理（代理只能在原始对象基础上生成，不能提前固化）；③ 构造器注入无法解决的根因（实例化就需要依赖）。
- **深挖方向**：追问 "为什么不能只用两级"，答「早期对象没经过 AOP 代理，若在暴露时就要代理就只能通过三级缓存动态生成」——这是最深层的考点；追问 "怎么排查循环依赖"，答「对比 DefaultSingletonBeanRegistry 日志 + 断点看三个 map」。

**扣分项：** 只会背"三个 map 名字"；说不出为什么需要三级缓存。

### Q69：@Transactional 事务失效的场景？
**参考回答：** 常见五类：① 方法不是 public（代理无法拦截）；② 同类内部调用——this 调用绕过代理对象，注解不生效（自调用问题）；③ 异常被 catch 了没抛出去，事务感知不到；④ 默认只回滚 RuntimeException 和 Error，checked 异常需要 rollbackFor=Exception.class；⑤ 代理未生效（写了 @Transactional 但没走 bean 代理），或多数据源没指定 transactionManager。

**关键点解析：**
- **面试官意图**：这是 Spring 高频"坑题"。技术经理期待你「每类都能说一个典型场景 + 解决方案」，这是真实生产经验试金石。
- **怎么答**：五类必须全：非 public、自调用、catch 吞异常、checked 异常、代理失效。**自调用是最高频**，补充解决（注入自身代理对象 self 调用 / 拆到另一个 Bean / 用 TransactionTemplate）；**catch 吞异常**也必考，解决是「catch 里手动 setRollbackOnly 或重新抛出」。
- **深挖方向**：追问 "自调用怎么解决最优雅"，答「方式一注入 ApplicationContext 取自身代理，方式二拆 Service 让调用走代理，方式三用 TransactionTemplate 编程式事务——我推荐拆 Bean 最干净」；追问 "事务传播 REQUIRES_NEW 场景"，答「日志独立提交不随主事务回滚」。

**扣分项：** 只记住一两类；说不出自调用的解决方式。

### Q70：Spring Boot 自动配置原理？
**参考回答：** 入口是 @SpringBootApplication（组合 @EnableAutoConfiguration）。它通过 AutoConfiguration.imports 加载所有候选自动配置类，每个候选类内部用 @Conditional 系列（类是否存在、属性是否配置、Bean 是否缺失）判断是否生效。因此开发不用写大量 XML——"约定优于配置 + 按需条件装配"。

**关键点解析：**
- **面试官意图**：Spring Boot 灵魂问题。技术经理期待一个"从注解入口到条件装配"的完整链路，并理解为什么能"零配置跑起来"。
- **怎么答**：三个环节：① 入口注解组合（@SpringBootConfiguration + @EnableAutoConfiguration + @ComponentScan）；② 自动配置类加载机制（imports 文件，Spring Boot 2.7+ 是 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports）；③ @Conditional 条件装配。能说出 imports 文件名说明真看过。
- **深挖方向**：追问 "怎么自定义 starter"，答「写 AutoConfiguration + imports 注册 + @ConditionalOnClass/@ConditionalOnMissingBean 控制生效」；追问 "条件不满足会怎样"，答「对应的 Bean 不注册，走回退配置」。

**扣分项：** 只知道"约定大于配置"这句话；说不清 @Conditional 机制。

### Q71：Spring AOP 用 JDK 还是 CGLIB 代理？
**参考回答：** 默认规则：目标类实现了接口 → JDK 动态代理（基于接口）；没有接口 → CGLIB（生成子类）。但 Spring Boot 2.x 起默认 proxyTargetClass=true，统一用 CGLIB。注意：CGLIB 对 final 方法无法代理、非 public 方法也有限制。

**关键点解析：**
- **面试官意图**：考察 AOP 底层机制细节。技术经理通过这道题判断你是否真用过 Spring Boot（很多人还在背"JDK 默认"的老黄历）。
- **怎么答**：先答经典规则（有接口 JDK / 无接口 CGLIB），立刻补"Spring Boot 2.x 默认 proxyTargetClass=true 全走 CGLIB"——这句话是"版本敏感度"的证明。再补 CGLIB 限制（final/非 public），体现完整认知。
- **深挖方向**：追问 "JDK 代理和 CGLIB 实现原理"，答「JDK 基于 InvocationHandler + 接口；CGLIB 通过 ASM 生成子类字节码，方法被改写」；追问 "怎么强制 JDK"，答「spring.aop.proxy-target-class=false」。

**扣分项：** 还在答"默认 JDK 代理"（不懂 Boot 2.x 变更）；两种代理的实现原理说不清。

### Q72：MyBatis 工作原理？
**参考回答：** 配置解析成 Configuration（含 MappedStatement）→ SqlSessionFactory 构建 SqlSession → 执行时通过 Executor → 根据 MappedStatement 组装 SQL 和参数 → JDBC 执行 → 结果映射回对象。Mapper 接口是动态代理（MapperProxy）：调用接口方法时按「全限定名.方法名」找到对应 MappedStatement 执行。

**关键点解析：**
- **面试官意图**：MyBatis 必考题。技术经理期待核心链路（SqlSessionFactory → Executor → MappedStatement → JDBC）+"Mapper 动态代理"这个关键点。
- **怎么答**：先链路后重点：把"Mapper 接口为什么不用实现类"讲清楚（MapperProxy 动态代理 + 方法签名定位 SQL），这是 MyBatis 最精髓的设计。再加一句"MyBatis-Plus 的分页/条件构造都是在这个 Executor 层用插件扩展的",衔接项目里的 MP。
- **深挖方向**：追问 "一级二级缓存挂在哪"，答「一级在 BaseExecutor 的 localCache（SqlSession 级），二级在 CachingExecutor（Mapper 级）」——把缓存实现位置说出来是深度的体现。

**扣分项：** 只背"SqlSession 执行 SQL"；说不清 MapperProxy 动态代理。

### Q73：MyBatis 一级缓存和二级缓存？
**参考回答：** 一级缓存是 SqlSession 级别，默认开启，同一次会话内相同 SQL（含参数）直接复用结果；增删改执行后自动清空。二级缓存是 Mapper 命名空间级别，跨 SqlSession 共享，需手动开启并配置缓存实现和序列化。二级缓存适合读多写少、对实时性要求不高的数据；分布式环境要谨慎，避免缓存了跨节点不一致的数据。

**关键点解析：**
- **面试官意图**：考察缓存机制的「范围 + 默认行为 + 适用边界」。技术经理重点看你会不会">二级缓存有坑"。
- **怎么答**：三个层次：① 一级（SqlSession 级、默认开、更新清空）；② 二级（Mapper 级、需开启、跨会话）；③ 工程判断——"二级缓存适合读多写少且不敏感实时性的数据，分布式要谨慎"。最后这句判断力比背诵值钱。
- **深坑方向**：追问 "为什么增删改会清缓存"，答「MyBatis 在 update 时清了所在 Mapper 的缓存边界，避免脏读」；追问 "多表 join 的二级缓存问题"，答「查 A 表命中缓存但 B 表更新了，缓存不失效 → 脏数据，所以多表查询慎用二级缓存」。

**扣分项：** 只会说"一二级缓存存在"；不知道多表 join 的脏读坑。

### Q74：MyBatis-Plus 比 MyBatis 多了什么？
**参考回答：** 增强而非替换：① 通用 Mapper——单表 CRUD 免写 XML；② 条件构造器（LambdaQueryWrapper）类型安全；③ 内置分页插件（物理分页）；④ 逻辑删除、字段自动填充、乐观锁插件；⑤ 代码生成器。复杂/多表 SQL 仍走手写 XML。底层原理：基于 MyBatis 插件机制（Interceptor）改写 Executor 行为。

**关键点解析：**
- **面试官意图**：考察「MyBatis 增强工具的理解 + 适用边界」。技术经理期待你既知道它能省什么，又知道它不能替代什么。
- **怎么答**：列 4-5 个核心能力（通用 CRUD/条件构造器/分页/逻辑删除/自动填充），特别强调"复杂 SQL 仍手写 XML"（这是边界认知，防止面试官觉得你只会 CRUD 生成器）。点出原理"插件机制扩展 Executor"衔接 Q72。
- **深挖方向**：追问 "逻辑删除怎么实现"，答「全局配置 @TableLogic 字段，SQL 自动加 deleted=0 条件，查询自动过滤已删数据」；追问 "乐观锁插件怎么用"，答「@Version 字段 + Interceptor 自动生成 UPDATE ... SET version=version+1 WHERE version=?"」。

**扣分项：** 把 MP 说成"万能的"；不知道复杂 SQL 要走 XML。

### Q75：MyBatis 分页插件原理？
**参考回答：** 基于 MyBatis 的 Interceptor（插件）机制，拦截 Executor 的 query 方法。拿到待执行 SQL 后，先改写为 COUNT 查询获取总数（Page 对象存 total），再按不同数据库方言在原 SQL 后拼接 LIMIT/OFFSET（或 ROWNUM），实现物理分页，而不是把全表查回内存再过滤。

**关键点解析：**
- **面试官意图**：考察"插件机制"的理解深度。技术经理期待听到物理分页 vs 内存分页的区别，以及拦截器改写 SQL 的过程。
- **怎么答**：核心三句：① 拦截 Executor.query；② 改写 COUNT + 拼 LIMIT（按方言）；③ 物理分页（性能好）。主动对比"逻辑分页是查全量内存过滤"（PageHelper 早期/limit 手写错误时的坑）——体现你理解为什么必须在 SQL 层分页。
- **深坑方向**：追问 "分页插件为什么会影响性能"，答「每次分页多一条 COUNT 查询，超大偏移（LIMIT 100000,10）仍慢，用游标/条件分页替代」；追问 "手写 SQL 分页和插件的区别"，答「插件自动拼 COUNT 和 LIMIT，手写每页都要写，且方言不同语法不同」。

**扣分项：** 不知道是"改写 SQL"；以为是 MyBatis 内置功能（其实是插件）。

### Q76：MyBatis 动态 SQL 有哪些标签？
**参考回答：** 核心标签：if（条件）、where（自动处理前缀 AND/OR）、set（update 逗号）、choose/when/otherwise（分支）、foreach（集合遍历 in）、trim（自定义前后缀裁剪）、include（SQL 片段复用）。底层用 OGNL 表达式解析 if 条件。配合 #{} 占位参数防 SQL 注入。

**关键点解析：**
- **面试官意图**：基础认知题。技术经理期待你能"按用途分组"说出标签，并对 where/set 的自动处理有理解。
- **怎么答**：按用途组答：条件（if/choose）、拼接优化（where/set/trim）、循环（foreach）、复用（include）。重点讲 where 和 set 的"自动去冗余"（where 自动去除最前 AND/OR；set 自动处理末尾逗号）——这是最容易考的实现细节。foreach 的 collection 传入（list/array/Map）。
- **深挖方向**：追问 "#{} 和 ${} 区别"，答「#{} 预编译占位防注入，${} 直接拼字符串有注入风险，只能用于表名/列名等不能占位的场景」；追问 "动态 SQL 底层"，答「OGNL 表达式求值 + SqlSource 动态拼接」。

**扣分项：** 只说"if 和 foreach"；说不清 #{} 防注入。

### Q77：Spring Boot 3 和 Spring Boot 2 主要区别？
**参考回答：** 四点：① 基础设施：Java 17 起为基线，废弃大量 deprecated；② 命名空间：javax 迁移到 jakarta（如 javax.servlet → jakarta.servlet），旧代码 API 全要改；③ 原生支持：GraalVM Native Image + AOT 编译，启动快但需要编译期约束；④ 可观测性：基于 Micrometer 的 Observability 体系（HTTP/DB/MQ 自动埋点）。迁移重点在第三方库的 jakarta 兼容性。

**关键点解析：**
- **面试官意图**：考察技术栈版本演进意识——你的灵犀项目就是 Boot 3，技术经理必然会问"Boot 3 变化"来验证项目技术选型的真实性。
- **怎么答**：四点各一句：Java 17 基线、Jakarta 迁移（最重的迁移成本）、Native/AOT、可观测性。特别强调 "javax→jakarta 的迁移是把第三方库一起拉起来的工作量"——体现真实迁移经验。
- **深挖方向**：追问 "你在项目里感受到最大的变化"，答「依赖要全部换 jakarta 版本（如 Tomcat、MyBatis 兼容包）、spring.factories 换成 AutoConfiguration.imports」——能说出 imports 文件变化 = 真用过 Boot 3。

**扣分项：** 只说"Java 17"；说不出 jakarta 迁移对第三方库的影响。

### Q78：你项目中 AOP 具体用在哪些场景？
**参考回答：** 四个场景：① 统一日志和接口耗时埋点（@Around 记录耗时、入参出参、traceId）；② 登录态校验和权限控制（AOP 切在注解上，省去每个接口写校验）；③ 声明式事务；④ 统一异常处理与降级（切面捕获后走兜底）。使用边界：只做横切关注点，不包裹强业务逻辑，避免流程不透明。

**关键点解析：**
- **面试官意图**：考察"AOP 是不是真用过、会不会乱用"。技术经理反感把 AOP 滥用成"用了显得高级"。
- **怎么答**：2-3 个真实场景就够了（日志埋点 + 鉴权 + 事务），关键补一句边界：「AOP 适合横切关注点，业务逻辑不放切面，否则流程不可读、难调试」——这句"克制"是区分度最高的点。AI 项目可加"模型调用统一埋点"很自然。
- **深挖方向**：追问 "鉴权用拦截器还是 AOP"，答「过滤器（Servlet）→ 拦截器（Spring MVC）→ AOP（方法级）：鉴权用拦截器拿 Handler 更合适，AOP 适合方法级横切（日志/事务/限流）」——能讲出三者层级差异就是深度。

**扣分项：** 只说"用来做日志"；把业务逻辑塞进切面（设计反模式）。

---

## 模块七：MySQL（79-85）

### Q79：MySQL 事务隔离级别有哪些？InnoDB 默认哪个？
**参考回答：** 四个隔离级别：读未提交（脏读）、读已提交（不可重复读）、可重复读（InnoDB 默认）、串行化（彻底隔离但无并发）。InnoDB 默认的是可重复读（REPEATABLE READ），并通过 MVCC 让普通读不加锁，避免默认级别导致并发性能下降。选级要考虑业务并发和一致性：银行扣款读已提交即可，长事务统计才需要可重复读。

**关键点解析：**
- **面试官意图**：事务基础必考。技术经理期待「四级定义 + 默认 RR + 为什么能默认 RR 还有性能」的闭环。
- **怎么答**：四级各带一个"读现象"（脏读/不可重复读/幻读），明确默认 RR，重点解释「MVCC 让 RR 的普通读无锁」——这句话说明你不只背级别表。业务选级例子（不同业务不同级别）是应用意识的体现。
- **深挖方向**：追问 "RR 和 RC 的 ReadView 区别"（RR 事务内固定、RC 每次读新生成）；追问 "幻读在 RR 下怎么防"，答「MVCC 解决快照读幻读，当前读（for update）用 Next-Key Lock 防幻读」。

**扣分项：** 把默认级别说成读已提交；不知道 MVCC 让 RR 无锁。

### Q80：MVCC 是什么？怎么实现的？
**参考回答：** 多版本并发控制，让读写不互相阻塞。每行记录两个隐藏字段：trx_id（最近修改事务 ID）、roll_pointer（指向前版本 undo log，形成版本链）。普通读通过 ReadView（快照，含活跃事务列表和最小/最大事务 ID）判断哪个版本对当前事务可见。RR 的 ReadView 在事务第一个读时生成并复用，RC 每次读重新生成——所以 RR 可重复读、RC 不可重复读。

**关键点解析：**
- **面试官意图**：这是 MySQL 最深的基础题之一。技术经理通过它快速判断你是"背概念"还是"真懂实现"。
- **怎么答**：四要素：① 隐藏字段 trx_id + roll_pointer；② undo log 版本链；③ ReadView 判断规则（活跃事务可见性比较）；④ RR/RC 的 ReadView 生成时机差异。能解释"为什么 RR 可重复读、RC 不可重复读"落在 ReadView 时机上是满分级回答。
- **深挖方向**：追问 "ReadView 具体怎么判断版本可见"，答「比较版本 trx_id 与 ReadView 的 up_limit_id/low_limit_id 和活跃列表：小于 up_limit_id 可见，等于自己的可见，在活跃列表不可见，大于 low_limit_id 不可见，否则沿版本链往前找」——能说出大致规则即证真懂。

**扣分项：** 只答"Mvcc 是版本控制"；说不出 ReadView 和 RR/RC 差异。

### Q81：B+ 树索引原理？为什么不用 B 树？
**参考回答：** B+ 树的特点：① 非叶子节点只存 key 和指针，不存数据——同样页大小能装更多 key，树更矮，磁盘 IO 更少；② 叶子节点存整行（或主键），且叶子之间用双向链表串联——范围查询和排序直接在叶子链上顺序扫描；③ 所有查询都必须到叶子节点，路径固定、性能稳定。B 树每个节点都存数据，树更高、叶子无链表，范围扫描要多次回溯节点，磁盘 IO 更多。

**关键点解析：**
- **面试官意图**：索引原理必考。技术经理考察"为什么 B+ 树"背后的两个核心原因：IO 次数和范围查询。
- **怎么答**：两个核心论点：① 矮胖（非叶子不存数据 → 更矮 → 更少 IO）；② 叶子链表（范围查询顺链扫）。顺带讲"每次节点访问≈一次磁盘 IO，树高≈IO 次数"。能算出"3 层 B+ 树可存上千万数据"是量化加分的体现。
- **深挖方向**：追问 "聚簇索引和非聚簇索引区别"，答「聚簇（InnoDB 主键索引）叶子存整行，数据物理有序；非聚簇叶子存主键，查询要回表」；追问 "为什么回表慢"，答「多一次磁盘 IO，覆盖索引可避免」。

**扣分项：** 只说"B+ 树快"；说不出"非叶子只存 key"和叶子链表。

### Q82：什么情况索引会失效？
**参考回答：** 主要场景：① 违反最左前缀（复合索引查中间的列却跳过最左列）；② 对索引列使用函数或计算（LENGTH(name)>5、id+1=10）；③ 模糊查询 LIKE '%xx' 前缀通配；④ 隐式类型转换（字符串列和数字比较，或字符集不一致）；⑤ OR 连接时一侧不是索引列；⑥ 数据量小或选择性低，优化器直接全表扫。排查方法：EXPLAIN 看 key 是否用了、type 是什么。

**关键点解析：**
- **面试官意图**：考察索引使用实战。技术经理期待你把"理论失效场景"和"排查手段"（EXPLAIN）结合，防止只会背列表。
- **怎么答**：5-6 个失效场景各配一个小例子，重点解释「隐式类型转换」和「函数」两个常见坑（很多人只记得 LIKE）。结尾一定落到"用 EXPLAIN 验证 key 是否真的用了、type 是否 range 以下"——把"背场景"升级成"会排查"。
- **深挖方向**：追问 "最左前缀为什么失效"，答「复合索引 (a,b,c) 按 a→b→c 排序，跳 a 用不到整体；只用 a 或 a+b 可以，只用 b/c 不行」；追问 "= 和范围列组合"，答「范围列之后的列索引失效（只能定位到范围起点）」。

**扣分项：** 只背列表不举例子；不知道用 EXPLAIN 验证。

### Q83：EXPLAIN 的关键字段怎么看？
**参考回答：** 核心字段：① id——SQL 执行顺序；② table；③ type——访问类型，从好到差：system/const/eq_ref/ref/range/index/ALL（重点看是否 ref/range 以上，ALL 或 index 是危险信号）；④ key——实际使用的索引（与 possible_keys 对比）；⑤ rows——预估扫描行数（越小越好，和真实命中误差大时可能统计信息过期）；⑥ Extra——Using index（覆盖索引）、Using filesort（排序没吃到索引）、Using temporary（临时表）、Using where。filesort/temporary 是优化重点。

**关键点解析：**
- **面试官意图**：考察 SQL 优化的实操能力。技术经理会给一条慢 SQL 让你 explain 分析——记住字段含义和排序是硬能力。
- **怎么答**：按「type 排序（尤其 ref/range 分界）+ key 实际使用 + rows + Extra 告警信号」讲，能把 filesort/temporary 解释成"该建索引的信号"是实操级的理解。
- **深挖方向**：追问 "const/ref/range 分别代表什么"，答「const 主键或唯一命中一行；ref 普通索引等值匹配多行；range 索引范围扫描（>、between、in 优化）」，再追问 "all 和 index 区别"，答「index 扫整个索引树（比 all 扫表盘 IO 少但都该避免）」。

**扣分项：** 只会看 rows；不认识 filesort/temporary 的代价。

### Q84：慢 SQL 怎么优化？
**参考回答：** 流程化五步：① 开启慢查询日志定位慢 SQL（阈值如 1s）；② EXPLAIN 分析是否走索引、有没有 filesort/temporary；③ 针对性优化索引（缺哪补哪、复合索引设计、覆盖索引）；④ 优化 SQL 写法（避免 SELECT *、函数包索引列、子查询改 JOIN、大分页改游标）；⑤ 结构性优化（数据量大到分区/分表、读写分离）。原则：先定位再动手，先用最小改动验证。

**关键点解析：**
- **面试官意图**：综合考察 SQL 优化方法论。技术经理期待"定位 → 分析 → 优化"的流程，而不是零散技巧。
- **怎么答**：按五步流程讲，中间重点：EXPLAIN 分析 + 索引设计（含覆盖索引这种"极致优化"）+ 写法优化。"数据量真的很大才考虑分表"这句是工程判断力——应届生容易一上来就想分库分表。
- **深挖方向**：追问 "覆盖索引怎么练"，答「查询列都在索引里，直接索引返回免回表——如 SELECT a,b FROM t WHERE a=X 建 (a,b) 索引」；追问 "深分页怎么优化"，答「LIMIT 100000,10 改 WHERE id>上次最大值 LIMIT 10 或 ES search_after」。

**扣分项：** 一张嘴就"分库分表"（看不到量级）；没有定位环节直接说"加索引"。

### Q85：MySQL 锁有哪些？什么时候会死锁？
**参考回答：** 按粒度：表锁（DDL、MyISAM）、行锁（InnoDB）。InnoDB 行锁细分：Record Lock（记录）、Gap Lock（间隙）、Next-Key Lock（记录+间隙，防幻读）、插入意向锁；另有意向锁（IS/IX）用于快速判断表锁能否加。死锁成因：两个事务按相反顺序加锁，或间隙锁/插入位置互相等待。解决方案：统一加锁顺序、事务尽量短、必要索引缩小锁范围；死锁发生时代码捕获 Deadlock 异常做有限重试（回滚后重试一次）。

**关键点解析：**
- **面试官意图**：锁和死锁是 MySQL 高级题。技术经理期待"行锁细分 + 死锁成因 + 实际应对"，特别是"捕获死锁重试"这个生产经验。
- **怎么答**：三层：① 锁分类（表锁/行锁 + 行锁细分，重点 Next-Key 防幻读）；② 死锁成因（反向加锁、间隙锁）；③ 应对（统一顺序、短事务、小锁范围 + 异常重试）。用项目里的"库存扣减统一先锁商品再锁订单"举例更真实。
- **深挖方向**：追问 "怎么查死锁"，答「SHOW ENGINE INNODB STATUS 看 LATEST DETECTED DEADLOCK，能看到正在等待的锁和 SQL」；追问 "间隙锁什么时候触发"，答「RR 隔离级别下范围查询和当前读（SELECT ... FOR UPDATE 条件没完全命中索引记录时）」。

**扣分项：** 只答"行锁表锁"；不知道 Next-Key Lock 和死锁重试。

---

## 模块八：Redis（86-90）

### Q86：Redis 五种基本数据类型及使用场景？
**参考回答：** String：缓存值、计数器、分布式锁、验证码；Hash：对象字段（用户信息、购物车）；List：消息队列、最新列表、时间线；Set：去重、共同好友（交集/并集/差集）、抽奖；ZSet：排行榜、延迟队列、限流窗口。选型核心：让 Redis 操作直接满足需求，而不是取出数据在应用层再算——例如排行榜用 ZREVRANGEBYSCORE，投票用 SINTERSTORE。

**关键点解析：**
- **面试官意图**：Redis 基础必考。技术经理期待"类型 ↔ 场景"的双向映射，以及"操作即结果"的选型思维。
- **怎么答**：五个类型各 2 个典型场景即可，重点展示「操作式选型」：如"点赞去重用 Set 的 SADD 天然幂等"、"最新评论用 List 的 LPUSH+LRANGE"，"排行榜用 ZSet 的 ZADD+ZREVRANGE"。能把场景落到具体命令是加分。
- **深挖方向**：追问 "ZSet 底层结构"，答「跳表 + 哈希表（dict 存成员-分数映射）」，追问 "List 做队列的坑"，答「无 ACK、断连丢数据，可靠队列用 Stream/MQ」。

**扣分项：** 只能对类型说不出场景；类型和场景对不上。

### Q87：缓存穿透、击穿、雪崩怎么区分和解决？
**参考回答：** ① 穿透：查的数据根本不存在，缓存永远不命中，请求直击 DB。解决：布隆过滤器前置拦截 + 空值缓存（短 TTL）。② 击穿：单个热点 key 过期瞬间大量请求打 DB。解决：互斥锁重建（Redisson 锁让一个请求去查库）+ 逻辑过期（值上带过期时间，后台异步刷新）。③ 雪崩：大量 key 同时过期或 Redis 整体不可用，请求全部压到 DB。解决：过期时间加随机抖动、多级缓存、限流熔断降级 + DB 保护。项目里健康建议用的是"短 TTL + 后台刷新 + 随机抖动"组合。

**关键点解析：**
- **面试官意图**：Redis 三大经典问题。技术经理期待「区分清楚三者的成因 + 各给至少两种方案 + 有项目实践」，且不能混淆。
- **怎么答**：三个问题分别"成因 → 方案"对齐：穿透（不存在数据 → 布隆+空值）、击穿（单热点过期 → 互斥锁）、雪崩（批量过期/整体挂 → 随机 TTL+多级）。结尾落到"我在健康建议场景选了短 TTL + 后台刷新 + 抖动"，用项目实例收束。**区分度**：能说清三者"规模不同"（一个 key vs 一批 key vs 从不存在的 key）。
- **深挖方向**：追问 "逻辑过期和互斥锁哪个好"，答「互斥锁简单但有锁等待；逻辑过期无阻塞但实现复杂、有短暂脏读——看业务容忍度」；追问 "布隆过滤器误判怎么办"，答「会误拦合法请求，可把误判率参数调低并用旁路列表补偿」。

**扣分项：** 三个问题混淆（把击穿说成雪崩）；只会一种解法。

### Q88：Redis 持久化 RDB 和 AOF？
**参考回答：** RDB 是定时全量快照（二进制），体积小、恢复快，但两次快照之间的数据会丢。AOF 是追加写命令日志，可配置 appendfsync always/everysec/no——数据更安全，但文件大、恢复慢。生产常用"RDB + AOF 混合"：用 RDB 快速打底 + AOF 补增量，兼顾恢复速度和数据安全。选型看"能容忍丢多少数据"：可丢 1 分钟用 RDB，不丢数据用 everysec AOF。

**关键点解析：**
- **面试官意图**：考察持久化选型的「权衡思维」。技术经理最看重的不是背参数，而是"用可容忍丢失量来驱动选型"。
- **怎么答**：RDB/AOF 各自"机制 + 优缺点"，重点讲混合方案（Redis 4.0+ AOF 用 RDB 做 base + 增量命令）和选型判断（按可容忍丢失量）。给出"everysec 是生产常见平衡点"这种实操结论更可靠。
- **深挖方向**：追问 "RDB 的触发时机"，答「配置 save 规则（如 900s 内 1 次变更）、shutdown 前、主从全量复制时生成」；追问 "AOF 重写是什么"，答「BGREWRITEAOF 把历史命令压缩成最小命令集，解决 AOF 无限膨胀」。

**扣分项：** 只说"RDB 快 AOF 安全"；不知道混合持久化。

### Q89：Redis 集群方案了解吗？
**参考回答：** 三层演进：① 主从复制——读写分离，主写从读，解决读扩展；② 哨兵——在主从之上加监控和自动故障转移，高可用（主挂自动选新主）；③ Cluster——16384 个哈希槽做数据分片，客户端按 CRC16(key)%16384 路由，解决容量和写扩展。选型：单机够用不用集群；读多写少主从+哨兵；容量/写压力大（超单机内存）才上 Cluster。取模/一致性哈希/哈希槽的区别：哈希槽由集群统一管理，扩缩容（槽迁移）对客户端透明。

**关键点解析：**
- **面试官意图**：考察集群方案的系统认知和选型判断。技术经理期待"什么规模用什么方案"而不是背三套名词。
- **怎么答**：三个方案按"解决什么问题"讲（读扩展/高可用/容量+写扩展），补一句选型判断（按规模往上层）。能说出"CRC16 % 16384"和"槽迁移对客户端透明"是深度点。
- **深挖方向**：追问 "哨兵怎么判定主挂了"，答「主观下线（本哨兵 ping 超时）→ 客观下线（多数哨兵同意）→ 选举 leader 完成故障转移」；追问 "Cluster 的 MOVE 重定向"，答「客户端请求的槽不在当前节点时返回 MOVED 错误 + 新节点地址，客户端缓存槽位」。

**扣分项：** 混用主从/哨兵/Cluster 的功能；说不出哈希槽。

### Q90：Redis 和 Memcached 区别？
**参考回答：** ① 数据结构：Redis 丰富（String/Hash/List/Set/ZSet），Memcached 只有 String；② 持久化：Redis 支持 RDB/AOF，Memcached 纯内存重启即丢；③ 集群：Redis 有主从/哨兵/Cluster，Memcached 靠客户端分片；④ 并发模型：Redis 单线程（6.0 起 IO 多线程）+ Lua 保证原子性，Memcached 多线程；⑤ 内存淘汰：Redis 多种策略（LRU/LFU/过期），Memcached 以 LRU 为主。项目里排行榜/分布式锁/持久化需求依赖 Redis 的 ZSet 和 Lua，Memcached 无法满足。

**关键点解析：**
- **面试官意图**：考察选型对比能力。技术经理期待"功能差异 + 为什么我们选 Redis"。
- **怎么答**：四维对比（数据结构/持久化/集群/并发模型）各一句，落到项目选型理由（排行榜 ZSet、Lua 锁）。能说"Redis 6.0 单线程收窄到 IO 多线程但命令执行仍单线程"是精确认知。
- **深挖方向**：追问 "Memcached 还存在哪些场景有优势"，答「纯 KV 超大规模场景内存利用率高、多线程吞吐可观，但生态和持久化弱」——客观看待两者而非一味贬低。

**扣分项：** "Memcached 早淘汰了"（反智）；说不出维度。

---

## 模块九：Elasticsearch & 消息队列（91-95）

### Q91：ES 倒排索引原理？
**参考回答：** 数据组织方式从"文档 → 词"变为"词 → 文档"：建立索引时对文档分词，生成 Term Dictionary（词典）和 Posting List（每个词对应哪些文档 ID）。查询时先查词典命中词，再取其文档列表做合并/过滤。倒排索引的优势：一次词典查找 + 有序 postings 合并，比数据库 LIKE 全表扫描快几个数量级——这就是 ES 全文检索性能好的根因。

**关键点解析：**
- **面试官意图**：ES 必考基础。技术经理期待"正序 vs 倒排"对比 + 为什么快。
- **怎么答**：三层：① 正排 vs 倒排概念；② 结构（词典 + 倒排表 + 可选位置/词频信息）；③ 为什么快（词典可以用 FST 压缩加速查找，posting list 用跳表/位图快速合并）。能说出 FST（有限状态转换器）是超纲加分。
- **深挖方向**：追问 "ES 为什么近实时（写入后 1s 左右才能搜到）"，答「写入先到内存 buffer + translog，refresh 间隔（默认 1s）才生成可检索的 segment」——这是 ES 深度考点；追问 "segment 合并"，答「后台合并减少段数量提升查询性能」。

**扣分项：** 只答"倒排是反过来"；说不出词典+倒排表结构。

### Q92：IK 分词器 ik_smart 和 ik_max_word 区别？
**参考回答：** ik_smart：智能最少切分，词少、粒度粗，适合查询侧减少无意义分词；ik_max_word：最大切分，把组合词切到最细粒度，召回更全，适合索引侧。最佳实践：「索引用 ik_max_word、查询用 ik_smart」，既保证召回又不让查询词太碎。中文专有名词（农业术语/商品名）会被拆错，需配自定义词典扩展，或使用远程词典动态热更新。

**关键点解析：**
- **面试官意图**：考察分词选型的真实理解。技术经理期待"索引/查询两侧粒度策略"的对称思维 + 自定义词典意识（这在你的搜索项目里直接相关）。
- **怎么答**：先两者定义对比，再给"索引 max_word / 查询 smart"的最佳实践，最后补"自定义词典"（你项目里做商品搜索一定会遇到专有名词拆错的问题——关联简历里的长尾召回 35% 提升，很自然）。
- **深挖方向**：追问 "自定义词典怎么热更新"，答「配置文件 DB 化 + 定时 reload，或 ES 7.x 用 dynamic dictionary 远程拉取」；追问 "查询时为什么不用 max_word"，答「切碎后噪音词多，影响相关性打分」。

**扣分项：** 两个模式说反；不知道自定义词典。

### Q93：RocketMQ 如何保证消息不丢失？
**参考回答：** 分三段分别保证：① 生产端：同步发送 + 处理 SendResult 状态，失败重试（可设 retryTimesWhenSendFailed）或落库补偿；② Broker：同步刷盘（SYNC_FLUSH）保证内存消息落盘 + 主从同步（SYNC_MASTER）保证副本；③ 消费端：消费成功才 ACK，失败返回重试状态（RECONSUME_LATER），超过重试次数进死信队列人工处理。只保证一段不算数——三个环节都要覆盖才是"消息不丢失"。

**关键点解析：**
- **面试官意图**：MQ 可靠性必考题。技术经理期待你按"生产-存储-消费"三段完整回答——这是教科书标准答案框架，只要框架对内容就差不了。
- **怎么答**：三段结构 + 每段一个关键手段（同步发送/同步刷盘/手动 ACK），最后强调"三段缺一不可"衔接"消息从哪到哪的安全"。主动提"同步发送性能代价大，业务按可靠性级别选择"体现权衡。
- **深挖方向**：追问 "消费端重复消费怎么办"，答「业务幂等（唯一键去重）」衔接 Q94 附近；追问 "刷盘和主从同步的区别"，答「刷盘保"本机不丢"，同步保"副本不丢"，两者都做才抗单机故障」。

**扣分项：** 只答"Broker 持久化"；漏掉生产端或消费端。

### Q94：RocketMQ 事务消息原理？
**参考回答：** 流程：① 发送方发送"半消息"（消息到达 Broker 但消费者不可见）；② 发送方执行本地事务（如写订单）；③ 本地事务成功 → commit 半消息使其可见；本地事务失败 → rollback 删除；④ 若第 ② 步执行期间进程崩溃或没返回结果，Broker 会定时"回查"发送方，发送方通过回查逻辑（查本地事务执行结果）决定最终 commit/rollback。它解决的是"发消息和本地事务不一致"的最终一致性，不是强一致。

**关键点解析：**
- **面试官意图**：考察分布式事务的核心方案之一。技术经理期待"半消息 + 本地事务 + 回查"三个关键机制的完整链条。
- **怎么答**：四步流程 + 回查机制讲清楚，核心是理解"为什么需要半消息"（防止消费方先看到消息但本地事务回滚了）和"回查"（解决崩溃不确定）。结尾点明"最终一致性"定位。
- **深坑方向**：追问 "回查次数和时机"，答「Broker 定时（如 60s 一次）最多回查 N 次（如 15 次），超过进入人工处理」；追问 "和 Seata 等方案区别"，答「事务消息沿用于 MQ 的最终一致思路，Seata 提供 AT/TCC 等更体系化的分布式事务」。

**扣分项：** 说不清"半消息"概念；把事务消息说成强一致。

### Q95：Kafka 和 RocketMQ 区别？
**参考回答：** 都高吞吐，但定位不同：Kafka 吞吐极高、生态强（连接 Flink/Spark），顺序性按分区保证，适合日志采集、流处理、大数据管道；RocketMQ 提供事务消息、定时/延时消息、按 Tag 过滤、更完善的重试与死信体系，延迟较低，适合金融级/业务级可靠消息。选型看场景：海量日志流式处理选 Kafka；业务消息需要事务/定时/过滤选 RocketMQ。

**关键点解析：**
- **面试官意图**：考察 MQ 选型判断力。技术经理看重"按场景选型"而非"哪个更好"。
- **怎么答**：两段式：Kafka 的强项（吞吐/生态/流处理）+ RocketMQ 的强项（事务/定时/过滤/可靠机制），落到"业务消息 vs 数据管道"的选型结论。能点出"RocketMQ 消息顺序依赖队列、价格体系更适合电商订单"更贴你的项目。
- **深挖方向**：追问 "为什么 Kafka 吞吐那么高"，答「顺序写磁盘 + 页缓存 + 零拷贝 + 分区并行消费」——零拷贝这个词是加分点；追问 "RocketMQ 消费对实时性的控制"，答「消息做消息轨迹追踪、消费点位可重置（skip 或重放）」——能说出消息轨迹/消费追溯是深度。

**扣分项：** 直接说"Kafka 比 RocketMQ 好"（没有场景思维）。

---

## 模块十：AI 框架 & IoT（96-100）

### Q96：RAG 的检索质量怎么评估？
**参考回答：** 用带标注的离线测试集评估。指标：召回率（Top-K 中相关文档的覆盖面）、精确率（召回的文档有多少相关）、F1、MRR（第一个正确结果的排名倒数）、NDCG（考虑排序位置加权的质量）。同时要看两类错误：漏检（该召回的没召回 → 回答没依据）和噪音（召回的太多无关 → 上下文被污染）。注意：检索质量 != 最终回答质量，回答质量还要单独评估忠实性（是否有引用、是否编造）和完整性。

**关键点解析：**
- **面试官意图**：考察 RAG 的系统化思维。技术经理期待"离线评测集 + 检索指标 + 生成指标分离"，证明你不是盲目加向量库。
- **怎么答**：三层：① 离线标注集（有 ground truth 才能评估）；② 检索指标（召回/精确/MRR/NDCG，抓"第一个正确结果排名"这个 MRR 含义）；③ 区分"检索好 ≠ 回答好"——加上忠实度和引用评估。结尾补一句"在线还会看用户反馈和 CTR"是加分的完整闭环。
- **深挖方向**：追问 "召回率提高但回答变差怎么排查"，答「多步归因：先看召回文档是否相关（漏检/噪音），再看 prompt 拼接和模型是否忠实引用——用 trace 把检索结果和回答对应起来」；追问 "评测集怎么建"，答「人工标注 + LLM 辅助标注 + 随机抽样核对」。

**扣分项：** 只答"看回答好不好"没有指标；混淆检索和生成的评估。

### Q97：LangChain 的核心组件有哪些？
**参考回答：** 核心组件：① Models——LLM/ChatModel 统一调用接口；② Prompts——模板化提示词；③ Chains / LCEL——把组件串成可复用执行链；④ Memory——会话记忆管理；⑤ Agents + Tools——模型决策调用工具；⑥ Retrievers——检索接口抽象（配合 VectorStore）；⑦ Output Parsers——结构化输出解析；⑧ Callbacks——调用过程监控/token 统计。实习中实际用到 Models、Retrievers、Agents/Tools、Output Parsers 和 LCEL，重点是把它们组合成"可降级、可观测"的链路。

**关键点解析：**
- **面试官意图**：考察对框架的系统理解。技术经理期待"能枚举组件 + 能说清组件之间的关系 + 项目里实际用了哪些"。
- **怎么答**：枚举 6-8 个组件各一句话，重点讲"组件怎么协作"（Model 产出、Parser 结构化、Retriever 供给上下文、Agent 决策调工具、Callback 监控），落到实习实际用到的。强调"链路要可降级可观测"呼应你的工程观。
- **深挖方向**：追问 "LCEL 相比普通代码的优势"，答「声明式组合、统一支持 sync/async/stream、runnable 可序列化和可观测、便于在链的任何位置挂回调」；追问 "Agent 的规划怎么实现"，答「ReAct 循环：模型输出 Thought/Action，执行工具拿到 Observation 再推理」。

**扣分项：** 只背组件名说不出协作；没有项目实例。

### Q98：SSE 和 WebSocket 怎么选？
**参考回答：** 按通信方向和数据特征选：SSE 是 HTTP 上的服务端单向事件流，浏览器 EventSource 原生支持、自动重连、实现简单——适合"服务端 → 前端"的单向推送，如 AI 逐 token 输出、系统通知、日志流；WebSocket 是 TCP 上的全双工长连接，适合双向实时交互——聊天、协同编辑、游戏。AI 对话本质只有"服务端 → 前端"一个方向（用户提问一次），所以用 SSE 更轻量、省资源、天然带重连；只有需要双向频繁交互时才上 WebSocket。

**关键点解析：**
- **面试官意图**：考察实时通信选型的判断力。技术经理期待你不仅知道两者定义，还能给出"我的场景为什么选它"的推理。
- **怎么答**：三维度：① 方向（单向 vs 双向）；② 底层（HTTP vs TCP）；③ 特性（自动重连 vs 需手写）。用"AI 对话是单向流"这个本质理由收束选型——这是有说服力的工程论证。
- **深挖方向**：追问 "SSE 的缺点"，答「单向、浏览器 EventSource 有并发连接数限制（每域 6 个）、Http2 之下仍有连接数问题」——说缺点比说优点更能体现理解深度；追问 "什么时候必须 WebSocket"，答「双向实时（协作编辑、推送+上行控制）」。

**扣分项：** 不知道 EventSource 连接上限这种真实工程约束；只会说"SSE 更简单"。

### Q99：华为云 IoTDA 的设备影子是什么？
**参考回答：** 设备影子是云端维护的设备状态副本，两个属性：期望值（desired，应用侧下发待设备执行的状态）和上报值（reported，设备实际上报的当前状态）。价值：① 解耦——应用不直接连线设备，只读写影子；② 离线可用——设备离线时应用也能写期望值，设备上线后同步执行；③ 弱网适配——状态暂存，网络恢复后补传。项目里用影子做设备状态缓存和离线同步，配合"设备-位置"绑定保持一致。

**关键点解析：**
- **面试官意图**：考察云 IoT 的核心概念。技术经理期待"期望值 vs 上报值"的定义 + 为什么要影子（解耦/离线/弱网）。
- **怎么答**：定义（两个属性）→ 价值（解耦 + 离线 + 弱网）→ 项目应用（状态缓存/离线同步 + 与本地绑定逻辑的一致性）。三个层次讲完就是完整答案。
- **深挖方向**：追问 "影子数据和真实的设备状态不一致怎么办"，答「以设备上报值为准更新影子，应用侧看影子做展示，命令下发走影子期望值——一致性是最终一致，配合设备回调确认」；追问 "和 MQTT 订阅的关系"，答「设备通过 MQTT 订阅影子 topic 获取期望值，上报数据写回 reported」。

**扣分项：** 说不出期望值/上报值；不知道影子的"离线解耦"价值。

### Q100：如果入职后给你一个从零开始的 AI 项目，你怎么规划？
**参考回答：** 我按六个阶段推进：
1. 需求澄清——先回答"要解决什么问题、用户是谁、成功怎么衡量"（定指标，如回答命中率、响应延迟、成本预算），避免做出来没人用。
2. 技术选型——模型选择（效果/成本/合规）、框架（LangChain/Spring AI）、检索与存储（ES/向量库）、缓存与异步（Redis/RocketMQ），每个选型给权衡。
3. MVP 验证——端到端跑通最小闭环（一个场景一条链路），尽早暴露模型效果和链路问题。
4. 工程化——接口规范、结构化输出校验、异常与降级、日志/追踪（traceId 全链路）、压测。
5. 迭代优化——建立评测集，按指标迭代检索质量和生成质量；控制 token 成本。
6. 上线运维——灰度发布、监控告警、版本可回滚、成本看板。
核心原则：**指标先行、最小闭环先跑、可回滚兜底**，而不是先堆功能。

**关键点解析：**
- **面试官意图**：这是"开放题 + 压轴题"，考察候选人从 0 到 1 的产品工程思维。技术经理期待听到"指标体系、MVP、工程化、运维闭环"——而不只是"我会用 LangChain"。
- **怎么答**：六阶段各 1-2 句，每阶段带一个关键动作（定指标/权衡选型/最小闭环/结构化校验/评测集/灰度回滚）。结尾用三原则收束（指标先行、最小闭环、可回滚），把散点收拢成方法论。
- **深挖方向**：追问 "如果第一版就 70% 时间花在模型效果上怎么办"，答「先定可接受的底线指标，用提示词工程和检索优化快速达到底线，再上工程化——防止过度工程化滞后交付」；追问 "Token 成本怎么控制"，答「缓存高频问题答案（语义缓存）、控制召回上下文大小、选性价比模型分层（简单问题小模型）」。

**扣分项：** 一上来就列技术栈（没有需求指标意识）；只说"先搭个 Spring Boot 项目"（没有 AI 项目的特别关注点：评测、成本、降级）。

---

## 附录：面试答题框架（STAR-CE）

| 步骤 | 说明 | 示例 |
|------|------|------|
| **S**ituation | 背景：什么项目/什么问题 | "养老系统健康评估响应 8 秒" |
| **T**ask | 任务：我负责什么 | "负责健康评估引擎，目标是降到 1 秒内" |
| **A**ction | 行动：具体怎么做（技术选型+实现） | "耗时拆解 → AI 结果缓存 → 多级缓存 → 降级" |
| **R**esult | 结果：量化数据 | "响应 8s→1s，查阅效率提升 30%" |
| **C**hallenge | 挑战：最大的难点 | "缓存一致性 + 模型超时兜底" |
| **E**volution | 演进：如果重做会怎么改进 | "缓存失效广播 + 模型失败重试策略" |

> 使用建议：
> 1. 项目追问题一律用 STAR-CE 六段式，控制在 2-3 分钟。
> 2. 每道题先在心里说「面试官为什么问这道题」再开口——判断意图能让回答更贴需求。
> 3. 数字必须能展开：简历里出现的 35%、99%、50%、8s→1s、3min→10s 都要准备完整的"怎么测/怎么算"。
> 4. 场景化记忆：把 100 题按"我的项目 → 通用八股 → 开放题"分组，先练项目题，八股背关键点，开放题练方法论。