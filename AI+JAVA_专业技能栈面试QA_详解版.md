# 专业技能栈面试 QA 全集（详解版）

> 基于 `D:\简历\AI+JAVA\AI+JAVA_简历_模板覆盖版.docx` 专业技能五条线，结合项目经历，给出专业标准的面试 QA。
> 每题包含：**问题 → 参考回答 → 关键点解析（面试官意图 / 怎么答 / 深挖方向）→ 结合项目 → 常见扣分项**。
> 文末附「JVM 内存块详解」「零拷贝详解」两个专题。

---

## GOAL（专业交付目标）

| 维度 | 目标 |
|------|------|
| **产出** | 覆盖简历五条技能线的完整 QA，每题可背诵 + 可深挖 |
| **内容** | 参考回答 + 关键点解析 + 项目结合 + 扣分项，四要素齐全 |
| **深度** | JVM 内存区域逐块讲清作用；零拷贝从传统 IO 到 mmap/sendfile/splice 完整对比 |
| **结合** | 答题尽量落到养老系统、灵犀写作、传习实习三个项目 |
| **标准** | 技术经理视角，问得准、答得稳、能往深里走 |

---

## 技能线一：Java 编程 / 线程池 / JVM

### Q1：线程池的核心参数有哪些？任务提交流程是什么？
**参考回答：** 七大参数：corePoolSize（核心线程数）、maximumPoolSize（最大线程数）、keepAliveTime（非核心线程空闲存活时间）、unit（时间单位）、workQueue（任务队列）、threadFactory（线程工厂）、handler（拒绝策略）。任务提交流程：① 任务到来先判断核心线程是否满，未满创建核心线程执行；② 核心满则入队列；③ 队列满则创建非核心线程（直到 maximumPoolSize）；④ 都满则走拒绝策略。

**关键点解析：**
- **面试官意图**：考察并发基础是否扎实。技术经理期待"七大参数 + 四步提交流程"完整，并能区分"进队列"和"扩线程"的先后——这是最容易答错的点。
- **怎么答**：参数先背全，再按"核心→队列→最大→拒绝"四步讲清提交顺序。重点是"队列满才扩到最大线程"，很多人误以为"核心满直接扩线程"。
- **深挖方向**：追问"为什么用有界队列"，答「无界队列下 maximumPoolSize 形同虚设，任务无限堆积导致 OOM——这是生产事故高发点」。

**结合项目：** 灵犀写作中我用 ThreadPoolTaskExecutor + @Async 构建异步任务体系，文章生成和图片生成等耗时操作解耦为后台任务。核心线程按 IO 密集型配置（机器核数 ×2），用有界队列防止任务堆积，拒绝策略选 CallerRunsPolicy 形成背压降速，避免长任务阻塞主线程。

**常见扣分项：** 参数背不全；提交流程顺序记错（以为核心满就扩线程）；用无界队列还不自知。

### Q2：线程池怎么选型？CPU 密集型和 IO 密集型有什么区别？
**参考回答：** CPU 密集型：线程数 = N+1（N=CPU 核数），多了反而因线程切换降低吞吐；IO 密集型：线程数 = 2N 或更大，因为线程大部分时间在等 IO，多线程能提升 CPU 利用率。选型核心是"让 CPU 不闲着也不空转切换"。队列必须有界，避免 OOM。

**关键点解析：**
- **面试官意图**：考察"会不会选型"而非"会不会背参数"。技术经理想听你根据任务类型做判断。
- **怎么答**：先给公式（N+1 / 2N），再解释本质（CPU 密集切换是浪费，IO 密集多线程填补等待）。强调"公式是起点，实际要看压测"。
- **深挖方向**：追问"为什么 CPU 密集是 N+1 不是 N"，答「N 个线程跑满 CPU，多 1 个应对偶发缺页/调度抖动」。

**结合项目：** 灵犀写作的异步任务（调大模型生成文章）是典型 IO 密集型——线程大部分时间在等模型响应，所以我按 2N 配置核心线程，实测吞吐提升约 50%。养老系统的 IoT 增量同步也是 IO 密集（等华为云 IoTDA 响应），同样按 IO 密集配置。

**常见扣分项：** "统一用 2N"；不知道区分 CPU/IO 密集；不会结合实际场景。

### Q3：拒绝策略有哪些？怎么选？
**参考回答：** 四种：AbortPolicy（默认，抛 RejectedExecutionException）、CallerRunsPolicy（调用方线程执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢弃队列最老任务）。选型看业务：任务不能丢选 CallerRuns（背压降速）；可丢且要最新选 DiscardOldest；要快速失败选 Abort + 告警。

**关键点解析：**
- **面试官意图**：考察场景化选型能力。技术经理期待"知道四种 + 会按场景选"。
- **怎么答**：四种各一句，重点讲选型逻辑（能否丢、要快失败还是要降速）。
- **深挖方向**：追问"CallerRuns 的副作用"，答「调用方线程被占用执行任务，等于把压力回传给上游，形成天然背压；如果上游是 Tomcat 线程，可能导致接口变慢——这正是想要的降速效果」。

**结合项目：** 灵犀写作选 CallerRunsPolicy——文章生成任务不能丢（丢了用户任务没了），让调用方线程执行形成背压，比抛异常更平滑。实习中订单链路用 RocketMQ 削峰，本质也是类似的"降速不丢任务"思路。

**常见扣分项：** 只知道 AbortPolicy；说不出选型理由。

### Q4：常用并发工具类有哪些？CountDownLatch、CyclicBarrier、Semaphore 区别？
**参考回答：** 三大同步器：① CountDownLatch——一次性计数器，一个或多个线程等待 N 个操作完成（不可重置）；② CyclicBarrier——可重置的屏障，N 个线程互相等待到齐后一起执行（可复用，可带 barrierAction）；③ Semaphore——信号量，控制同时访问的线程数（许可数）。另外还有 Exchanger（两线程交换数据）、Phaser（增强版 CyclicBarrier）。

**关键点解析：**
- **面试官意图**：考察 JUC 工具类的场景理解。技术经理期待"三者区别 + 使用场景"，特别是 CountDownLatch 和 CyclicBarrier 的"一次性 vs 可复用"差异。
- **怎么答**：三者各给"机制 + 场景"：CountDownLatch（多任务汇总后继续）、CyclicBarrier（多线程齐步走）、Semaphore（限流）。重点对比 CountDownLatch 不可重置、CyclicBarrier 可 reset。
- **深挖方向**：追问"CountDownLatch 和 CyclicBarrier 底层"，答「都基于 AQS，CountDownLatch 用共享模式 state 做计数，CyclicBarrier 用 ReentrantLock + Condition」。

**结合项目：** 灵犀写作多智能体编排里，配图搜索和部分内容生成并行执行，可以用 CountDownLatch 等所有并行节点完成再汇聚合并结果（项目实际用 StateGraph 的汇聚节点实现类似语义）。养老系统并发压测时用 CountDownLatch 控制多线程同时发起请求，模拟瞬时并发。

**常见扣分项：** 混淆 CountDownLatch 和 CyclicBarrier；说不出使用场景。

### Q5：JVM 内存结构说一下？哪些是线程共享，哪些是线程私有？
**参考回答：** 五大运行时数据区：① 程序计数器（PC，线程私有）——当前线程执行字节码行号；② 虚拟机栈（线程私有）——栈帧（局部变量表、操作数栈、动态链接、返回地址）；③ 本地方法栈（线程私有）——native 方法；④ 堆（线程共享）——对象实例，GC 主战场，分新生代（Eden/S0/S1）和老年代；⑤ 方法区/元空间（线程共享）——类元数据、常量池、静态变量。另外还有直接内存（操作系统本地内存，NIO 堆外内存用）。

**关键点解析：**
- **面试官意图**：JVM 基础必考。技术经理期待"五大区域 + 共享/私有分类 + 堆分代"，并能区分 JDK 8 前后方法区变化。
- **怎么答**：按"私有（PC/栈/本地栈）+ 共享（堆/方法区）"分类讲，结构清晰。重点讲"JDK 8 方法区→元空间，从 JVM 内存移到本地内存"。
- **深挖方向**：追问"哪些区域会 OOM"，答「堆 OOM（对象太多）、元空间 OOM（类太多）、栈 OOM（递归过深 StackOverflowError）、直接内存 OOM（NIO 堆外内存泄漏）」。

**结合项目：** 养老系统压测时关注堆内存和 GC——健康评估缓存热点数据放堆，如果缓存无上限会堆 OOM。灵犀写作异步任务多，虚拟机栈深度和线程数要控制。实习中排查问题时用 jmap/jstack 分析堆和线程，正是基于对这些区域的理解。

**常见扣分项：** 漏掉直接内存；分不清共享/私有；不知道元空间变化。

> JVM 内存各块详解见文末「专题一：JVM 内存块详解」。

### Q6：垃圾回收机制？分代收集为什么这么设计？
**参考回答：** 分代假说：① 绝大多数对象朝生夕灭（新生代）；② 熬过越多次 GC 的对象越难回收（老年代）。所以堆分新生代（复制算法，Eden/S0/S1）和老年代（标记-整理）。新生代 Minor GC 频繁但快，老年代 Major/Full GC 少但慢。对象先分配 Eden，Minor GC 存活进 Survivor，年龄够了（默认 15）进老年代；大对象直接进老年代。

**关键点解析：**
- **面试官意图**：考察 GC 框架理解。技术经理期待"分代假说 + 算法选择 + 对象晋升流程"。
- **怎么答**：从"为什么分代"（假说）切入，再讲"怎么分"（新生代/老年代）+ "怎么回收"（复制/标记整理）+ "对象怎么晋升"。逻辑链条完整。
- **深挖方向**：追问"为什么 Survivor 分两个"，答「复制算法需要一块空区放存活对象，两区交替使用（from/to），避免碎片」；追问"大对象为什么直接进老年代"，答「大对象在新生代复制代价高，且可能提前触发 GC」。

**结合项目：** 养老系统健康建议缓存是长生命周期对象，会晋升到老年代；灵犀写作的任务状态如果频繁创建销毁是短生命周期，留在新生代被 Minor GC 回收。理解分代后，调优时知道"老年代涨得快 = 长生命周期对象多或大对象多"。

**常见扣分项：** 说不清分代假说；算法和代对应不上；不知道对象晋升流程。

### Q7：常见的垃圾收集器有哪些？怎么演进？
**参考回答：** 演进路线：Serial（单线程）→ Parallel（多线程，吞吐优先）→ CMS（并发，低停顿，标记-清除有碎片）→ G1（Region 分块 + 可预测停顿）→ ZGC/Shenandoah（着色指针/读屏障，亚毫秒停顿）。选型：单机小堆 Serial/Parallel；大堆低停顿 G1；超低延迟 ZGC。

**关键点解析：**
- **面试官意图**：考察收集器演进认知。技术经理期待"演进趋势 + 各自特点 + 选型依据"。
- **怎么答**：按时间线讲，重点 G1（Region + 停顿可预测是亮点）和 ZGC（着色指针 + 亚毫秒）。不用每个都展开。
- **深挖方向**：追问"G1 和 CMS 区别"，答「CMS 标记-清除有碎片 + 并发失败退化 Full GC；G1 用 Region 局部回收 + 停顿可预测（MaxGCPauseMillis）」。

**结合项目：** 养老系统 Spring Boot 2.7 默认用 G1（JDK 9+ 默认），大堆场景停顿可控。灵犀写作 Spring Boot 3 同样 G1。调优时看 GC 日志判断是否需要调整 Region 或停顿目标。

**常见扣分项：** 只说"CMS 最好"（过时）；说不出 G1 的 Region 概念。

---

## 技能线二：Spring / SpringBoot / MyBatis

### Q8：Spring IOC 是什么？解决了什么问题？
**参考回答：** IOC（控制反转）：对象创建和依赖关系从"代码里 new"反转给容器管理，通过 DI（依赖注入）在运行时把依赖塞进对象。解决的核心问题是**解耦**——类之间依赖接口而非具体实现，且易测试（可 Mock 注入）。注入方式：构造器注入（推荐，保证不可变和必填）、Setter 注入、字段注入。

**关键点解析：**
- **面试官意图**：Spring 灵魂题。技术经理期待"IOC 解决什么问题 + 和 DI 的关系 + 注入方式优劣"。
- **怎么答**：先答"控制反转 + 解耦 + 易测试"，再区分 IOC（思想）和 DI（实现），最后讲注入方式推荐构造器。
- **深挖方向**：追问"为什么推荐构造器注入"，答「保证不可变（final）、必填校验（编译期暴露）、不依赖 Spring 也能 new（易测试）、避免字段注入的循环依赖隐患」。

**结合项目：** 养老系统里 AI Client、Service、Repository 都通过构造器注入，换实现（如换千帆模型适配层）只改配置不改业务代码。灵犀写作的策略模式多源配图，ImageProvider 各实现通过 Spring 注入，调用方只依赖接口——这就是 IOC 解耦的典型应用。

**常见扣分项：** 只说"容器管理对象"；说不出解耦和易测试；不知道注入方式优劣。

### Q9：Spring AOP 是什么？JDK 代理和 CGLIB 怎么选？
**参考回答：** AOP（面向切面）：把日志、事务、权限等横切关注点从业务代码抽离，运行时通过动态代理生成增强对象。代理方式：① JDK 动态代理——目标类实现了接口时用，基于 InvocationHandler；② CGLIB——没有接口时用，通过 ASM 生成子类。Spring Boot 2.x 起默认 proxyTargetClass=true，统一走 CGLIB。

**关键点解析：**
- **面试官意图**：考察 AOP 底层。技术经理期待"代理方式 + 默认策略 + 两种代理区别"。
- **怎么答**：先答 AOP 解决什么（横切关注点），再讲两种代理（JDK 基于接口 / CGLIB 生成子类），重点补"Boot 2.x 默认 CGLIB"。
- **深挖方向**：追问"CGLIB 的限制"，答「final 方法和类无法代理（不能覆写），非 public 方法也有限制」；追问"AOP 通知类型"，答「Before/After/AfterReturning/AfterThrowing/Around」。

**结合项目：** 养老系统用 AOP 做接口耗时埋点和统一异常处理；灵犀写作用 AOP 统一记录模型调用参数、返回结果和耗时，便于排查 AI 调用问题。实习中鉴权用拦截器（拿 Handler 更合适），AOP 适合方法级横切（日志/事务/限流）。

**常见扣分项：** 还在答"默认 JDK 代理"（不懂 Boot 2.x 变更）；说不出代理实现原理。

### Q10：Spring Boot 自动配置原理？
**参考回答：** 入口 @SpringBootApplication 组合了 @EnableAutoConfiguration，它通过 AutoConfiguration.imports（Spring Boot 2.7+ 的文件）加载所有候选自动配置类，每个候选类用 @Conditional 系列（@ConditionalOnClass 类是否存在、@ConditionalOnMissingBean Bean 是否缺失、@ConditionalOnProperty 属性是否配置）判断是否生效。所以"约定优于配置 + 按需条件装配"。

**关键点解析：**
- **面试官意图**：Spring Boot 灵魂题。技术经理期待"入口注解 → imports 加载 → @Conditional 装配"完整链路。
- **怎么答**：三段：入口注解 → imports 文件加载候选 → @Conditional 条件判断。能说出 imports 文件路径说明真看过。
- **深挖方向**：追问"怎么自定义 starter"，答「写 AutoConfiguration + imports 注册 + @ConditionalOnMissingBean 允许覆盖」。

**结合项目：** 灵犀写作 Spring Boot 3 + Spring AI Alibaba，Spring AI 的自动配置就是通过 imports 加载 ChatClient 等组件，我用 @ConditionalOnMissingBean 覆盖默认实现注入自定义的模型适配层。

**常见扣分项：** 只说"约定大于配置"；说不清 @Conditional 机制。

### Q11：@Transactional 事务在什么情况下会失效？
**参考回答：** 五类失效场景：① 方法不是 public（代理无法拦截）；② 同类内部自调用（this 调用绕过代理对象）；③ 异常被 catch 吞掉没抛出；④ 抛 checked 异常但没配 rollbackFor（默认只回滚 RuntimeException/Error）；⑤ 代理未生效或没配事务管理器。

**关键点解析：**
- **面试官意图**：Spring 高频"坑题"。技术经理期待每类都能举例 + 给解决。
- **怎么答**：五类全，重点讲自调用（最高频）和 catch 吞异常。自调用解决：注入自身代理 / 拆 Bean / TransactionTemplate。
- **深挖方向**：追问"自调用怎么解决最优雅"，答「拆到另一个 Bean 让调用走代理，或用 TransactionTemplate 编程式事务——我推荐拆 Bean」。

**结合项目：** 养老系统下单/库存扣减用 @Transactional，特别注意：① 方法 public；② 异常要抛出不能 catch；③ 同类内 A 方法调 B 方法（B 有 @Transactional）会失效，拆到不同 Service 解决。实习中订单链路的事务边界设计也遵循这些规则。

**常见扣分项：** 只记一两类；说不出自调用的解决方式。

### Q12：MyBatis 工作原理？Mapper 接口为什么不用写实现？
**参考回答：** 配置解析成 Configuration（含 MappedStatement）→ SqlSessionFactory → SqlSession → Executor 执行 → MappedStatement 组装 SQL → JDBC → 结果映射。Mapper 接口不写实现是因为**动态代理**：MyBatis 用 MapperProxyFactory 生成代理对象，调用接口方法时按"全限定名.方法名"找到对应 MappedStatement 执行 SQL。

**关键点解析：**
- **面试官意图**：MyBatis 必考。技术经理期待"核心链路 + MapperProxy 动态代理"。
- **怎么答**：先链路（Configuration→Factory→Session→Executor→Statement→JDBC），重点讲 MapperProxy："接口方法签名定位 SQL"是精髓。
- **深挖方向**：追问"一级二级缓存挂哪"，答「一级在 BaseExecutor 的 localCache（SqlSession 级），二级在 CachingExecutor（Mapper 级）」。

**结合项目：** 养老系统用 MyBatis-Plus，本质还是 MyBatis 机制——通用 CRUD 和条件构造器都是基于 Interceptor 在 Executor 层扩展。灵犀写作用 MyBatis Flex，原理相似。理解了 MapperProxy 才能解释为什么 MP 的 BaseMapper 不用写实现也能用。

**常见扣分项：** 只背"SqlSession 执行 SQL"；说不清 MapperProxy。

---

## 技能线三：MySQL

### Q13：MySQL 事务的四大特性？隔离级别有哪些？
**参考回答：** ACID：原子性（要么全做要么全不做）、一致性（从一个一致状态到另一个）、隔离性（并发事务互不干扰）、持久性（提交后永久）。四种隔离级别：读未提交（脏读）、读已提交（不可重复读）、可重复读（InnoDB 默认，解决不可重复读）、串行化（解决幻读，但无并发）。

**关键点解析：**
- **面试官意图**：事务基础必考。技术经理期待"ACID 四性 + 四级隔离 + 默认 RR"。
- **怎么答**：ACID 各一句，隔离级别按"从低到高 + 各自解决的读现象"讲，明确 InnoDB 默认 RR。
- **深挖方向**：追问"InnoDB 怎么在 RR 下防幻读"，答「快照读用 MVCC 防幻读，当前读（for update）用 Next-Key Lock 防幻读」。

**结合项目：** 养老系统下单/库存扣减用事务保证原子性（@Transactional），隔离级别用默认 RR。实习中订单链路的事务边界设计要保证"扣库存 + 创建订单"要么都成功要么都回滚。

**常见扣分项：** ACID 记不全；把默认级别说成读已提交。

### Q14：MySQL 索引底层为什么用 B+ 树？
**参考回答：** B+ 树两个核心优势：① 非叶子节点只存 key 和指针，不存数据——同样页大小装更多 key，树更矮，磁盘 IO 更少；② 叶子节点存数据且用双向链表串联——范围查询和排序直接在叶子链顺序扫描。对比 B 树（每个节点存数据，树更高、叶子无链表），B+ 树更适合磁盘存储和范围查询。

**关键点解析：**
- **面试官意图**：索引原理必考。技术经理期待"两个核心原因：IO 次数 + 范围查询"。
- **怎么答**：两个论点：矮胖（少 IO）+ 叶子链表（范围快）。能算出"3 层 B+ 树存千万级数据"是量化加分。
- **深挖方向**：追问"聚簇索引和非聚簇索引"，答「聚簇（主键索引）叶子存整行，数据物理有序；非聚簇叶子存主键，查询要回表」。

**结合项目：** 养老系统的设备表、老人表都按查询字段建索引，EXPLAIN 验证走索引。实习中商品搜索虽然主要用 ES，但 MySQL 侧的商品/订单查询也靠 B+ 树索引保证性能，慢 SQL 用 EXPLAIN 分析 key 和 type。

**常见扣分项：** 只说"B+ 树快"；说不出"非叶子只存 key"和叶子链表。

### Q15：什么情况下索引会失效？
**参考回答：** 主要场景：① 违反最左前缀（复合索引跳过最左列）；② 对索引列用函数或计算（LENGTH(name)>5、id+1=10）；③ LIKE '%xxx' 前缀通配；④ 隐式类型转换（字符串列和数字比较）；⑤ OR 连接非索引列；⑥ 数据量小或选择性低，优化器直接全表扫。排查：EXPLAIN 看 key 和 type。

**关键点解析：**
- **面试官意图**：考察索引实战。技术经理期待"失效场景 + EXPLAIN 排查"。
- **怎么答**：5-6 个场景各配例子，重点讲隐式类型转换和函数（最常见坑）。结尾落到"用 EXPLAIN 验证"。
- **深挖方向**：追问"最左前缀为什么失效"，答「复合索引 (a,b,c) 按 a→b→c 排序，跳 a 用不到整体」。

**结合项目：** 养老系统 SQL Review 时重点查这些失效场景——比如设备名 LIKE '%设备%' 要改成前缀匹配或走 ES。实习中做 SQL Review 和 CodeReview，遇到过字符串列传数字导致隐式转换不走索引的问题，用 EXPLAIN 定位后修正。

**常见扣分项：** 只背列表不举例子；不知道用 EXPLAIN 验证。

### Q16：执行计划 EXPLAIN 的关键字段怎么看？
**参考回答：** 核心：① type——访问类型，从好到差 system/const/eq_ref/ref/range/index/ALL（ALL 全表扫危险）；② key——实际使用的索引；③ rows——预估扫描行数；④ Extra——Using index（覆盖索引，好）、Using filesort（排序没走索引，要优化）、Using temporary（临时表，要优化）。重点看 type 是否 ref/range 以上、Extra 有没有 filesort/temporary。

**关键点解析：**
- **面试官意图**：考察 SQL 优化实操。技术经理会给慢 SQL 让你 explain 分析。
- **怎么答**：按"type 排序 + key + rows + Extra 告警"讲，把 filesort/temporary 解释成"该建索引的信号"。
- **深挖方向**：追问"const 和 ref 区别"，答「const 主键/唯一索引等值命中一行；ref 普通索引等值匹配多行」。

**结合项目：** 养老系统慢 SQL 优化流程：先 EXPLAIN 看 type 和 Extra → 发现 Using filesort → 给排序字段加索引 → 复测。实习中搜索接口的 MySQL 查询也走这个排查流程。

**常见扣分项：** 只会看 rows；不认识 filesort/temporary 的代价。

---

## 技能线四：Redis

### Q17：Redis 五种基本数据类型及使用场景？
**参考回答：** ① String——缓存值、计数器、分布式锁、验证码；② Hash——对象字段（用户信息、商品属性）；③ List——简单队列、最新列表、时间线；④ Set——去重、共同好友（交并差集）、抽奖；⑤ ZSet——排行榜、延迟队列、限流窗口。选型核心：让 Redis 操作直接满足需求，而不是取出数据在应用层算。

**关键点解析：**
- **面试官意图**：Redis 基础必考。技术经理期待"类型 ↔ 场景"映射 + 操作式选型思维。
- **怎么答**：五类型各 2 个场景，重点展示"操作即结果"：排行榜用 ZREVRANGEBYSCORE 而非取出排序。
- **深挖方向**：追问"ZSet 底层"，答「跳表（skiplist）+ 哈希表，跳表按分数排序，哈希表存成员-分数映射」。

**结合项目：** 养老系统用 String 缓存健康建议、Hash 存老人信息、ZSet 做设备告警优先级排行。灵犀写作用 String 缓存热门模板。实习中商品搜索用 Set 做搜索历史去重、ZSet 做热门搜索词排行。

**常见扣分项：** 类型对不上场景；说不出具体命令。

### Q18：缓存穿透、击穿、雪崩怎么区分和解决？
**参考回答：** ① 穿透——查的数据根本不存在，缓存永不命中，请求直击 DB。解决：布隆过滤器 + 空值缓存（短 TTL）。② 击穿——单个热点 key 过期瞬间大量请求打 DB。解决：互斥锁重建（Redisson）+ 逻辑过期（后台异步刷新）。③ 雪崩——大量 key 同时过期或 Redis 整体不可用。解决：过期时间加随机抖动、多级缓存、限流熔断降级。

**关键点解析：**
- **面试官意图**：Redis 三大经典问题。技术经理期待"成因区分 + 各给方案 + 项目实践"。
- **怎么答**：三者按"规模不同"区分（一个不存在的 key / 一个热点 key / 一批 key + 整体挂），各给 2 种方案。能说清"互斥锁 vs 逻辑过期权衡"是深度。
- **深挖方向**：追问"布隆过滤器误判怎么办"，答「会误拦合法请求，调低误判率参数 + 旁路列表补偿」。

**结合项目：** 养老系统健康建议是热点数据，用"短 TTL + 后台刷新 + 随机抖动"组合防击穿和雪崩。实习中商品搜索对不存在的商品 ID 用空值缓存防穿透。灵犀写作任务状态缓存设置随机 TTL 防雪崩。

**常见扣分项：** 三个问题混淆；只会一种解法。

---

## 技能线五：网络协议 / 操作系统 / Linux

### Q19：TCP 三次握手为什么是三次？两次不行吗？
**参考回答：** 三次握手目的：① 同步双方序列号；② 防止历史失效连接请求被误建立。两次不行：如果客户端旧 SYN 延迟到达到服务端，服务端两次握手就建连接并分配资源，但客户端早关了，服务端资源被浪费。三次握手让客户端第三次 ACK 时可以拒绝（发 RST），避免无效连接占资源。

**关键点解析：**
- **面试官意图**：网络必考。技术经理期待"为什么三次 + 两次的问题"。
- **怎么答**：核心讲"两次无法防止历史连接 + 无法可靠同步序列号"。历史 SYN 的例子是标准答法。
- **深挖方向**：追问"四次挥手为什么四次"，答「TCP 全双工，每端各自关闭（FIN-ACK 两次各一遍）」；追问"TIME_WAIT 为什么 2MSL"，答「确保最后一个 ACK 到达 + 让旧报文消亡」。

**结合项目：** 养老系统家属端日均 2000+ 次访问，理解三次握手有助于排查"连接建立慢"问题（TIME_WAIT 堆积、端口耗尽）。灵犀写作 SSE 长连接也基于 TCP，理解握手有助于分析连接建立和保活。

**常见扣分项：** 只背"三次握手流程"说不清为什么；"两次也行"（错误）。

### Q20：HTTP 和 HTTPS 的区别？HTTPS 握手过程？
**参考回答：** HTTP 明文传输，端口 80；HTTPS = HTTP + TLS/SSL，加密传输，端口 443。HTTPS 握手：① 客户端发 ClientHello（支持的 TLS 版本、加密套件、随机数）；② 服务端回 ServerHello（选定套件、随机数、证书）；③ 客户端验证证书，生成预主密钥用服务端公钥加密发回；④ 双方算出会话密钥，后续对称加密通信。

**关键点解析：**
- **面试官意图**：考察安全通信认知。技术经理期待"区别 + 握手大致流程 + 非对称/对称切换"。
- **怎么答**：先区别（明文/加密、端口），再握手四步，重点点出"非对称交换密钥 + 对称加密数据"的设计。
- **深挖方向**：追问"为什么不全用非对称"，答「非对称计算开销大，只用它交换密钥，后续用对称提升性能」。

**结合项目：** 养老系统微信小程序走 HTTPS（微信强制），后端配置 TLS 证书。灵犀写作前端也走 HTTPS。理解 HTTPS 有助于排查"证书过期/混合内容"等问题。

**常见扣分项：** 只说"HTTPS 安全"；说不清握手和非对称/对称切换。

### Q21：进程和线程的区别？协程是什么？
**参考回答：** 进程是资源分配的最小单位，线程是 CPU 调度的最小单位。区别：① 资源——进程独立内存空间，线程共享进程内存（堆、全局变量）；② 切换开销——进程切换要切页表和上下文，开销大；线程共享地址空间，切换快；③ 通信——进程间靠 IPC（管道/消息队列/共享内存/Socket），线程直接读写共享内存（要同步）。协程是用户态轻量级线程，调度由用户控制，切换不进内核，开销极小。

**关键点解析：**
- **面试官意图**：OS 基础必考。技术经理期待"进程/线程区别 + 上下文切换 + 协程认知"。
- **怎么答**：按"资源/切换/通信"三维度对比进程线程，协程点出"用户态调度、不进内核"。
- **深挖方向**：追问"线程上下文切换保存什么"，答「寄存器、程序计数器、栈指针；线程共享地址空间所以不切页表」；追问"Java 的虚拟线程是什么"，答「JDK 21 的虚拟线程就是协程思路，由 JVM 调度，用少量平台线程跑海量虚拟线程」。

**结合项目：** 灵犀写作异步任务体系用线程（ThreadPoolTaskExecutor），理解线程切换开销才知道为什么不能无限建线程。养老系统 IoT 同步用线程池而非每设备一个线程，正是控制切换开销。理解协程有助于理解 Go/Kotlin 的并发模型差异。

**常见扣分项：** 只说"进程大线程小"；说不出上下文切换；不知道协程。

### Q22：零拷贝是什么？有哪几种方式？（重点题）
**参考回答：** 零拷贝不是"没有拷贝"，而是减少 CPU 参与的拷贝和上下文切换。传统 IO 读文件再发网络：4 次上下文切换 + 4 次拷贝（2 次 DMA + 2 次 CPU）。零拷贝方式：① mmap——文件映射到用户空间内存，减少 1 次 CPU 拷贝（2 次 DMA + 1 次 CPU + 4 次切换）；② sendfile——数据全程在内核态从磁盘到 Socket，2 次 DMA + 0 次 CPU + 2 次切换（Linux 2.4+ 用 SG-DMA）；③ splice——管道传输，类似 sendfile 但用管道连接。

**关键点解析：**
- **面试官意图**：OS 和 IO 优化深度题。技术经理期待"传统 IO 痛点 + 三种方式 + 拷贝/切换次数对比"。
- **怎么答**：先讲传统 IO 的"4 切换 + 4 拷贝"，再讲三种零拷贝各自减少什么。重点对比"mmap 还要 CPU 拷贝一次 / sendfile 全程内核态不进用户空间"。
- **深挖方向**：追问"Java 里怎么用零拷贝"，答「NIO 的 FileChannel.transferTo() 对应 sendfile，MappedByteBuffer 对应 mmap」；追问"Netty 的零拷贝"，答「Netty 的零拷贝是另一层概念：CompositeByteBuf 合并 Buffer 不拷贝、FileRegion 用 sendfile」。

**结合项目：** 养老系统和灵犀写作涉及文件上传下载（如 PDF 健康报告、生成的文章导出），理解零拷贝知道为什么大文件传输要用 NIO 的 transferTo 而非传统 IO。实习中 RAG 知识库处理 PDF 文档入库，大文件读取也涉及 IO 优化。Kafka 的高吞吐也依赖零拷贝（sendfile）+ 顺序写磁盘，理解原理能解释 MQ 选型。

**常见扣分项：** "零拷贝就是没有拷贝"（错误）；说不清几种方式和次数对比。

> 零拷贝详解见文末「专题二：零拷贝详解」。

### Q23：常用的 Linux 性能排查指令有哪些？
**参考回答：** 系统化排查：① CPU——top（看 load average 和 CPU 使用率）、mpstat（多核）、vmstat（上下文切换、运行队列）；② 内存——free（总量/可用）、top（进程内存）、pmap（进程内存映射）；③ 磁盘 IO——iostat（读写速率、await）、iotop（哪个进程在 IO）；④ 网络——netstat/ss（连接状态、端口）、tcpdump（抓包）、iftop（流量）；⑤ 进程——ps（进程状态）、jps（Java 进程）、jstack（线程栈）、jmap（堆 dump）。

**关键点解析：**
- **面试官意图**：考察运维和排查能力。技术经理期待"按维度分类 + 会用 + 知道看什么"。
- **怎么答**：按"CPU/内存/IO/网络/进程"五维分类讲，每维 2-3 个命令 + 看什么指标。能说"top 看 load average 和 CPU、free 看 available 而非 free、iostat 看 await 和 %util"是实操级。
- **深挖方向**：追问"线上 CPU 飙高怎么排查"，答「top 找进程 → top -Hp 找线程 → jstack 看线程在干嘛 → 定位代码」；追问"内存涨不崩怎么排查"，答「jmap dump → MAT 分析大对象 → 定位泄漏」。

**结合项目：** 养老系统压测时用 top+jstack 排查 CPU 飙高（定位到某个线程死循环），用 free+ jmap 排查内存增长（缓存无上限）。灵犀写作异步任务堆积时用 jps+jstack 看线程状态。实习中用 netstat/ss 排查连接数（TIME_WAIT 堆积），用 iostat 看磁盘 IO（日志写太多）。

**常见扣分项：** 只会说 top 和 ps；不会按维度分类；不知道 jstack/jmap。

### Q24：TCP 的可靠传输怎么保证？
**参考回答：** TCP 可靠性靠：① 三次握手/四次挥手保证连接建立和释放可靠；② 序列号和确认号保证有序和不丢；③ 超时重传（RTO）和快重传（收到 3 个重复 ACK）保证丢包恢复；④ 流量控制（滑动窗口，接收方通告窗口大小）防止淹没接收方；⑤ 拥塞控制（慢开始、拥塞避免、快重传、快恢复）防止网络拥塞。

**关键点解析：**
- **面试官意图**：网络深度题。技术经理期待"多个机制 + 各自作用"。
- **怎么答**：五机制：握手挥手、序列号/确认、重传、流量控制、拥塞控制。每个一句话。
- **深挖方向**：追问"流量控制和拥塞控制区别"，答「流量控制是端到端（接收方能力），拥塞控制是网络感知（全局拥塞）」。

**结合项目：** 养老系统 IoT 设备通过 MQTT（基于 TCP）上报数据，理解 TCP 可靠性才知道为什么 MQTT 在弱网下要设计 QoS 级别和重传。灵犀写作 SSE 基于 TCP 长连接，理解滑动窗口有助于分析流式传输的背压。

**常见扣分项：** 只说"三次握手"；说不出流量/拥塞控制。

### Q25：操作系统的进程间通信（IPC）有哪些方式？
**参考回答：** ① 管道（pipe）——父子进程单向通信；② 命名管道（FIFO）——无亲缘关系进程也可用；③ 消息队列——结构化消息，独立于进程生命周期；④ 共享内存——最快，多进程映射同一块物理内存（需同步）；⑤ 信号量——同步互斥；⑥ 信号——异步通知（如 kill）；⑦ Socket——跨机器通信。

**关键点解析：**
- **面试官意图**：OS 基础。技术经理期待"至少说出 4-5 种 + 各自特点"。
- **怎么答**：按"同机/跨机"和"速度"讲：共享内存最快但要同步，Socket 最通用可跨机。
- **深挖方向**：追问"为什么共享内存最快"，答「直接读写内存，不经过内核拷贝（其他 IPC 要从用户态进内核态再出来）」。

**结合项目：** 实习中 Redis 本身就是基于 Socket 的进程间通信，RocketMQ 的 Broker 和 Producer/Consumer 也是 Socket。养老系统的微服务间调用（OpenFeign）本质也是 Socket 通信。理解 IPC 有助于理解中间件的通信模型。

**常见扣分项：** 只说 Socket；说不出共享内存为什么快。

---

## 专题一：JVM 内存块详解（每部分的作用）

> 对应简历"对 JVM 内存结构与垃圾回收机制有基本认识"。

### 程序计数器（Program Counter Register）
- **性质**：线程私有，唯一不会 OOM 的区域
- **作用**：记录当前线程执行的字节码行号，线程切换后能恢复到正确位置
- **细节**：执行 Java 方法记字节码指令地址，执行 native 方法为空（Undefined）

### 虚拟机栈（VM Stack）
- **性质**：线程私有，生命周期同线程
- **作用**：每个方法执行时创建一个**栈帧**，方法调用就是栈帧入栈/出栈
- **栈帧组成**：
  - 局部变量表——方法参数和方法内局部变量（基本类型直接存，引用存引用地址）
  - 操作数栈——执行字节码指令的工作区（如 iadd 弹两个数相加压回）
  - 动态链接——指向运行时常量池的方法引用（支持多态）
  - 返回地址——方法正常返回或异常返回后跳回调用者位置
- **异常**：栈深度超限 → StackOverflowError（递归过深）；无法分配 → OOM

### 本地方法栈（Native Method Stack）
- **性质**：线程私有
- **作用**：为 native 方法服务（如 `Unsafe` 类、JNI 调用），类似虚拟机栈但针对 native
- **异常**：同样 StackOverflowError / OOM

### 堆（Heap）
- **性质**：线程共享，GC 主战场，所有对象实例和数组在此分配
- **作用**：存储对象，是垃圾收集器管理的核心区域
- **分代**：
  - 新生代（Young Gen）：Eden + Survivor0 + Survivor1（比例 8:1:1）。对象先分配 Eden，Minor GC 后存活进 Survivor，年龄达阈值（默认 15）进老年代
  - 老年代（Old Gen）：长生命周期对象、大对象（直接分配）
- **异常**：堆 OOM（对象太多/内存泄漏）
- **结合项目**：养老系统的健康建议缓存（长生命周期）会进老年代；灵犀写作的临时对象（短生命周期）在新生代被 Minor GC 回收

### 方法区 / 元空间（Method Area / Metaspace）
- **性质**：线程共享
- **作用**：存储类元信息（类的结构、字段、方法）、常量池、静态变量、JIT 编译后的代码
- **JDK 版本变化**：
  - JDK 7 及以前：方法区 = 永久代（PermGen，JVM 内存，易 OOM）
  - JDK 8：永久代 → 元空间（Metaspace，**本地内存**），字符串常量池移到堆
  - 变化原因：永久代大小固定易 OOM、GC 复杂；元空间用本地内存，大小动态扩展
- **异常**：元空间 OOM（加载类太多，如动态代理/反射滥用）

### 运行时常量池（Runtime Constant Pool）
- **性质**：方法区的一部分
- **作用**：class 文件常量池加载到内存后的运行时表示，包含符号引用（运行时解析为直接引用）
- **JDK 8 后**：字符串常量池移到堆，避免永久代 OOM

### 直接内存（Direct Memory）
- **性质**：不是 JVM 运行时数据区，是操作系统本地内存
- **作用**：NIO 的 `ByteBuffer.allocateDirect()` 分配堆外内存，避免内核态↔用户态拷贝（零拷贝基础）
- **异常**：直接内存 OOM（堆外内存泄漏，jmap 看不到，要用 Native Memory Tracking）
- **结合项目**：灵犀写作如果用 NIO 优化文件传输会涉及直接内存；Kafka 的零拷贝也依赖直接内存

### 各区域 OOM 汇总表

| 区域 | 线程 | OOM 触发原因 |
|------|------|--------------|
| 程序计数器 | 私有 | 不会 OOM |
| 虚拟机栈 | 私有 | 栈深度超限 StackOverflowError；无法分配 OOM |
| 本地方法栈 | 私有 | 同上 |
| 堆 | 共享 | 对象太多/内存泄漏 |
| 元空间 | 共享 | 加载类太多（动态代理/反射） |
| 直接内存 | —— | NIO 堆外内存泄漏 |

---

## 专题二：零拷贝详解

> 对应简历"了解操作系统"深挖点，技术经理高频追问。

### 传统 IO 的痛点（基准对照）

以"读取磁盘文件发到网络"为例（`read + write`）：

```
应用层:   buffer(用户空间)
              ↑↓ 2次CPU拷贝
内核空间: kernel buffer ↔ socket buffer
              ↑              ↑
           1次DMA        2次DMA
磁盘 ───────→  (读)          (写) ───────→ 网卡
```

- **4 次上下文切换**：read(用户→内核) + read 返回(内核→用户) + write(用户→内核) + write 返回(内核→用户)
- **4 次数据拷贝**：2 次 DMA（磁盘→内核、内核→网卡）+ 2 次 CPU（内核→用户、用户→内核）
- **痛点**：数据从内核到用户再回内核，CPU 白忙两次

### 方式一：mmap（内存映射）

```
应用层:   mmap 映射区(与内核共享)
              ↑ 只1次CPU拷贝(内核→socket)
内核空间: kernel buffer(mmap) → socket buffer
              ↑              ↑
           1次DMA        2次DMA
磁盘 ───────→              ───────→ 网卡
```

- **原理**：`mmap()` 把文件映射到用户空间内存，用户和内核**共享同一块物理内存**，省掉"内核→用户"那次 CPU 拷贝
- **次数**：2 次 DMA + 1 次 CPU + 4 次上下文切换
- **Java 对应**：`MappedByteBuffer`（基于 mmap）
- **适用**：需要对文件内容随机读写（映射后可直接读写）
- **注意**：mmap 后仍需 write 发网络，所以还有 1 次 CPU 拷贝（内核→socket buffer）和 4 次切换

### 方式二：sendfile（Linux 2.1+，2.4+ 优化）

```
应用层:   (不接触数据)
              ↑ 只2次上下文切换
内核空间: kernel buffer → socket buffer → 网卡
              ↑              ↑
           1次DMA        2次DMA
磁盘 ───────→              ───────→
```

- **原理**：`sendfile()` 系统调用让数据**全程在内核态**从磁盘到 Socket，不进用户空间
- **次数**：
  - Linux 2.1：2 次 DMA + 1 次 CPU（内核 buffer→socket buffer）+ 2 次切换
  - Linux 2.4+（SG-DMA）：2 次 DMA + **0 次 CPU** + 2 次切换（真正的零拷贝，网卡用 SG-DMA 直接从 kernel buffer 取数据）
- **Java 对应**：`FileChannel.transferTo()`（基于 sendfile）
- **适用**：只需把文件发到网络，不修改内容（如静态文件服务、Kafka 发送消息）

### 方式三：splice（Linux 2.6.17+）

- **原理**：用**管道**连接两个文件描述符，数据在内核态通过管道传输，不进用户空间，不经过 CPU 拷贝
- **次数**：2 次 DMA + 0 次 CPU + 2 次切换
- **与 sendfile 区别**：sendfile 是"文件→socket"，splice 更通用（任意两个 fd 之间，如文件→管道、管道→文件）
- **适用**：需要在两个 fd 间搬数据但不需要用户态处理

### 四种方式对比表

| 方式 | CPU 拷贝 | DMA 拷贝 | 上下文切换 | Java 对应 | 适用场景 |
|------|---------|---------|-----------|-----------|---------|
| 传统 IO | 2 | 2 | 4 | InputStream/OutputStream | 通用 |
| mmap | 1 | 2 | 4 | MappedByteBuffer | 需随机读写文件 |
| sendfile(2.4+) | **0** | 2 | **2** | FileChannel.transferTo() | 文件→网络（Kafka） |
| splice | 0 | 2 | 2 | 无直接对应 | fd 间管道传输 |

### Java 中的零拷贝

1. **MappedByteBuffer**（mmap）：把文件映射到内存，直接读写，适合大文件随机访问
2. **FileChannel.transferTo()**（sendfile）：把当前通道数据直接传到目标通道（如 SocketChannel），适合文件→网络
3. **堆外内存（DirectByteBuffer）**：减少一次"堆内→堆外"拷贝，配合 NIO 使用
4. **Netty 的零拷贝**（另一层概念）：
   - CompositeByteBuf——合并多个 Buffer 不拷贝
   - FileRegion——封装 FileChannel.transferTo() 用 sendfile
   - Unpooled.wrappedBuffer()——包装不拷贝

### 结合项目

- **Kafka 高吞吐**：实习中用 RocketMQ，但理解 Kafka 的零拷贝（sendfile + 顺序写）能解释"为什么 Kafka 吞吐高"，用于 MQ 选型对比
- **养老系统文件传输**：健康报告 PDF 上传下载，大文件用 `FileChannel.transferTo()` 比传统 IO 快
- **灵犀写作**：如果生成的大文章要导出或传输，NIO 零拷贝能优化
- **RAG 知识库**：实习中 PDF 入库读取大文件，理解 mmap 知道为什么大文件读取用 MappedByteBuffer 更高效

---

## 附录：答题策略

### 五条技能线的优先级

| 技能线 | 简历表述 | 面试权重 | 答题策略 |
|--------|---------|---------|---------|
| Java/JVM | 熟悉线程池/JVM | 高 | 线程池必背，JVM 区域 + GC 框架，零拷贝是加分深挖 |
| Spring | 熟悉 Spring/Boot/MyBatis | 高 | IOC/AOP/自动配置/事务失效必背，MyBatis 原理 |
| MySQL | 熟悉事务/索引/执行计划 | 高 | ACID/隔离级别/B+树/失效/EXPLAIN 必背 |
| Redis | 熟悉数据类型/缓存问题 | 高 | 五类型 + 穿透/击穿/雪崩必背，结合项目 |
| 网络/OS | 熟悉网络协议/OS/Linux | 中 | 三次握手/进程线程/零拷贝/Linux 指令 |

### 记忆口诀

- **JVM 区域**："私有三件套（PC/栈/本地栈），共享两件套（堆/方法区），加个直接内存"
- **零拷贝次数**："传统 4-4，mmap 1-4，sendfile 0-2"（CPU 拷贝-切换次数）
- **缓存三问题**："穿透不存在（布隆+空值），击穿单热点（互斥锁），雪崩大批量（随机 TTL）"
- **事务失效五类**："非 public、自调用、catch 吞、checked 不回滚、代理没生效"

---

## 参考来源

- JVM 内存区域（JavaGuide）：https://javaguide.cn/java/jvm/memory-area.html
- JVM 面试题（小林coding）：https://www.xiaolincoding.com/interview/jvm.html
- 零拷贝原理（pdai.tech）：https://pdai.tech/md/java/io/java-io-nio-zerocopy.html
- 零拷贝三剑客对比：https://zhuanlan.zhihu.com/p/2040964730236442584
- Linux 性能排查（小林coding）：https://www.xiaolincoding.com/interview/linux.html
- Java 并发工具类（JavaGuide）：https://javaguide.cn/java/concurrent/java-concurrent-questions-03.html
- 进程与线程（JavaGuide OS）：https://javaguide.cn/cs-basics/operating-system/operating-system-basic-questions-01.html
- TCP 三次握手（小林coding）：https://www.xiaolincoding.com/network/3_tcp/tcp_interview.html
- Redis 数据结构（小林coding）：https://www.xiaolincoding.com/interview/redis.html
- MySQL 索引原理：https://javaguide.cn/database/mysql/mysql-questions-01.html
