# 纯 AI 简历 · 面试 100 问 QA 详解版

> 基于 `D:\简历\纯AI\纯AI_简历_STAR优化版.docx`，从技术经理 / 技术面试官视角设计。
> 候选人画像：**应届生（2027 届），2026 年 9 月可实习**，方向 **AI 应用开发 + Java 后端双栈**，核心经历两段实训项目——**农业知识库问答智能体（个人项目）** 与 **电商自然语言 AI 智能检索系统（4 人团队，AI 模块负责人）**。
> 每题包含：**问题 → 参考回答 → 关键点解析（面试官意图 / 怎么答 / 深挖方向）→ 常见扣分项**。
> 阅读约定：参考回答为可直接口述的"骨架"，关键点解析用于理解"为什么这么答、会被怎么追问"，扣分项用于自我对照排雷。

---

## 模块一：自我介绍 & 职业动机（Q1–Q10）

### Q1：请用 1 分钟做一个自我介绍。

**参考回答：** 我是江西农业大学软件工程专业的应届生，2027 届，今年 9 月可以到岗实习。我的方向是 **AI 应用开发 + Java 后端**。核心经历两段：第一段是个人实训项目「农业知识库问答智能体」，我用 LangChain + LCEL 搭了私有知识库优先、通用知识兜底的双工具 Agent，解决了通用大模型编造农技方案的问题，支持多轮会话和 SSE 流式输出；第二段是 4 人实训项目「电商自然语言 AI 智能检索系统」，我作为 AI 模块负责人，独立完成商品 RAG 全链路——从 SKU 幂等入库、RedisStack 向量召回 + MMR 去重、三层幻觉防控，到 OpenFeign 跨语言调用和 RocketMQ + Elastic-Job 数据同步。我的特点是能把模型能力收敛成可测试、可维护的业务接口。

**关键点解析：**
- **面试官意图**：第一题在 1 分钟内判断三件事——你有没有技术主线、表达是否结构化、和岗位是否匹配。技术经理不期待你背稿，期待听到"你是谁 + 做过什么 + 你的特点"三段式。
- **怎么答**：按"背景一句话 → 两段经历各带一个差异化亮点 → 双栈定位 → 个人特点"组织。农业项目突出"双工具 + 幻觉拦截"，电商项目突出"RAG 全链路 + 跨服务协同"，两个项目不重复堆名词，而是各给一个面试官能顺着追问的钩子。
- **深挖方向**：面试官大概率顺着你提到的"双工具""三层幻觉防控""OpenFeign 跨语言"追问，所以自我介绍里出现的每个名词都必须能在 2–3 分钟内展开，否则等于给自己埋雷。

**扣分项：** 超时 >2min；背诵感强；只堆框架名（LangChain、Redis、SpringBoot）没有业务问题和解决方案；没有个人定位句。

### Q2：你为什么选择 AI 应用开发方向？

**参考回答：** 我觉得 AI 落地不只是调 API，真正的难点在检索质量、输出稳定性、延迟控制和异常兜底——这些恰好和我的 Java 工程背景高度契合。我不想做"只写 prompt 的 API 使用者"，而是想做"能把模型能力稳定接到业务链路里的工程师"。电商项目里我就验证了这条路径：模型会编造价格规格，我用"检索低分过滤 + System Prompt 约束 + 商品实体校验"三层把幻觉压下去，这是纯调 API 做不到的。

**关键点解析：**
- **面试官意图**：验证你的动机是否真实、是否经过思考，而不是"AI 火所以跟风"。技术经理见过太多嘴上说热爱 AI、实际只调过 openai.chat.completions 的候选人。
- **怎么答**：用"AI 落地的工程难点正好匹配我的 Java 工程能力"这个逻辑链，再摆出"现实证据"（三层幻觉防控）。关键是让面试官听到你理解"模型能力 ≠ 产品能力"。
- **深挖方向**：可能追问"你说的工程难点具体指什么""调 API 和做 AI 应用区别在哪"，准备好"检索召回、结构化输出、SSE 断流恢复"三个具体例子即可。

**扣分项：** 回答"AI 热门 / 薪资高所以选"——完全没有个人判断；"觉得 AI 有未来"——空泛。

### Q3：你觉得自己最大的优势和不足分别是什么？

**参考回答：** 优势是"全链路意识 + 双栈协同"——我不只写 AI 推理那段，还关注数据入库幂等、跨服务调用契约、异常兜底和可观测性，能在 Java 和 Python 之间做接口联调。不足是：作为应届生，工程深度还有限，比如 RocketMQ 我用在项目里做了异步同步，但它的存储模型、顺序消息、事务消息底层原理还停留在使用层，目前在补源码级知识。

**关键点解析：**
- **面试官意图**：优势看"是否与岗位强相关且可验证"，不足看"是否真实、是否在补"。这是技术经理快速筛选"自我认知清晰度"的题。
- **怎么答**：优势必须给实例（全链路意识 → 电商项目里做了幂等入库、跨服务联调、异常兜底）；不足必须是"真实的、可改进的、正在改进的"三要素齐全。承认 RocketMQ 停留使用层 → 正在补源码，体现行动力。
- **深挖方向**：他可能追问"RocketMQ 源码你学到哪了"——这是送分题，提前准备"NameServer/Broker/CommitLog/ConsumeQueue 四件套 + 消息存储流程"就能漂亮收尾。注意别把不足说成"我太追求完美"。

**扣分项：** "我没什么不足"；不足说成优点；不足与岗位核心能力冲突（"我不擅长沟通"——但岗位需要跨语言联调协作）。

### Q4：你的职业规划是什么？

**参考回答：** 短期（1–2 年）：在 AI 应用开发岗位上深耕，把实习期积累的 RAG、Agent 工程化做扎实，同时补齐中间件底层原理和分布式系统设计短板。中期（3–5 年）：成为能独立负责一个 AI 系统模块的技术骨干，具备从需求拆解到性能调优的全链路能力。长期：成为连接 AI 和业务的技术桥梁，能推动 AI 能力在企业里真正落地。

**关键点解析：**
- **面试官意图**：判断稳定性（你是否会干一年就走）、目标是否可落地、以及你是否把成长寄托在公司平台上。
- **怎么答**：按"短期补短板 → 中期独当一面 → 长期技术+业务桥梁"三段阶梯，重点是让面试官看到成长路径"绑定在这个岗位上"（AI 应用开发），而不是"有计划跳槽"。
- **深挖方向**：他可能问"具体怎么补分布式短板"，答"读 RocketMQ 存储源码 + 做 1–2 个分布式事务/缓存一致性实验 + 跟进 LangGraph、MCP 这类 AI 工程化方向"即可，体现规划的具体性。

**扣分项：** "想当架构师"（空泛）；"想创业"（不安定信号）；规划与岗位无关。

### Q5：你对加班怎么看？

**参考回答：** 我关注的是"产出"而非"工时"。项目攻坚期——比如电商项目联调上线前——主动加班是应该的，那是交付质量的一部分。但日常我会用工程手段减少返工：测试记录和问题复盘文档能降低重复踩坑，接口契约先行能减少联调返工，本质上也是提升效率。

**关键点解析：**
- **面试官意图**：确认你对高强度工作的接受度，同时看你的工作价值观。技术经理不想听"我能无限加班"，而想听"你有责任感且有方法"。
- **怎么答**：两段式——先表态"攻坚期没问题"，再展示"我用效率思维减少加班"的工程手段（文档沉淀、契约先行、测试覆盖）。这样既显责任心又显专业性，而且和你简历里"工程复盘""接口契约"呼应。
- **深挖方向**：可能追问"你实际加过班吗"，可以答电商项目双服务联调阶段的加班，落点放在"保证交付质量"。

**扣分项：** 只答"接受"无实质内容；"不接受加班"过于生硬；"我热爱加班"（假）。

### Q6：期望薪资是多少？

**参考回答：** 我对行业应届薪资有一定了解，现阶段我更看重岗位成长空间和技术方向匹配度——能在一个真正做 AI 工程落地的团队里积累经验，对我来说比短期薪资更重要。薪资方面我持开放态度，愿意参考公司体系和岗位级别来协商；如果一定要个区间，我希望按面试定级浮动。

**关键点解析：**
- **面试官意图**：初面阶段技术经理问薪资更多是走流程，真正定薪在 HR 环节。他期待你给一个"合理的、可协商的区间"，而不是立刻咬死数字。
- **怎么答**：不急着报数字，先强调"成长空间和技术方向匹配"是首要考虑，再表达"参考公司体系协商"的开放性。如果对方非要数字，给一个窄区间并说明按面试定级浮动。
- **深挖方向**：HR 环节会再谈，保持口径一致即可；不要在此环节陷入讨价还价。

**扣分项：** 报价过高（显得不近市场）或过低（显得没信心）；"都行"（显得没底线）。

### Q7：你还投了哪些公司/岗位？

**参考回答：** 主要投 AI 应用开发和 Java 后端方向，集中在能做 AI 工程落地的团队。这两个方向本质是同一类能力——都是把模型能力或后端能力接到业务里。相比"面广"，我更倾向"面深"——选一个技术方向对口、团队氛围好的地方长期发展。

**关键点解析：**
- **面试官意图**：判断你的求职定位是否清晰、是否是海投党。海投容易被认为"拿到 offer 就骑驴找马"。
- **怎么答**：强调"方向聚焦"（AI 应用开发 + Java 后端双主线，本质同一类能力），以及"选团队标准"（技术对口、能长期发展）。不要报具体公司名，也不要答"就投了你们一家"（不真实）。
- **深挖方向**：可能追问"同时拿到多个 offer 怎么选"，答"看技术栈匹配度、团队成长性、业务前景"即显理性。

**扣分项：** 报出一堆完全不相关的岗位；贬低其他公司抬高当前公司（油腻）。

### Q8：你有什么想问我们的？

**参考回答：** 我准备了三个问题：① 团队目前 AI 项目的技术栈和架构是怎样的？比如检索是纯向量还是混合检索，Agent 编排用的是 LangChain 还是 LangGraph？ ② 对这个岗位的新人，前 3 个月的核心期望是什么？ ③ 团队的技术分享和成长机制是怎样的？

**关键点解析：**
- **面试官意图**：提问环节是加分黄金窗口——它体现你的求职诚意、思考深度和对岗位的真实兴趣。技术经理会通过你的问题反推"你关心什么"。
- **怎么答**：准备 2–3 个"让面试官愿意展开说"的问题：问团队技术栈（显兴趣 + 显你懂行，能问到"混合检索""LangGraph"这种细节）、问新人期望（显执行力）、问成长机制（显长期主义）。
- **深挖方向**：如果面试官回答很简短，可用"刚才您提到 XX，能再展开讲讲吗"做二次追问，把互动变深。

**扣分项：** "没有问题"（兴趣缺失）；一上来就问薪资福利年假（初面丢好感）。

### Q9：如果给你一个完全陌生的技术栈，你怎么上手？

**参考回答：** 三步走：① 快速通读官方文档建立全景认知；② 跑通一个最小 Demo 验证理解；③ 带着业务场景深入关键模块。同时用团队已有代码做参照，避免从零踩坑。我学 LangChain 就是这样：先通读 LCEL 文档，再用它写了个最简的双工具 Agent，最后在农业项目里落地，过程中把检索来源、会话状态、异常链路的边界都摸清了。

**关键点解析：**
- **面试官意图**：考察"学习能力"和"可迁移能力"——技术天天变，能否快速上手比"用过什么"更重要。技术经理愿意招"会学习的人"。
- **怎么答**：给清晰的"文档 → Demo → 业务场景"三步方法论，并强调"向老代码、老同事、已有规范学习"——这三点展示的不是技术而是"融入团队的方式"，很加分。最后用 LangChain 的真实经历佐证。
- **深挖方向**：可能追问"遇到文档没写的坑怎么办"，答"看源码 / 提 issue / 翻社区踩坑文章 / 主动问同事"，体现多路径解决能力。

**扣分项：** "百度一下就行"（太随意）；只说学不会就换（没有韧性）。

### Q10：你平时怎么学习新技术？

**参考回答：** 分层学习 + 输出闭环：① 先看官方文档建立全景认知；② 找一个真实场景做 Demo 验证；③ 写笔记或做团队分享沉淀；④ 在项目里应用并收集反馈。比如 RAG 幻觉优化，我先通读 LangChain 检索和 prompt 约束的文档，在电商项目里落地三层防控，最后做成了一场团队技术分享，输出标准化文档。我的 GitHub 上也有学习沉淀。

**关键点解析：**
- **面试官意图**：区分"被动学习者"（看视频、收藏夹吃灰）和"主动学习者"（有输入-实践-输出闭环）。这也是技术经理判断你成长速度的题。
- **怎么答**：突出"输出闭环"——写笔记、做技术分享、整理排查记录，这比"爱看视频"高一个档次。简历里"组织 3 场技术分享"就是闭环的证据。
- **深挖方向**：可以主动出示证据（"我的面试准备文档/学习笔记在 GitHub 上"），把学习能力变成可验证的事实。

**扣分项：** "刷 B 站教程"（被动）；只输入不输出（学了就忘）。

---

## 模块二：项目深挖 — 农业知识库问答智能体（Q11–Q30）

> 个人实训项目（2026.02–2026.05），技术栈：LangChain + LCEL + Agent + RAG + FastAPI + SSE + Pydantic + InMemorySaver。
> 核心设计：私有知识库优先、通用知识兜底的双工具调度；多轮会话记忆；SSE 流式输出；Pydantic 结构化交付。

### Q11：农业问答智能体的整体架构是什么样的？数据怎么流转？

**参考回答：** 整体是 FastAPI 后端 + 前端聊天页 + 私有知识库三层。数据流：用户提问 → Agent 调度 → 先走私有 PDF 检索 Tool，命中相关片段就用它做依据回答；没命中或低分再兜底走通用农业知识 Tool → 模型基于检索内容 + System Prompt 生成结构化回答 → Pydantic 校验字段 → 通过 FastAPI 的 astream 按 Token 分片经 SSE 推给前端。会话状态用 InMemorySaver 按 thread_id 隔离，多轮上下文持续传递。

**关键点解析：**
- **面试官意图**：第一道项目题，判断你对系统全貌的掌控度，而不是只会说"我用了 LangChain"。他想听数据怎么从输入流到输出、每个组件的职责边界。
- **怎么答**：按"入口 → 调度 → 检索 → 生成 → 校验 → 流式输出 → 状态保持"一条线讲，每个环节点出一个组件和一个职责。关键词是"边界"——私有 Tool 干什么、通用 Tool 干什么、Agent 在中间做什么决策，要分清。
- **深挖方向**：他会顺着任意环节往下问（Q12 双工具、Q15 System Prompt、Q17 InMemorySaver、Q20 astream），所以这条主线讲 40 秒，把时间留给深水区。

**扣分项：** 只报框架名不说数据流；把双工具说成"两个都调一下取更好的"（没懂路由逻辑）。

### Q12：为什么选"私有知识库优先 + 通用兜底"的双工具路由，而不是只用一个？

**参考回答：** 因为两套工具解决不同问题。私有 PDF 是农技手册，权威但覆盖面窄，能解决"私有资料无法读取"和"方案不脱离资料"的问题；通用大模型覆盖面广，但会编造具体病害的防治细节。所以我用 System Prompt 约束"知识库优先、无匹配再兜底"——优先用私有数据保证准确性，私有检索不到时才让通用知识补位，避免直接拒绝用户。如果只用私有，覆盖不够；如果只用通用，又回到编造问题。

**关键点解析：**
- **面试官意图**：考察"技术选型背后的业务理解"。他想看你是不是为了用 Agent 而用 Agent，还是真的因为单工具解决不了问题才上双工具。
- **怎么答**：核心是"单工具的缺陷 → 双工具的互补 → 路由规则"。强调这不是炫技，而是"准确性 vs 覆盖面"的权衡。
- **深挖方向**：可能追问"为什么不做成一个 RAG，把通用知识也灌进去"，答"通用知识量级太大、更新频繁、且和私有手册的可信度不同，混灌会污染检索结果可信度，分层调度更可控"。

**扣分项：** 说不出单工具的缺陷；"因为 Agent 就该有多个工具"（没有业务理由）。

### Q13：私有 PDF 检索 Tool 怎么实现的？从 PDF 到可检索的流程是什么？

**参考回答：** 流程是：PDF 解析提取文本 → 按标题层级和语义段落切块（不硬切字符）→ 每块过 embedding 模型转向量 → 存入向量库 → 查询时把用户问题向量化做相似度召回 → 返回 Top-K 片段。Tool 接口对 Agent 暴露的是"传入问题、返回相关文档片段"，Agent 不关心底层是 PDF 还是别的来源。

**关键点解析：**
- **面试官意图**：验证你真的做过 RAG，而不是"调了个 LangChain 的 Retriever 就说是自己的"。重点看分块策略、向量化、召回这几步。
- **怎么答**：按"解析 → 分块 → 向量化 → 入库 → 召回"五步讲，分块要强调"语义切分 + overlap"，别只说"我切成 500 字一段"。
- **深挖方向**：可能追问"embedding 模型用的什么""向量库是什么""Top-K 取多少"，准备好答案；追问"分块大小怎么定的"，答"根据模型上下文窗口和检索效果在测试集上调"。

**扣分项：** "我用 LangChain 的 loader 和 splitter"（说不清原理）；"切成固定 1000 字"（不懂语义切分）。

### Q14：通用农业知识 Tool 具体是什么？是直接调通用大模型吗？

**参考回答：** 通用农业知识 Tool 本质是"不接私有知识库，直接让模型基于自身参数知识回答"——可以理解为一个无 RAG 的模型调用封装成 Tool。它的作用是在私有检索低分命中、用户问的是常识性农业问题时兜底。它和私有检索 Tool 的区别是"有没有外部知识依据"。

**关键点解析：**
- **面试官意图**：很多人把"通用知识 Tool"说得含糊，面试官想确认你懂它就是"裸模型调用"，而不是又一个 RAG。
- **怎么答**：直接点明"本质是无 RAG 的模型调用"，并说清和私有 Tool 的边界（有没有外部依据）。诚实且清晰。
- **深挖方向**：可能追问"既然是裸模型，那它回答的可靠性怎么保证"，答"通过 System Prompt 约束它只回答常识性问题 + 输出风险提示，复杂病害防治仍走私有库；它本身就不是用来兜底专业问题的"。

**扣分项：** 把通用 Tool 说得很神秘；含糊带过"就是个接口"。

### Q15：用 System Prompt 约束"知识库优先"——具体怎么写？模型不遵守怎么办？

**参考回答：** Prompt 里明确写：① 你必须优先使用 search_private_kb 工具检索私有知识库；② 只有在工具返回结果低于相关度阈值或无结果时，才可使用 general_agriculture 工具兜底；③ 回答必须基于检索到的内容，不得编造未在工具结果中出现的数据。模型不遵守是常态——LLM 对 Prompt 的服从不是 100%。我的兜底是：检索 Tool 返回时带上相关度分数，分数过低我在代码层直接判定"未命中"，强制走兜底，而不是只靠 prompt 约束模型自觉。

**关键点解析：**
- **面试官意图**：考察"对 LLM 行为不确定性的认知"。很多人以为写句 prompt 模型就听话了，面试官想看你知道"prompt 约束是软的，代码约束是硬的"。
- **怎么答**：先说 prompt 怎么写（明确优先级 + 触发条件 + 禁止编造），再说"模型不遵守怎么办"——关键是把"软约束"补成"硬约束"（分数阈值在代码层强制路由）。
- **深挖方向**：可能追问"相关度阈值怎么定"，答"在测试集上观察命中和误命中分布，选一个让召回率和准确率平衡的点，不是拍脑袋"；追问"阈值定错会怎样"，答"太高漏召回，太低混入不相关，需要持续调"。

**扣分项：** 只说"我在 prompt 里写了优先用知识库"就完了；以为 prompt 100% 可靠。

### Q16：LangChain 和 LCEL 在这个项目里怎么用的？为什么用 LCEL？

**参考回答：** LangChain 提供组件——模型、Prompt、Tool、检索器、输出解析器；LCEL 是它的管道语法，用 `|` 把组件串成链。我用 LCEL 把"检索 → Prompt 组装 → 模型调用 → 输出解析"串成一条可复用链。选 LCEL 的原因：① 声明式组合，链结构清晰可读；② 原生支持流式（astream）、异步、批量；③ 支持 fallback 机制，主链失败可以自动切到备选链。比手写一堆函数调用更易维护。

**关键点解析：**
- **面试官意图**：区分"用过 LangChain"和"理解 LCEL"。后者是 LangChain 现在主推的范式，能说出 LCEL 的优势说明你跟得上版本。
- **怎么答**：点出 LCEL 三个实在的好处——声明式、流式原生、fallback。别只说"因为它新"。
- **深挖方向**：可能追问"LCEL 的 Runnable 接口""fallback 怎么配"，准备好"Runnable 支持 invoke/stream/batch 三种调用""with_fallbacks 接备选链"的说法。

**扣分项：** "LCEL 就是链式调用"（太浅）；说不清和普通函数调用的区别。

### Q17：InMemorySaver 是什么？为什么选它而不是数据库持久化？

**参考回答：** InMemorySaver 是 LangChain 用来存对话状态的检查点保存器，把每轮的对话历史和中间状态存在进程内存里，用 thread_id 作为 key 隔离不同会话。选它是因为这是实训项目，单实例、对话量不大，内存方案足够、开发快。它的局限我也清楚：服务重启会话丢失、多实例不共享。如果是生产，我会换成基于数据库或 Redis 的 Saver，比如 SqliteSaver 或自定义 RedisSaver。

**关键点解析：**
- **面试官意图**：考察"技术选型的场景意识 + 边界认知"。他想听你是不是因为"知道 InMemorySaver 的局限但当前场景够用"才选它，而不是不知道有更好的。
- **怎么答**：先说它是什么（检查点保存器 + thread_id 隔离），再说"为什么选"（实训场景够用、快），最后主动说"局限 + 生产替代方案"——这句是区分度，显示你知道边界。
- **深挖方向**：可能追问"生产换 Redis 的话，存什么结构"，答"按 thread_id 存消息列表 + 状态快照，设 TTL 控制容量"；追问"会话历史无限增长怎么办"，答"滑动窗口保留最近 N 轮 + 摘要压缩旧消息"。

**扣分项：** "InMemorySaver 就是存内存"（不懂检查点概念）；不知道重启会丢；说成生产方案。

### Q18：thread_id 隔离用户会话——多用户并发下怎么保证不串话？

**参考回答：** thread_id 是会话的唯一标识，每个用户或每次会话生成一个唯一的 thread_id，InMemorySaver 按 thread_id 分别存各自的对话状态。并发下不串话的前提是：thread_id 的生成和传递是可靠的——比如登录后绑定到用户 ID，或者前端每次请求带上会话 ID。只要 thread_id 不重复、不被串用，状态就是隔离的。真正的风险不在 InMemorySaver 本身，而在 thread_id 的来源不可控（比如前端写死或复用）。

**关键点解析：**
- **面试官意图**：深入一道，看你是不是真的理解"隔离"靠的是 key 的唯一性，而不是某个组件的魔法。
- **怎么答**：点明"隔离的根因是 thread_id 唯一性"，而不是 InMemorySaver 有什么并发魔法。再说风险点在 thread_id 来源。
- **深挖方向**：可能追问"如果前端伪造别人的 thread_id 会怎样"，答"会读到别人会话——所以生产要绑定鉴权，thread_id 从服务端基于用户身份生成或校验，不能信任前端传入"。

**扣分项：** "InMemorySaver 自己处理并发"（不懂原理）；忽略 thread_id 来源风险。

### Q19：多轮上下文怎么持续传递？作物、病害这些实体跨轮怎么保持？

**参考回答：** 每轮对话，Agent 把"用户本轮问题 + 历史消息"一起传给模型，模型自己理解上下文。比如用户第一轮说"我家番茄叶子发黄"，第二轮问"怎么治"，模型能从历史里知道"番茄"这个实体。InMemorySaver 存的就是这些历史消息。实体的持续传递不是我把"作物=番茄"单独存起来，而是整个对话历史作为上下文让模型自行维持。这也是多轮比单轮更准的原因——上下文消解指代。

**关键点解析：**
- **面试官意图**：很多人以为多轮要自己提取实体存状态，面试官想确认你懂"多轮上下文本质是历史消息回传给模型"。
- **怎么答**：纠正"手动存实体"的误解，说清"历史消息作为上下文回传，模型自行维持指代"。这是 LLM 多轮的基本原理。
- **深挖方向**：可能追问"历史消息很长怎么办""上下文窗口放不下"，答"滑动窗口保留最近 N 轮 + 对更早的消息做摘要压缩，控制 token"。

**扣分项：** 以为要自己提取"作物=番茄"存进状态机；说不清多轮为什么比单轮强。

### Q20：FastAPI 异步 astream 怎么封装的？

**参考回答：** FastAPI 的路由声明为返回 `StreamingResponse` 或直接用 `async` 生成器。我把 LangChain 链的 astream 输出包成一个 async generator，每次 yield 一个 token 片段，按 SSE 协议格式（`data: ...\n\n`）写出去。关键点：① 路由是 async 的，不阻塞事件循环；② astream 是模型原生流式，不等整段生成；③ 全局异常捕获包住生成器，出错时 yield 一个错误事件再关闭流，而不是让连接挂死。

**关键点解析：**
- **面试官意图**：考察 FastAPI 异步 + LLM 流式的工程封装能力。重点看"async 生成器 + SSE 格式 + 异常处理"的组合。
- **怎么答**：三个要点都要覆盖——async 不阻塞、astream 原生流式、异常 yield 后关流。最后一句是亮点，很多人流式异常处理是空的。
- **深挖方向**：可能追问"StreamingResponse 和 EventSourceResponse 区别""为什么用 async generator 不用同步"，答"async 不占 worker 线程，高并发下吞吐更好；EventSourceResponse 对 SSE 协议封装更完整（心跳、事件名）"。

**扣分项：** 只说"我用了流式"说不清封装；异常处理空白；同步接口硬返回流。

### Q21：SSE 流式输出，Token 分片返回——和一次性返回 JSON 有什么区别？为什么要流式？

**参考回答：** 区别在用户感知的首字延迟。一次性 JSON 要等整段生成完才返回，长文本可能等 10 秒，用户以为卡死；SSE 流式是模型每产出一个 token 就立刻推给前端，用户几百毫秒就看到字往外蹦，体感快很多。本质不是模型变快了，而是把"等完整结果"变成"边生成边显示"。代价是：流式不能做整体后处理（比如改完整体再返回），且前端要处理增量拼接。

**关键点解析：**
- **面试官意图**：考察"流式 vs 非流式"的本质权衡，而不是"流式就是好"。
- **怎么答**：点出"首字延迟改善"是核心收益，再说"模型没变快，是显示策略变了"——这句显示你懂本质。最后主动说代价（后处理受限、前端要拼接），显示全面思考。
- **深挖方向**：可能追问"结构化输出和流式怎么兼容"（这是个真实矛盾，见 Q24），或"流式下断网怎么办"，答"前端记录已收 token，断线后用补偿接口拉完整结果"。

**扣分项：** "流式更快所以用流式"（没懂本质）；只说好处不说代价。

### Q22：全局异常捕获在流式场景下怎么做？异常中断后怎么恢复？

**参考回答：** 流式异常捕获用 try/except 包住整个 async generator，捕获到异常时：① yield 一个结构化的错误事件（比如 `event: error, data: {msg}`）；② 关闭流，不让连接挂死；③ 后端记录 traceId 和错误日志。恢复有两种：轻量的——前端收到 error 事件后提示用户重试；重一点的——前端记录已收到的 token 和会话 thread_id，调一个补偿接口让后端基于 thread_id 续上，因为 InMemorySaver 还存着之前的上下文，续传是可行的。

**关键点解析：**
- **面试官意图**：流式异常处理是很多人空白的地方，能答好说明真做过流式生产。重点看"异常 yield + 关流 + 续传"。
- **怎么答**：三层——异常时发错误事件、关流、记日志；恢复靠 thread_id + InMemorySaver 续传。续传这点很亮，因为用上了 Q17 的检查点机制。
- **深挖方向**：可能追问"异常发生在模型生成一半，已推送的 token 怎么办"，答"已推送的就算了，续传时让模型基于已生成内容继续，或干脆让用户重新发起，取决于业务能否容忍重复"。

**扣分项：** 异常处理空白；"断了就断了用户自己重试"（无兜底）。

### Q23：打字机式展示——前端怎么对接 SSE？

**参考回答：** 前端用浏览器原生的 EventSource（或 fetch + ReadableStream 处理 SSE）监听后端流，每收到一个 data 事件就把 token 追加到消息容器，配合 CSS 或逐字延迟实现打字机视觉效果。关键点：① 前端要能增量拼接，不能每次全量替换；② 要处理完成事件（知道这次回答结束了）和错误事件（给用户提示）；③ 断线要有重连或补偿逻辑。打字机是展示层，核心是增量接收 SSE。

**关键点解析：**
- **面试官意图**：虽然你是后端，但面试官想看你对前后端契约的理解——SSE 不是后端推完就完，前端怎么消费你得更清楚才能定好契约。
- **怎么答**：点出"EventSource 监听 + 增量拼接 + 完成/错误事件处理 + 断线补偿"。强调契约意识（完成事件、错误事件要约定好）。
- **深挖方向**：可能追问"EventSource 不支持 POST 怎么办""跨域怎么处理"，答"EventSource 只支持 GET，复杂请求用 fetch + ReadableStream 手动解析 SSE；跨域要后端配 CORS 和 SSE 允许头"。

**扣分项：** 只说"前端展示一下"说不清对接；不知道完成事件要约定。

### Q24：Pydantic 结构化输出怎么约束的？模型不按格式输出怎么办？

**参考回答：** 用 Pydantic 定义回答 schema：作物（str）、病害（str）、风险等级（enum: 低/中/高）、防治方案（str）。LangChain 的结构化输出解析器会把这个 schema 转成 prompt 指令告诉模型，并尝试把模型输出解析回 Pydantic 对象。模型不按格式输出是常态，我的兜底链是：① 解析失败先有限重试（带上错误提示让模型纠正）；② 仍失败则用兜底默认值或标记字段缺失，不让脏数据进下游；③ 关键字段缺失（比如防治方案空）不直接返回前端，走兜底。结构化输出是"尽力而为 + 校验兜底"，不是强保证。

**关键点解析：**
- **面试官意图**：结构化输出是 AI 工程高频痛点，面试官想看你是不是"以为加了 Pydantic 就一定按格式"。重点看校验和兜底。
- **怎么答**：先说 schema 怎么定义，再说"模型不听话怎么办"——有限重试 + 兜底默认 + 关键字段不放过。强调"尽力而为"而非强保证。
- **深挖方向**：可能追问"流式和结构化怎么兼容"（流式时还没拿到完整 JSON 无法解析），答"流式推送原始 token 给前端展示，完整生成后再做结构化解析和校验，两段式；或者只流式展示正文，结构化字段在流结束后一次性返回"。

**扣分项：** "用了 Pydantic 就一定按格式"（不懂 LLM 不确定性）；解析失败直接抛错给用户。

### Q25：字段选了作物、病害、风险等级、防治方案——为什么选这几个字段？

**参考回答：** 这是按农业问答的核心信息需求设计的。作物和病害是问题的主语和对象，定位"这是什么病"；风险等级让用户快速判断严重程度；防治方案是用户最终要的行动建议。这四个字段覆盖了"识别 → 评估 → 应对"的完整决策链。没选价格、用量这种字段，是因为它们因地区、季节差异大，私有手册未必有，强行结构化反而容易编造。

**关键点解析：**
- **面试官意图**：考察"字段设计背后的业务思考"——是不是抄了个模板，还是真的想过用户要什么。
- **怎么答**：点出"识别 → 评估 → 应对"决策链，再说"为什么不选某些字段"——主动排除显示你有取舍意识。
- **深挖方向**：可能追问"如果用户问的不是病害防治呢，比如问种植技术"，答"那这套字段不完全适用，实际要做字段集的分支或更通用的 schema，这个项目聚焦病害问答所以简化了"。

**扣分项：** "就是随便定的"；说不出字段间的逻辑关系。

### Q26：通用大模型农业病害方案编造——你怎么发现这个问题的？怎么定位是"编造"？

**参考回答：** 我在测试时让模型回答某种具体病害的防治，它给出很详细的方案，但去对照私有农技手册发现手册里根本没这条，或者用量和手册不符——这就是编造。定位"编造"的方法是：让回答必须带检索来源引用，如果模型说的内容在引用片段里找不到对应，就判定为脱离资料的编造。这也是我上"私有优先 + 检索约束"的动机——把回答限定在有依据的内容上。

**关键点解析：**
- **面试官意图**：考察"问题发现意识"——你是真在业务里撞到幻觉了，还是听说 RAG 能治幻觉就套上去。能说出"怎么定位是编造"说明你做过验证。
- **怎么答**：讲一个具体例子（某病害方案和手册不符），再说"怎么定位"——靠检索来源引用比对。这把"幻觉"从一个抽象概念落到了可验证的方法。
- **深挖方向**：可能追问"完全消除幻觉可能吗"，答"不可能，LLM 本质是概率生成；工程目标是降低频率和让回答可追溯，不是零幻觉"。

**扣分项：** "听别人说大模型会编造"；说不出怎么定位编造。

### Q27：如果检索不到相关内容，Agent 怎么处理？直接说不知道还是硬编？

**参考回答：** 按我的双工具路由：私有检索分数低于阈值判定"未命中"，这时不是硬编，而是走通用知识 Tool 兜底；如果通用知识 Tool 也判定不靠谱（比如问题太专业），就在回答里明确告知"知识库未覆盖该问题，建议咨询农技专家"，并在风险等级字段标"未知"。核心原则是：宁可说不知道，也不编造具体数据。回答里会带来源标识，让用户区分"有依据"和"兜底"。

**关键点解析：**
- **面试官意图**：这道题考"幻觉防控的最后一道关"——检索不到时的处理，是衡量工程成熟度的关键。
- **怎么答**：讲清"私有未命中 → 通用兜底 → 都不行就诚实说不知道 + 标记未知"，绝不硬编。带来源标识是亮点。
- **深挖方向**：可能追问"说不知道会不会影响用户体验"，答"短期看是损失，但农业防治给错方案代价更大（误用农药）；长期看诚实回答建立信任，且可以引导用户补充信息再检索"。

**扣分项：** "检索不到就让模型自己答"（回到编造）；"直接返回空"（无兜底）。

### Q28：PDF 文档预处理流程？怎么做分块？切分粒度怎么定？

**参考回答：** 流程：PDF 解析（提取文本，注意表格和图片的处理）→ 文本清洗 → 按标题层级和语义段落切块，保留少量 overlap 避免上下文被截断 → 每块带上来源页码、章节等元数据 → 向量化入库。切分粒度不是一次定死，在测试集上对比不同块大小对召回率和准确率的影响再定。原则是：块太大检索不精准（一个块里混多个主题），块太小丢上下文（一个块说不清一件事），要找平衡点。

**关键点解析：**
- **面试官意图**：RAG 基础题，考察分块策略的理解——是只会调 splitter，还是懂粒度权衡。
- **怎么答**：流程 + 粒度权衡 + 元数据 + 可调。粒度权衡那句"太大不精、太小丢上下文"是关键。
- **深挖方向**：可能追问"overlap 设多少""表格怎么切"，答"overlap 一般块大小的 10–20%；表格要么整块保留要么转成结构化文本再切，不能硬切"。

**扣分项：** "切成固定字符数"；overlap 是什么说不清；不知道粒度要调。

### Q29：这个项目如果重新做，你会改进什么？

**参考回答：** 四点：① 会话状态从 InMemorySaver 迁到 Redis/数据库，支持重启恢复和多实例；② 检索效果可观测化——记录每次召回的 query、命中片段、相关度分数、是否被采用，建评测集量化召回质量；③ 幻觉防控再加深一层——给回答加引用溯源，前端可点击查看原文片段；④ 部署容器化，现在实训是本地跑，生产要 Docker + 健康检查 + 限流，避免单个长连接拖垮服务。

**关键点解析：**
- **面试官意图**：考察"复盘和改进意识"——技术经理很看重你能不能看出自己项目的不足。这也是判断成长性的题。
- **怎么答**：四个改进点对应四个维度——可靠性（状态持久化）、可观测（检索效果量化）、可信度（引用溯源）、生产化（容器部署）。每点都对应你当前方案的边界，显示你知道局限。
- **深挖方向**：可能追问"哪个最优先"，答"会话状态持久化——它直接决定可用性，重启丢会话是硬伤，先补这个"。

**扣分项：** "我觉得没什么可改进"（无复盘能力）；改进点空泛（"要做得更好"）。

### Q30：农业问答和通用 ChatBot 的本质区别是什么？

**参考回答：** 三点本质区别：① 知识来源——通用 ChatBot 靠模型参数知识，农业问答必须靠外部私有知识库，因为模型参数里没有这本农技手册；② 准确性要求——通用闲聊可以发散，农业防治给错方案会误农，所以要约束回答依据 + 幻觉防控；③ 输出形态——通用 ChatBot 是自由文本，农业问答要结构化（作物、病害、防治方案），便于前端渲染和后续对接业务系统。本质是从"自由对话"收敛到"有依据、可结构化、可追溯的专业问答"。

**关键点解析：**
- **面试官意图**：拔高题，看你能不能跳出实现讲清"这个品类的本质"。这是区分"会用工具"和"懂业务"的题。
- **怎么答**：三点对比——知识来源、准确性、输出形态。落脚在"从自由对话收敛到专业问答"，这句话显示你抓住了品类本质。
- **深挖方向**：可能追问"那如果做法律咨询、医疗问答呢"，答"本质相同——都是私有知识 + 高准确性 + 结构化输出 + 幻觉防控，农业问答的这套架构可以迁移过去，只是知识库和字段不同"。

**扣分项：** "就是加了个 RAG"；说不出和通用 ChatBot 的本质差异。

---

## 模块三：项目深挖 — 电商自然语言 AI 智能检索系统（Q31–Q55）

> 4 人实训项目（2026.05–2026.08），候选人角色：**AI 模块全权负责人**。
> 技术栈：SpringCloud Alibaba + Nacos + OpenFeign（Java 侧）/ FastAPI + LangChain + RedisStack + MySQL/SQLAlchemy + Elasticsearch + RocketMQ + Elastic-Job（AI 侧）。
> 核心设计：商品 RAG 全链路；口语化查询；ES 关键词 + AI 语义双模式；幂等入库；三层幻觉防控；异步同步一致性。

### Q31：这个项目整体架构？Java 和 Python 两个服务怎么分工协作？

**参考回答：** 整体是 SpringCloud 微服务 + 独立 Python AI 检索服务。Java 侧负责业务微服务（用户、订单等），Python 侧（FastAPI）负责 AI 语义检索和向量同步。协作链路：Java 微服务通过 OpenFeign 远程调用 FastAPI 的"向量同步接口"（商品变更时把 SKU 推过去建索引）和"智能推荐接口"（查询时走语义检索）；前端可自由切换"传统 ES 关键词检索"和"AI 语义检索"两种模式。我是 AI 模块负责人，独立完成 Python 侧全部开发 + 接口联调。

**关键点解析：**
- **面试官意图**：第一道项目题，判断你对跨语言架构全貌的掌控 + 个人边界。重点看你能不能讲清"为什么拆两个服务"和"调用链路"。
- **怎么答**：架构全景 + 分工 + 调用链路 + 个人边界。边界那句很重要——4 人项目，主动说清"我是 AI 模块全权负责"，不揽 Java 业务侧的功，显诚实。
- **深挖方向**：顺着 OpenFeign（Q48）、双模式（Q42）、幂等入库（Q33）、异步同步（Q52）往下问，所以全景讲 40 秒。

**扣分项：** 把 4 人项目说成"都是我做的"；说不清两个服务为什么拆开。

### Q32：为什么 AI 检索用 Python/FastAPI 而不是直接在 Java 里做？

**参考回答：** 三个原因：① AI 生态在 Python——LangChain、向量库客户端、embedding 调用这些在 Python 里成熟且文档全，Java 侧集成成本高；② 职责隔离——AI 推理是 IO 密集 + 长延迟，独立服务不影响 Java 业务服务的稳定性，也方便独立扩容；③ 团队分工——AI 模块由我负责，Python 是我的主栈，拆开能让团队并行开发不互相阻塞。代价是引入了跨语言调用开销和接口契约维护成本，但用 OpenFeign + 契约文档可以控制。

**关键点解析：**
- **面试官意图**：考察"技术选型的理由 + 权衡"，而不是"因为我会 Python"。他想听你是不是想过"为什么不在 Java 做"。
- **怎么答**：三点理由——生态、隔离、分工，再补一句代价（跨语言开销 + 契约成本），显示有取舍意识。
- **深挖方向**：可能追问"跨语言调用性能损耗怎么办"，答"OpenFeign 走 HTTP 有序列化和网络开销，对高频低延迟接口可以用 gRPC 或连接池复用 + 异步；但语义检索本身延迟在百毫秒级，HTTP 开销占比小"。

**扣分项：** "因为我熟悉 Python"；只说好处不说代价。

### Q33：商品向量入库怎么做幂等的？为什么要幂等？

**参考回答：** 用 MD5 生成文档指纹。流程：从 MySQL 读 SKU → 文本拼接（名称+描述+属性）→ 递归分片 → 对每个分片内容算 MD5 → 入向量库时以 MD5 为唯一键。重复推送同一商品时，MD5 相同说明内容没变，跳过；内容变了 MD5 不同，做更新（删旧片段、插新片段）。要幂等是因为：商品资料会持续变更、重复推送（比如消息重复消费或全量校对任务重跑），不做幂等会写入大量重复向量，污染召回结果还浪费存储。

**关键点解析：**
- **面试官意图**：幂等是后端高频概念，考察你是不是真在 RAG 入库场景用过。重点看"为什么要幂等 + 怎么实现"。
- **怎么答**：先讲为什么要（重复推送/重复消费会污染），再讲怎么实现（MD5 指纹做唯一键 + 内容变才更新）。MD5 用于"内容是否变化"的判断，这点要说清。
- **深挖方向**：可能追问"MD5 碰撞怎么办""为什么不直接用 SKU ID"，答"MD5 碰撞概率极低，且用于幂等判断而非唯一主键可接受；SKU ID 标识商品但商品内容会变，需要内容指纹判断是否更新，两者职责不同——ID 是业务主键，MD5 是内容版本"。

**扣分项：** 说不清为什么要幂等；"用 SKU ID 就行"（混淆了商品和内容版本）。

### Q34：文本拼接和递归分片具体怎么做？为什么用递归分片？

**参考回答：** 文本拼接是把 SKU 的名称、描述、规格属性拼成一段可检索文本。递归分片是 LangChain 的 RecursiveCharacterTextSplitter——先用一组分隔符（段落 → 句号 → 逗号）递归切分，直到块小于设定大小。用递归而不是固定字符切的原因：它优先在自然语义边界（段落、句子）切，尽量不把一句话从中间切断，保留语义完整性，对后续检索召回质量更友好。

**关键点解析：**
- **面试官意图**：深入 RAG 数据处理，看你分块是不是只会"固定 500 字"。RecursiveCharacterTextSplitter 是 LangChain 最常用的 splitter，能说出"优先在语义边界切"说明懂原理。
- **怎么答**：点出"递归 + 分隔符优先级 + 保语义完整"。
- **深挖方向**：可能追问"块大小设多少""Token 粒度是什么意思"，答"按 embedding 模型的 token 上限设块大小，留余量；Token 粒度指按 tokenizer 计数而非字符数，更贴合模型上下文"。

**扣分项：** "切成 1000 字一段"；不知道递归分片的语义边界优先。

### Q35：RedisStack 向量检索怎么搭建的？为什么选 RedisStack 而不是专用向量库？

**参考回答：** RedisStack 是 Redis 的扩展模块，支持向量索引（HNSW 或 FLAT）和向量检索命令。我建索引时定义向量维度、索引类型（HNSW）、距离度量（余弦相似度），把商品向量写入；查询时把用户问题向量化，用 FT.SEARCH 做近似近邻检索。选 RedisStack 的原因：① 项目本来就用 Redis 做缓存，向量检索复用同一基础设施，运维成本低；② 实训数据量不大，RedisStack 性能够用；③ 它还能同时存结构化字段，方便混合过滤。局限是数据量到百万级以上，专用库（Milvus、Qdrant）更合适。

**关键点解析：**
- **面试官意图**：考察"向量库选型理由 + 边界认知"。很多人无脑上专用向量库，面试官想看你是不是权衡过"复用 vs 专用"。
- **怎么答**：先说怎么搭（索引 + 维度 + 距离度量 + 检索命令），再说"为什么选"（复用 Redis + 数据量够用 + 混合过滤），最后主动说局限（百万级以上换专用库）。
- **深挖方向**：可能追问"HNSW 和 FLAT 区别""为什么用余弦不用欧氏"，答"HNSW 是图索引，查询快但建索引慢、占内存；FLAT 暴力计算，准但慢，适合小数据；余弦关注方向相似度，对文本语义更合适，且对向量长度不敏感"。

**扣分项：** "因为 Redis 熟"（没权衡）；不知道 RedisStack 性能边界。

### Q36：余弦相似度召回怎么做？为什么用余弦而不是欧氏距离？

**参考回答：** 把用户问题和商品向量都归一化后算余弦相似度，按分数排序取 Top-K。用余弦的原因：文本语义相似度关注的是向量方向（语义是否相近），而不是绝对长度；不同长度文本的 embedding 向量模长不同，欧氏距离会被长度干扰，余弦只看夹角，更符合"语义相近"的直觉。而且余弦取值 [-1,1]，便于设阈值过滤低分结果。

**关键点解析：**
- **面试官意图**：向量检索基础，考察对距离度量的理解，而不是只会调 API。
- **怎么答**：点出"余弦看方向、对长度不敏感、符合语义相似直觉、便于设阈值"。
- **深挖方向**：可能追问"归一化后余弦和欧氏什么关系"，答"向量归一化后，余弦相似度和欧氏距离是单调相关的（一个最大等价另一个最小），但工程上余弦的 [-1,1] 区间更便于设阈值和解释"。

**扣分项：** 说不清余弦和欧氏区别；"随便选的"。

### Q37：MMR 去重排序是什么？为什么要做去重？

**参考回答：** MMR（Maximal Marginal Relevance，最大边际相关性）是一种兼顾相关性和多样性的重排算法。纯余弦召回 Top-K 会出现"结果都很相似"的问题——比如召回 5 个商品都是同一款不同颜色，用户其实想看不同品类。MMR 的做法是：每次选一个候选时，不只看它和 query 的相似度，还看它和已选结果的相似度，惩罚过于接近已选结果的候选，从而让最终结果既相关又多样。在电商场景，多样性直接影响用户体验（不想看一屏同款）。

**关键点解析：**
- **面试官意图**：MMR 是 RAG 进阶点，能说出"为什么去重"说明你懂业务，而不是炫技。
- **怎么答**：先说纯召回的问题（结果同质化），再说 MMR 怎么解（相关性 + 多样性权衡），最后落到业务（电商多样性影响体验）。
- **深挖方向**：可能追问"MMR 的 lambda 参数怎么调""去重会不会牺牲相关性"，答"lambda 控制相关性和多样性的权重，在测试集上调；过度去重确实会牺牲相关性，所以 lambda 要平衡，不是越大越好"。

**扣分项：** "去重就是删重复的"（不懂 MMR 是多样性而非去字面重）；说不清为什么要做。

### Q38：三层幻觉防控——"检索低分过滤 → System Prompt 约束 → 商品实体校验"，每层具体做什么？

**参考回答：** 第一层检索低分过滤：向量召回时设相似度阈值，低于阈值的片段直接丢弃，不进入模型上下文，避免模型基于不相关内容编造。第二层 System Prompt 约束：prompt 明确要求"回答必须基于检索到的商品信息，不得编造价格、规格、库存等未在资料中出现的事实"。第三层商品实体校验：模型输出后，把回答里提到的商品名、价格、规格回查商品库实体，对不存在的字段标记或剔除，防止模型把不存在的产品说成有。三层是"输入侧过滤 → 生成侧约束 → 输出侧校验"的闭环。

**关键点解析：**
- **面试官意图**：这是简历亮点，必考。考察你是不是真的设计了三层，还是随便堆了三个词。重点看每层在数据流的什么位置。
- **怎么答**：三层按"输入侧 → 生成侧 → 输出侧"定位，每层给一个具体动作。最后那句"闭环"显示你理解这是端到端的防控，不是三件孤立的措施。
- **深挖方向**：可能追问"哪层最关键""校验失败怎么处理"，答"检索过滤是基础，没有好检索后两层都救不回来；实体校验失败的话，对应字段标'以商品页为准'或拒绝回答该属性，不让脏数据到用户"。

**扣分项：** 三层说不清各自在数据流的位置；"就是加了几道检查"（没设计感）。

### Q39：System Prompt 约束模型不编造价格——真的管用吗？模型还是会编怎么办？

**参考回答：** Prompt 约束是软约束，能降低编造频率但不能保证零。实测会显著减少"明显编造"，但偶尔仍会编。所以我不只靠这一层——前面有检索低分过滤保证输入相关，后面有实体校验兜底。三层叠加才是防控，单靠 prompt 不可靠。本质是承认 LLM 是概率生成，工程上用多层 + 校验把不可靠收敛到可控，而不是指望一句 prompt 解决。

**关键点解析：**
- **面试官意图**：这道题是在挑战你的设计——"prompt 真的管用吗"。面试官想看你是不是对 LLM 的不确定性有清醒认知，而不是迷信 prompt。
- **怎么答**：诚实承认"prompt 是软约束、不能保证零"，再说"所以才有三层、不是单靠 prompt"。这显示你不迷信单一手段、有系统思维。
- **深挖方向**：可能追问"那为什么不干脆不用 prompt 约束"，答"prompt 虽然软但成本低、能显著降低频率，是性价比最高的一层；硬约束（实体校验）成本高但不能漏，两者配合"。

**扣分项：** "加了 prompt 就不编了"（迷信）；"prompt 没用就别用了"（走极端）。

### Q40：商品实体校验怎么做？校验失败的字段怎么处理？

**参考回答：** 模型输出结构化结果后，把里面提到的商品名、价格、规格等字段，和商品库做回查：商品名是否存在于库、价格是否和库里的价格一致、规格是否匹配。校验失败的处理：价格不一致——以商品库为准，覆盖模型输出，或标注"请以商品页为准"；商品名不存在——剔除该条或标"未找到该商品"；规格不符——标"具体规格以商品页为准"。原则是：模型不能自己造商品和价格，事实必须以业务库为准，模型只负责组织语言。

**关键点解析：**
- **面试官意图**：第三层校验的具体实现，考察"事实以业务库为准"的意识——这是 AI 和业务系统协作的核心原则。
- **怎么答**：点出"回查 + 失败字段的分级处理"，强调"事实以业务库为准、模型只组织语言"。这句原则是亮点。
- **深挖方向**：可能追问"每次都回查商品库性能怎么办"，答"按商品 ID 批量查 + 缓存热门商品实体，校验是结构化字段比对，成本可控；实在敏感的场景可以只校验关键字段（价格、库存）不校验描述"。

**扣分项：** "校验失败就报错"；不知道事实要以业务库为准。

### Q41：Agent 和多轮筛选——用户连续提价格、品牌条件怎么承接？

**参考回答：** 用户第一轮说"找手机"，第二轮说"2000 到 3000 的"，第三轮说"华为的"。Agent 用 LangChain 封装向量检索 Tool，每次调用时把会话记忆里的历史条件提取出来——"手机 + 2000-3000 + 华为"——组合成带过滤条件的检索请求，而不是只检索最后一轮的"华为"。多轮的关键是把历史意图累积成过滤条件，配合会话记忆承接，让用户能像和导购对话一样逐步收敛。

**关键点解析：**
- **面试官意图**：考察"多轮条件累积"的工程实现，这是导购场景的核心难点。重点看你是不是把历史条件累积，而不是每轮独立检索。
- **怎么答**：举一个三轮累积的具体例子，点出"历史意图累积成过滤条件 + 会话记忆承接"。
- **深挖方向**：可能追问"条件冲突怎么办"，答"后说覆盖先说（'不要华为了'就移除华为过滤），或让 Agent 判断意图是'追加'还是'修改'，这需要一定的意图识别"。

**扣分项：** "每轮独立检索"（没累积）；说不清条件怎么组合。

### Q42：前端支持 ES 关键词和 RAG 语义双模式切换——为什么保留两种？

**参考回答：** 因为两种检索各有适用场景。ES 关键词检索：精确匹配强（用户搜"iPhone 15"这种明确型号）、性能高、成本低，适合明确意图的查询。RAG 语义检索：理解口语化模糊表达（"适合老人用的手机""续航长的"）、覆盖同义词和近义表达，适合模糊意图。保留两种让用户按场景选，也方便 A/B 对比效果。一刀切用语义会让精确查询变慢且可能偏，一刀切用关键词又处理不了口语化需求。

**关键点解析：**
- **面试官意图**：考察"混合检索的设计理由"。这是电商检索的常见架构，能说出"各有所长"说明懂权衡。
- **怎么答**：对比两种的适用场景，落到"保留双模式让用户选 + 便于对比"。最后那句"一刀切的代价"显示你理解为什么不只选一个。
- **深挖方向**：可能追问"能不能自动判断用哪种"，答"可以——用意图识别判断查询是明确型还是模糊型，自动路由；但实训阶段先做手动切换便于对比效果和收集数据"。

**扣分项：** "语义检索更好所以只用语义"；说不出两种各自的优势。

### Q43：ES + IK 分词 + DSL 多条件筛选怎么实现？

**参考回答：** 索引设计：全文检索字段（商品名、描述）用 text 类型 + IK 分词（索引侧用 ik_max_word 细粒度切分提高召回，查询侧按需选 ik_smart）；精确匹配和排序字段（价格、品类、品牌）用 keyword。查询用 bool 组合：must 放关键词匹配（match）、filter 放精确过滤（term/range 价格区间、品类）、sort 排序。深分页用 search_after 代替 from+size，避免性能问题。

**关键点解析：**
- **面试官意图**：ES 是 Java 后端高频，考察基础是否扎实。重点看字段类型、分词策略、bool 查询、分页优化。
- **怎么答**：四个点——字段类型（text vs keyword）、分词（ik_max_word 索引 / ik_smart 查询）、bool 组合（must/filter/sort）、分页（search_after）。
- **深挖方向**：可能追问"为什么 filter 不用 must""深分页为什么慢"，答"filter 不算分只过滤、能走缓存性能好；from+size 深分页要协调所有分片取 from+size 条再合并，深度越大越慢，search_after 用上一页最后一条排序值定位，性能稳定"。

**扣分项：** 全用 text 不知道 keyword；filter 和 must 分不清；不知道深分页问题。

### Q44：向量召回和 ES 关键词怎么融合排序？

**参考回答：** 两路结果融合常用 RRF（Reciprocal Rank Fusion，倒数排名融合）或加权分数融合。RRF 的做法：不直接比两路的原始分数（向量分数和 ES 相关度分数量纲不同），而是看每条结果在各自结果列表里的排名，按排名算融合分（排名越靠前贡献越大），这样把两路 Top-K 合并成一个统一排序。加权融合则是把两路分数归一化后按权重相加。RRF 的好处是不用调权重、对分数分布不敏感，工程上更稳。

**关键点解析：**
- **面试官意图**：混合检索的融合是进阶点，能说出 RRF 说明你真做过双路融合。重点看"为什么不直接加分数"。
- **怎么答**：点出"分数量纲不同不能直接加 → 用排名融合（RRF）"，并说 RRF 的好处（免调权重、稳）。
- **深挖方向**：可能追问"RRF 的公式""加权融合什么时候用"，答"RRF 融合分 = Σ 1/(k + rank)，k 常取 60；加权融合在两路分数可解释且稳定时用，比如明确知道语义权重该多少"。

**扣分项：** "把两个分数加起来"（量纲都没统一）；不知道 RRF。

### Q45：embedding 模型怎么选？向量维度多少？查询时怎么向量化？

**参考回答：** 选 embedding 模型考虑：中文支持好（电商是中文）、维度适中（维度高表达强但存储和检索成本高）、推理速度。向量维度由模型决定，常见的 768、1024。查询时把用户问题用同一个 embedding 模型转成向量，再去做近似近邻检索——关键是查询和入库必须用同一个模型和同一套预处理，否则向量空间不一致，相似度无意义。

**关键点解析：**
- **面试官意图**：embedding 是 RAG 的基础，考察你是不是真调过，知道"查询和入库同模型"这个基本点。
- **怎么答**：选型考虑（中文 + 维度 + 速度）+ 查询向量化 + "同模型同预处理"这个关键约束。
- **深挖方向**：可能追问"换 embedding 模型怎么办""向量维度太高怎么优化"，答"换模型必须重建全部索引，因为向量空间不同；维度高可以用量化（PQ/SQ）压缩存储和加速检索"。

**扣分项：** "随便选个模型"；不知道查询和入库要同模型。

### Q46：商品资料持续变更——怎么保证向量库和业务库一致？

**参考回答：** 我用"增量 + 定时补偿"双机制。增量：商品变更时 Java 侧通过 RocketMQ 异步推送变更消息，Python 侧消费后更新向量库（MD5 指纹判断内容变没变，变了才更新）。定时补偿：Elastic-Job 定时跑全量校对任务，把 MySQL 全量商品和向量库做对账，发现遗漏或多余的向量修复掉，兜底增量同步可能丢的消息。增量保证实时性，定时保证最终一致，两者互补。

**关键点解析：**
- **面试官意图**：数据一致性是后端必考，考察你是不是只会"发个消息同步"，还是有补偿兜底。重点看"增量 + 全量对账"双保险。
- **怎么答**：增量（MQ 推送 + 消费更新）+ 定时（全量对账兜底）+ 各自职责（实时 vs 最终一致）。这是生产级思维。
- **深挖方向**：可能追问"增量消息丢了怎么办""对账发现不一致怎么修"，答"消息丢靠定时对账兜底；对账不一致按商品维度重新拉取修复 + 记录对账日志，差异大的话触发告警人工介入"。

**扣分项：** 只说"发 MQ 同步"无兜底；不知道增量可能丢消息。

### Q47：RocketMQ 异步推送商品变更——为什么用 MQ 而不是同步调用？

**参考回答：** 三个原因：① 解耦——Java 业务侧不需要等 Python 向量库更新完才返回，商品变更操作快速完成不阻塞；② 削峰——大促批量改价时同步调 Python 会被打爆，MQ 缓冲让消费按能力处理；③ 容错——消费失败可以重试，不像同步调用失败就丢。代价是引入最终一致延迟（向量库更新有滞后），所以才有 Elastic-Job 定时对账兜底。同步调用适合"必须立即一致"的场景，但商品向量更新允许滞后，用 MQ 更合适。

**关键点解析：**
- **面试官意图**：MQ 的经典理由题，考察你是不是只会"因为要用 MQ 所以用"，还是懂解耦/削峰/容错。
- **怎么答**：三点理由 + 代价（最终一致延迟）+ 为什么这个场景能接受代价。最后对比"同步适合什么场景"显示你懂选型。
- **深挖方向**：可能追问"消息重复消费怎么办""消费顺序怎么保证"，答"重复消费靠 MD5 幂等天然防重；商品顺序性不强（每个商品独立），不需要全局顺序，并行消费即可"。

**扣分项：** "因为要用 MQ"；只说好处不说最终一致代价。

### Q48：OpenFeign 跨语言调用怎么做的？Java 调 Python 接口要注意什么？

**参考回答：** OpenFeign 是声明式 HTTP 客户端，我在 Java 侧定义一个接口注解 @FeignClient 指向 FastAPI 的 URL，方法映射到 FastAPI 的 REST 端点，调用时像调本地方法一样。注意点：① 超时——Python 侧语义检索是长延迟，Feign 的超时要设够长，别用默认短超时；② 异常——Feign 调用失败要降级（fallback），返回空结果或提示而非抛错把 Java 服务拖崩；③ 契约——接口参数和返回结构要和 FastAPI 严格对齐，用契约文档约束；④ 序列化——Java 对象和 JSON 的转换要处理空值和日期格式。

**关键点解析：**
- **面试官意图**：OpenFeign 是 SpringCloud 高频，考察你是不是真做过跨语言调用，知道超时/降级/契约这几个坑。
- **怎么答**：先说 Feign 是声明式 HTTP，再讲四个注意点——超时、异常降级、契约、序列化。超时和降级是跨语言调用的核心坑。
- **深挖方向**：可能追问"Feign 超时和 Ribbon/LoadBalancer 超时谁优先""fallback 怎么实现"，答"Feign 的超时配置优先级高于负载均衡器；fallback 实现对应接口的 fallback 类，在超时或异常时返回兜底结果"。

**扣分项：** "就是个 HTTP 调用"（不懂 Feign 声明式）；没考虑超时和降级。

### Q49：FastAPI 怎么和 OpenFeign 对接接口契约的？

**参考回答：** 以 FastAPI 的 OpenAPI 文档为准。FastAPI 自动生成 OpenAPI schema（/docs 或 /openapi.json），定义了每个接口的路径、参数、请求体和响应结构。Java 侧按这个文档定义 Feign 接口的签名和 DTO，字段名、类型、嵌套结构严格对齐。联调前先评审契约，联调时以文档为准，发现分歧先改契约再改代码。FastAPI 用 Pydantic 定义请求/响应模型，和 Java 的 DTO 字段一一对应。

**关键点解析：**
- **面试官意图**：接口契约是跨语言协作的核心，考察你有没有"契约先行"意识。
- **怎么答**：以 FastAPI 的 OpenAPI 为单一事实来源，Java DTO 对齐，契约先行 + 联调以文档为准。Pydantic 和 DTO 对应这点显示你真做过。
- **深挖方向**：可能追问"字段命名风格不同（Java 驼峰 / Python 蛇形）怎么办"，答"配置 Jackson 的命名策略做转换，或 FastAPI 用 alias 统一成驼峰，关键是契约里约定死一种风格"。

**扣分项：** "口头对一下就行"；没有契约文档意识。

### Q50：FastAPI 的 REST 接口设计——向量同步和智能推荐两个接口怎么划分？

**参考回答：** 按职责分两个接口：向量同步接口（POST）接收商品变更数据，做分片、MD5、向量库更新，是写路径，幂等；智能推荐接口（POST 或 GET）接收用户查询，做向量化、召回、MMR、三层幻觉防控、结构化输出，是读路径。分开的原因：写路径关注一致性和幂等（有 MQ 触发），读路径关注延迟和召回质量，两者 QPS 和 SLA 不同，独立接口便于独立优化和限流。

**关键点解析：**
- **面试官意图**：接口设计能力，考察你是不是把"读写职责"想清楚了。
- **怎么答**：读写分离 + 各自关注点（写一致性、读延迟）+ 为什么分开（QPS/SLA 不同）。读写分离是后端基本素养。
- **深挖方向**：可能追问"向量同步接口的幂等怎么做"（回到 Q33 的 MD5），"推荐接口返回什么结构"（回到 Pydantic 结构化）。

**扣分项：** 把同步和查询塞一个接口；读写职责混在一起。

### Q51：SQLAlchemy 读取 MySQL SKU——为什么不直接查全量一次性处理？

**参考回答：** 全量一次性读会撑爆内存——商品多时一次拉几十万条 SKU 进内存不现实。用 SQLAlchemy 分页读取或流式游标，按批次处理：读一批 → 拼接 → 分片 → MD5 → 入向量库 → 下一批。这样内存占用恒定可控，且中途失败可以从断点续做，不用从头再来。全量校对任务（Elastic-Job）也是分批跑，避免一次拉全表锁表或占内存。

**关键点解析：**
- **面试官意图**：考察大数据量处理的工程意识。很多人写过 `SELECT *` 全量拉，面试官想看你懂分批。
- **怎么答**：点出"全量撑爆内存 → 分批/流式 + 内存恒定 + 断点续做"。
- **深挖方向**：可能追问"分页查询深分页慢怎么办"，答"用游标分页（基于 ID 或时间戳的 where 条件）代替 offset 分页，避免深分页性能问题"。

**扣分项：** "SELECT * 全量读"；不知道内存问题。

### Q52：异步同步和一致性保障——增量更新和定时补偿具体怎么做？

**参考回答：** 增量更新：Java 业务侧商品变更时发 RocketMQ 消息，Python 消费端收到后，用 MD5 判断内容是否变化，变了就更新向量库对应向量（删旧插新），幂等防重复消费。定时补偿：Elastic-Job 定时（比如每天凌晨）跑全量校对——分批拉 MySQL 全量商品，和向量库逐条对账，向量库多出的（已下架商品）删除、缺失的（增量漏掉的）补建、MD5 不一致的重算。增量保实时，定时保最终一致，两者形成兜底闭环。

**关键点解析：**
- **面试官意图**：这是简历明确写的"增量更新与定时补偿机制"，必考。考察你能不能讲清两条链路各自做什么、怎么兜底。
- **怎么答**：增量链路（MQ + MD5 + 幂等 + 删旧插新）+ 定时链路（全量对账 + 多删少补 + 重算）+ 闭环关系。结构清晰是关键。
- **深挖方向**：可能追问"对账任务跑全量会不会很慢影响业务"，答"凌晨低峰跑 + 分批 + 限流，不影响在线；也可以增量对账（只对账最近变更的）减少全量频率"。

**扣分项：** 增量和定时混在一起说；说不清谁兜底谁。

### Q53：口语化模糊商品查询——"适合老人用的手机"这种怎么检索？

**参考回答：** 这种查询关键词检索（ES）很难处理——"老人用"不是商品属性字段，ES 关键词召回不到合适结果。语义检索的优势就在这：embedding 把"适合老人用"和"大字体、长续航、简易操作"这类商品的描述向量拉近，向量召回能跨字面匹配语义。所以口语化查询走 RAG 语义模式，向量召回 + MMR 多样性 + 三层幻觉防控，返回符合"老人适用"语义的商品。这也是保留双模式的意义——ES 处理不了这种，必须语义。

**关键点解析：**
- **面试官意图**：用具体例子考察你懂语义检索相对关键词检索的价值，而不是空谈"语义更好"。
- **怎么答**：点出"ES 处理不了非属性语义表达 → 向量跨字面匹配语义"，落到具体例子。这是语义检索的核心价值场景。
- **深挖方向**：可能追问"embedding 怎么知道老人要大字体"，答"embedding 模型在大量语料上预训练，学到了'老人用'和'大字体/简易'的语义关联，不是我们教的，是模型参数里的知识"。

**扣分项：** "语义检索就是更准"；说不出语义检索解决了关键词解决不了的什么问题。

### Q54：这个项目你作为 AI 模块负责人，具体怎么和 Java 侧协作的？

**参考回答：** 三个动作：① 契约先行——我先把 FastAPI 接口的 OpenAPI 文档定下来，和 Java 同学评审字段、错误码、分页规则，再各自开发；② 并行开发——契约定好后，Java 侧用 Mock 或我给的样例数据先开发，我这边独立做 Python 实现，通过 OpenFeign 对接时再联调；③ 联调排障——接口对不齐时，先看是契约理解偏差还是实现 bug，改契约走文档变更，改代码走各自提交，不口头改。遇到跨语言字段不一致（命名、空值、日期格式）由我主导统一到契约。

**关键点解析：**
- **面试官意图**：考察跨语言协作的工程能力，这是你简历"双栈协同"的验证。重点看契约先行 + 并行开发 + 联调方法。
- **怎么答**：三个动作——契约、并行、联调排障。强调"改契约走文档、不口头改"显示你懂协作规范。
- **深挖方向**：可能追问"联调时谁让步""时间紧契约没定清怎么办"，答"以业务正确性为准谁错谁改，不是我或 Java 侧谁主导；时间紧先定核心字段，边缘字段标 TODO 但不能没有契约"。

**扣分项：** "口头对一下就开发"；联调靠"改到能跑为止"。

### Q55：电商检索和农业问答两个项目，技术上有延续也有差异，你怎么总结这种演进？

**参考回答：** 延续：核心都是 RAG + Agent + 幻觉防控 + 结构化输出 + SSE 这套 AI 工程能力，农业项目验证了"私有知识优先 + 双工具"的模式，电商项目把它扩展到"多路召回 + MMR + 三层防控 + 跨服务协同"。差异：农业是单服务 + 内存会话，电商是微服务 + 跨语言 + 数据一致性保障，工程复杂度上了一个台阶——多了幂等入库、向量库选型、MQ 异步同步、定时对账这些后端工程能力。演进本质是从"做一个能跑的 AI 应用"到"做一个能稳定运行、和数据一致、和业务系统协同的 AI 服务"。

**关键点解析：**
- **面试官意图**：拔高题，考察你能不能跳出单个项目，讲清两段经历的"能力演进"。这显示你的成长性和自我认知。
- **怎么答**：延续（核心 AI 能力复用）+ 差异（工程复杂度升级）+ 演进本质（从"能跑"到"稳定运行+协同"）。最后那句本质是亮点。
- **深挖方向**：可能追问"下一阶段想往哪演进"，答"往生产化——评测体系、可观测性、成本控制、多模型路由，现在还会调，离真正生产级 AI 服务还差这些"。

**扣分项：** 把两个项目割裂讲；说不出技术演进的主线。

---

## 模块四：RAG / Agent / LLM 技术原理（Q56–Q75）

> 这部分是 AI 应用开发岗的硬核技术原理题，考察你是不是"会用 LangChain"之外还"懂为什么"。
> 覆盖：RAG 全链路、Agent 机制、LCEL/LangGraph、MCP、幻觉本质、SSE 与可观测性。

### Q56：什么是 RAG？它解决了 LLM 的什么问题？

**参考回答：** RAG 是检索增强生成——在模型生成前，先从外部知识库检索相关内容，作为上下文拼进 prompt，再让模型基于检索内容回答。它解决三个问题：① 知识时效——模型参数知识有训练截止日期，RAG 接外部库可以回答最新或私有知识；② 幻觉——让回答有据可依，降低编造；③ 私有知识——企业内部文档模型没见过，只有 RAG 能让它回答。本质是把"参数记忆"换成"外部检索 + 上下文"，用工程手段补模型的认知边界。

**关键点解析：**
- **面试官意图**：RAG 是这个岗位的 ABC 题，但面试官想听你抓住"它解决什么问题"的本质，而不是背定义。
- **怎么答**：定义 + 三个问题（时效/幻觉/私有）+ 本质（参数记忆 → 外部检索）。最后那句本质是区分度。
- **深挖方向**：可能追问"RAG 和微调区别"，答"RAG 是运行时加上下文，不改模型，适合知识频繁变；微调是改模型参数，适合改风格或能力，两者不互斥可结合"。

**扣分项：** "RAG 就是检索 + 生成"；说不出它解决了什么具体问题。

### Q57：RAG 的完整链路有哪些环节？

**参考回答：** 离线建库：文档加载 → 文本切分 → 向量化 → 入向量库（带元数据）。在线查询：用户问题 → 查询改写（可选）→ 问题向量化 → 向量召回 Top-K → （可选）重排序 Rerank → 拼进 prompt → 模型生成 → 结构化输出。每个环节都影响最终效果：切分决定检索粒度、embedding 决定语义表达、召回决定候选、rerank 决定精度、prompt 决定生成质量。

**关键点解析：**
- **面试官意图**：考察 RAG 全链路的掌握，重点看离线和在线两条线是否分清，以及"每个环节都影响效果"的认知。
- **怎么答**：离线建库 + 在线查询两条线，最后点出"每个环节都影响效果"——这显示你有全链路调优意识，而不是只懂其中一段。
- **深挖方向**：可能追问"哪个环节影响最大"，答"召回是基础，召回不到后面全白费；但召回质量够后，rerank 和 prompt 对最终精度提升明显"。

**扣分项：** 只说在线不说离线建库；把环节当孤立步骤不懂相互影响。

### Q58：文档分块有哪些策略？你选哪种？

**参考回答：** 常见策略：固定字符切分、递归字符切分（RecursiveCharacterTextSplitter，优先语义边界）、按标题/段落语义切分、按 token 切分。我选递归字符切分——它在字符上限内优先按段落、句号、逗号切，尽量不切断语义单元，平衡了实现简单和语义完整。更高级的可以按文档结构（Markdown 标题树）切，但需要解析文档结构，成本高。切分后保留 overlap 避免边界信息丢失，并带元数据（来源、章节）便于引用和过滤。

**关键点解析：**
- **面试官意图**：分块是 RAG 效果的关键变量，考察你对策略的了解和选型理由。
- **怎么答**：列策略 + 选递归 + 理由（语义边界优先）+ overlap 和元数据。体现你知道多种策略并有权衡。
- **深挖方向**：可能追问"overlap 设多大""分块大小对召回的影响"，答"overlap 一般块大小的 10–20% 防边界丢失；块太大召回不精准（混主题），太小丢上下文，在测试集上调"。

**扣分项：** 只知道固定字符切分；不知道 overlap 和元数据。

### Q59：embedding 是什么？它怎么把文本变成向量的？

**参考回答：** embedding 是把文本映射成稠密向量，让语义相近的文本在向量空间里距离也近。背后是 embedding 模型（如 BERT 系、bge、text-embedding 系列），它在大规模语料上训练，目标是让"同义/相关文本的向量相似度高、不相关的高"。查询时把用户问题和库里的文档都用同一个模型转向量，算相似度就能找语义相近的。模型不是把文本"编码成数字"那么简单，而是学到了语言的语义表示空间。

**关键点解析：**
- **面试官意图**：embedding 是 RAG 基础，考察你懂它是"语义空间映射"而不只是"文本变数组"。
- **怎么答**：定义（文本 → 稠密向量 + 语义相近则距离近）+ 模型原理（训练目标）+ 查询应用（同模型转向量算相似度）。
- **深挖方向**：可能追问"embedding 模型和 LLM 什么关系"，答"embedding 模型通常比 LLM 小很多，只负责产生向量表示；LLM 是生成模型。RAG 里 embedding 模型做检索、LLM 做生成，各司其职"。

**扣分项：** "把文字转成数字"；不懂语义空间概念。

### Q60：向量检索的相似度算法有哪些？什么场景用哪种？

**参考回答：** 常见：余弦相似度（看方向，对长度不敏感，文本语义常用）、欧氏距离（看绝对距离，对位置敏感）、内积/点积（未归一化时反映模长和方向）。文本语义检索一般用余弦，因为语义相似度关注方向且 embedding 向量长度不固定。向量库的索引算法有暴力（FLAT，准但慢）、HNSW（图索引，查询快建索引慢）、IVF（倒排，折中）。数据小用 FLAT，中等用 HNSW，海量用 IVF+PQ。

**关键点解析：**
- **面试官意图**：向量检索基础，考察相似度度量 + 索引算法两层知识。
- **怎么答**：相似度度量（余弦/欧氏/内积 + 各自特点 + 文本用余弦）+ 索引算法（FLAT/HNSW/IVF + 数据量选型）。
- **深挖方向**：可能追问"HNSW 原理""为什么 HNSW 快"，答"HNSW 是分层小世界图，查询时从顶层稀疏图快速定位到目标区域再往下层精找，类似跳表的思路，用空间换时间"。

**扣分项：** 只知道余弦；分不清相似度度量和索引算法是两个层面。

### Q61：什么是 Agent？它和普通 Chain 有什么区别？

**参考回答：** Agent 是让 LLM 自主决定调用哪些工具、调用顺序、什么时候停的机制。普通 Chain 是预定义的固定流程（检索→生成），每一步都写死了；Agent 是模型根据用户问题动态决策——比如这个问题要不要查知识库、查几次、查完够不够、要不要再调别的工具。核心是 LLM 做决策（通常用 ReAct 或 function calling 模式），工具是执行单元。区别是"固定流程"vs"模型动态路由"。

**关键点解析：**
- **面试官意图**：Agent 是 AI 应用核心概念，考察你抓住"动态决策"这个本质。
- **怎么答**：定义（LLM 自主决策工具调用）+ 和 Chain 对比（固定 vs 动态）+ 核心机制（ReAct/function calling）。
- **深挖方向**：可能追问"Agent 会不会无限循环""怎么让它停"，答"设置最大迭代次数 + 让模型判断'信息够了可以回答'作为终止条件，超次数强制停防止死循环"。

**扣分项：** "Agent 就是带工具的 LLM"；说不出和 Chain 的决策差异。

### Q62：ReAct 模式是什么？

**参考回答：** ReAct 是 Reasoning + Acting 的缩写——模型先推理（Thought，思考该怎么做）再行动（Action，调用某个工具），拿到工具结果（Observation）后再推理下一步，循环直到得出最终答案。本质是把"思考过程"显式化，让模型一步步推理 + 调工具 + 看结果再决策，而不是一次性给答案。优点是可解释、能纠错；缺点是 token 消耗大、慢。现代 function calling 是 ReAct 思想的工程化封装。

**关键点解析：**
- **面试官意图**：ReAct 是 Agent 经典模式，考察你懂"推理 + 行动循环"而不是只会调 function calling。
- **怎么答**：缩写含义 + 循环结构（Thought→Action→Observation）+ 本质（显式化思考）+ 优缺点 + 和 function calling 的关系。
- **深挖方向**：可能追问"function calling 和 ReAct 区别"，答"function calling 是模型原生支持的函数调用能力，结构化、稳定；ReAct 是提示词模式，靠 prompt 引导模型按 Thought-Action-Observation 走，function calling 是它的工程升级版"。

**扣分项：** "就是调工具"；不懂推理和行动的循环。

### Q63：LCEL 的 Runnable 接口是什么？

**参考回答：** LCEL 里所有组件（prompt、model、parser、retriever）都实现 Runnable 接口，提供统一的 invoke（同步）、batch（批量）、stream/astream（流式）方法。因为接口统一，组件可以用 `|` 串成链，链本身也是 Runnable，可以继续组合。这套统一接口让"流式、异步、批量、fallback"这些能力对任意链都开箱即用，不用每个组件单独实现。这是 LCEL 的设计核心——统一抽象带来组合性。

**关键点解析：**
- **面试官意图**：LCEL 深入题，考察你是不是真用过、懂它的设计哲学（统一接口带来组合性）。
- **怎么答**：统一接口（invoke/batch/stream）+ 用 `|` 组合 + 链也是 Runnable + 带来的能力（流式异步开箱即用）。设计哲学那句是亮点。
- **深挖方向**：可能追问"Runnable 怎么实现流式"，答"每个 Runnable 实现自己的 stream 逻辑，链的流式是各组件流式拼接——上游产出一个片段下游就处理一个，不用等全部"。

**扣分项：** "就是个接口"；不懂统一接口带来的组合性。

### Q64：LangGraph 和 LangChain/LCEL 什么关系？为什么要 LangGraph？

**参考回答：** LangChain/LCEL 适合线性的链式流程（A→B→C），但一旦有循环、条件分支、多 Agent 协作、状态机，LCEL 表达起来吃力。LangGraph 把流程建模成"状态 + 节点 + 边"的有向图，节点是一步操作，边定义跳转（含条件边），状态在节点间传递。它能表达循环（Agent 反复调用工具直到满足条件）、并行、人工介入、状态持久化。本质是从"链"升级到"图"，处理更复杂的 Agent 工作流。它是 LangChain 生态的一部分，不是替代而是进阶。

**关键点解析：**
- **面试官意图**：LangGraph 是当前 AI 工程热点，考察你是不是停留在 LCEL，还是跟进到了图编排。
- **怎么答**：LCEL 适合线性、LangGraph 适合图（循环/分支/多 Agent/状态机）+ 核心概念（状态/节点/边）+ 关系（进阶非替代）。
- **深挖方向**：可能追问"农业项目为什么没用 LangGraph"，答"农业项目流程线性（检索→生成），LCEL 够用；电商项目有多轮条件累积，理论上更适合 LangGraph，当时用 LCEL + 会话记忆实现了，是项目复杂度还不到必须上图的程度"。

**扣分项：** "LangGraph 就是新版 LangChain"；不懂它解决的"图"场景。

### Q65：LangGraph 的状态怎么在节点间传递？

**参考回答：** LangGraph 用一个全局 State 对象（通常是 TypedDict 或 Pydantic 模型）在节点间传递。每个节点接收当前 State、执行逻辑、返回一个 State 的更新（部分字段），框架把它 merge 进全局 State，再按边跳到下一节点。循环靠条件边——某个节点根据当前 State 判断下一步去哪（比如"工具调用次数 < 上限且信息不足→回工具节点，否则→结束节点"）。状态持久化靠 checkpointer（和 InMemorySaver 同源），支持断点续跑。

**关键点解析：**
- **面试官意图**：LangGraph 深入，考察你是不是知道状态机制，而不只会说"是图"。
- **怎么答**：全局 State + 节点返回更新 + merge + 条件边做循环 + checkpointer 持久化。把循环机制讲清是关键。
- **深挖方向**：可能追问"状态无限增长怎么办"，答"State 字段设计要克制，只放必要状态；消息历史用窗口或摘要压缩；checkpointer 存快照支持回滚"。

**扣分项：** "节点之间传参"（不懂全局 State）；不懂条件边做循环。

### Q66：MCP 是什么？它解决了什么问题？

**参考回答：** MCP（Model Context Protocol，模型上下文协议）是 Anthropic 推出的开放协议，标准化 LLM 应用和外部工具/数据源之间的连接。之前每个 AI 应用接外部工具（数据库、API、文件系统）都要各自写集成，工具和 应用紧耦合、不可复用。MCP 定义了统一的 Server/Client 协议——MCP Server 暴露工具/资源，任何支持 MCP 的 Client（如 Claude Desktop、IDE）都能连，实现"写一次工具，多应用复用"。本质是把工具集成从"应用各自造轮子"标准化成"协议互通"。

**关键点解析：**
- **面试官意图**：MCP 是简历明确"持续关注"的方向，考察你是不是真懂它解决的"工具集成标准化"问题。
- **怎么答**：定义（标准化协议）+ 解决的问题（工具集成各自造轮子、紧耦合）+ 架构（Server/Client，写一次多复用）+ 本质（标准化互通）。
- **深挖方向**：可能追问"MCP 和 function calling 区别"，答"function calling 是模型调用函数的能力，是模型层；MCP 是工具暴露和连接的协议，是集成层，MCP Server 暴露的工具最终可能通过 function calling 被模型调用，两者不同层面"。

**扣分项：** "就是个协议"说不清解决什么；和 function calling 混为一谈。

### Q67：LLM 为什么会产生幻觉？能完全消除吗？

**参考回答：** 幻觉根因是 LLM 的本质——它是基于概率预测下一个 token 的，不是检索事实，所以会生成"听起来合理但不符合事实"的内容。训练数据有错、知识过时、模型把相关概念混淆都会加剧。完全消除不可能，因为生成机制本身是概率性的。工程目标是降低频率 + 让回答可追溯：RAG 给依据、prompt 约束不编造、输出校验兜底、引用溯源让用户能验证。本质是把"不可控的生成"用工程手段收敛到"可控、可验证"。

**关键点解析：**
- **面试官意图**：幻觉是 AI 工程核心痛点，考察你是不是只会"用 RAG 治幻觉"，还是懂它的概率本质 + 不可完全消除。
- **怎么答**：根因（概率生成非检索事实）+ 完全不可能 + 工程目标（降低 + 可追溯，多层防控）+ 本质（不可控→可控可验证）。
- **深挖方向**：可能追问"幻觉有好处吗"，答"有一定——创意写作、头脑风暴时'不严格符合事实'反而是能力，问题是事实型任务里要抑制它，所以幻觉要分场景看"。

**扣分项：** "RAG 能消除幻觉"；不懂概率生成根因。

### Q68：Prompt Engineering 的核心原则有哪些？

**参考回答：** 几个核心原则：① 角色和任务明确——让模型知道它是谁、要干什么；② 提供上下文和依据——给检索内容或背景，约束回答来源；③ 结构化输出要求——明确字段和格式，配合结构化解析；④ 示例（few-shot）——给几个输入输出样例引导格式和风格；⑤ 边界约束——明确"不知道就说不知道""不得编造"等负面指令；⑥ 可测试和版本化——prompt 要能评估效果、按版本管理。本质是减少模型自由度、让输出可预期。

**关键点解析：**
- **面试官意图**：Prompt 是 AI 应用日常，考察你是不是只会写一句"请回答"，还是有体系。
- **怎么答**：六个原则 + 本质（减少自由度、可预期）。few-shot 和"可测试版本化"是亮点，显示工程意识。
- **深挖方向**：可能追问"prompt 越长越好吗"，答"不是——太长模型会忽略部分指令（lost in the middle），且增加 token 成本；要精炼、关键约束前置、用结构分隔"。

**扣分项：** "随便写写调到能跑"；不知道 few-shot 和版本化。

### Q69：few-shot 和 zero-shot 的区别？什么时候用 few-shot？

**参考回答：** zero-shot 是不给样例直接让模型按指令做；few-shot 是在 prompt 里给几个输入输出样例，引导模型按样例的格式、风格、逻辑回答。用 few-shot 的场景：① 输出格式复杂，靠指令说不清，样例更直观；② 需要特定风格或语气；③ 任务逻辑需要演示（比如分类边界模糊）。zero-shot 适合简单明确任务，省 token。代价是 few-shot 占 token、样例选不好会误导。

**关键点解析：**
- **面试官意图**：Prompt 基础概念，考察你懂两者的适用场景。
- **怎么答**：定义 + few-shot 适用场景（格式/风格/演示逻辑）+ zero-shot 适用（简单省 token）+ 代价。
- **深挖方向**：可能追问"few-shot 给几个样例合适"，答"一般 2–5 个，多了 token 贵且模型可能过拟合样例的表面特征；样例要覆盖典型情况、多样性，不能都像"。

**扣分项：** 两者分不清；"few-shot 更好所以都用"。

### Q70：什么是 function calling / tool use？

**参考回答：** function calling 是 LLM 原生支持的能力——你把可用工具的定义（名字、参数 schema、描述）告诉模型，模型判断需要用某工具时，输出一个结构化的"工具调用请求"（工具名 + 参数），应用执行后把结果回传给模型继续。这是 Agent 调用工具的工程基础，比纯 prompt 引导（ReAct）更结构化、稳定。tool use 是更广的概念，function calling 是它的主流实现方式。本质是把"模型决策要调工具"标准化成结构化输出，而不是从自然语言里解析。

**关键点解析：**
- **面试官意图**：function calling 是现代 Agent 的基础，考察你懂它的机制——结构化输出工具调用请求。
- **怎么答**：机制（工具定义→模型输出结构化调用→执行→回传）+ 比 ReAct 优势（结构化稳定）+ 和 tool use 的关系。
- **深挖方向**：可能追问"模型乱调工具怎么办"，答"工具描述要清晰、限定调用场景；应用层可以加校验和审批，敏感工具调用人确认；设最大调用次数"。

**扣分项：** "就是调函数"；不懂结构化输出的机制。

### Q71：Agent 调用工具失败怎么处理？

**参考回答：** 几层处理：① 工具自身做异常捕获，返回结构化的错误信息给模型（"查询失败，原因 XX"），让模型决定重试还是换工具；② 有限重试——网络等临时错误重试有限次，参数错误不重试直接报错；③ 降级——主工具失败切备选工具或返回兜底结果；④ 终止保护——设最大工具调用次数，超过强制结束防止死循环；⑤ 可观测——记录每次工具调用、参数、结果、耗时，便于排查。核心是不让单次工具失败把整个 Agent 拖崩，要能恢复或优雅降级。

**关键点解析：**
- **面试官意图**：Agent 容错是工程重点，考察你是不是只会"调通了就行"，还是有异常恢复设计。
- **怎么答**：五层处理——错误回传、有限重试、降级、终止保护、可观测。结构化错误回传给模型这点很关键，让模型参与决策。
- **深挖方向**：可能追问"工具重试会不会重复执行副作用操作"，答"工具要做幂等或区分可重试/不可重试错误——查询类可重试，写入类要幂等才重试，否则不重试走补偿"。

**扣分项：** "失败了就报错"；无重试和降级设计。

### Q72：LLM 应用的可观测性怎么做？要记录什么？

**参考回答：** 要记录全链路关键信息：① 输入——用户 query、会话 ID；② 检索——召回的片段、相关度分数、是否被采用；③ 模型调用——模型名、prompt 版本、token 用量、耗时、错误码；④ 工具调用——工具名、参数、结果、耗时；⑤ 输出——生成内容、结构化字段。配合 traceId 串联一次请求的所有环节。目的：排查效果问题（为什么召回不准）、性能问题（哪步慢）、成本问题（token 花在哪）、幻觉追溯（回答依据）。工具上可以用 LangSmith、OpenTelemetry 或自建日志。

**关键点解析：**
- **面试官意图**：可观测性是生产级 AI 应用的标志，考察你是不是停留在"能跑"，还是有全链路追踪意识。
- **怎么答**：五个记录维度 + traceId 串联 + 目的（效果/性能/成本/幻觉追溯）+ 工具选型。
- **深挖方向**：可能追问"traceId 怎么在流式下传递"，答"请求开始生成 traceId，写入每个事件的日志和事件元数据，流式响应里也带上，前端和后端日志能对齐"。

**扣分项：** "记个日志就行"；不知道要记录检索和 token 这些维度。

### Q73：LLM 调用的成本怎么控制？

**参考回答：** 几个手段：① 模型分级——简单任务用小模型，复杂任务才用大模型，按任务难度路由；② prompt 精简——去掉冗余指令、控制上下文长度，避免塞无关检索内容；③ 缓存——相同或相似 query 的结果缓存，命中直接返回不调模型；④ token 监控——记录每次调用的 token 用量，超阈值告警；⑤ 流式 + 早期终止——能在生成中途判断不需要继续就停。核心是"按需用模型、按需给上下文、能缓存就缓存"。

**关键点解析：**
- **面试官意图**：成本是 AI 应用生产化的硬约束，考察你有没有成本意识。
- **怎么答**：五个手段 + 核心原则（按需用模型、按需给上下文、能缓存就缓存）。模型分级和缓存是性价比最高的两点。
- **深挖方向**：可能追问"缓存相似 query 怎么做"，答"把 query 向量化，查向量缓存找最相近的命中，而不是只精确匹配；缓存设 TTL 防过期"。

**扣分项：** "用便宜模型就行"；不知道缓存和上下文控制。

### Q74：SSE 和 WebSocket 有什么区别？AI 场景为什么多用 SSE？

**参考回答：** SSE 是 HTTP 基础的服务端单向推送（服务端→客户端），用普通 HTTP 连接、文本协议、自动重连；WebSocket 是双向全双工协议，需要握手升级、二进制支持、更复杂。AI 场景多用 SSE 是因为：① 流式输出本质是服务端推、客户端收，单向就够，不需要 WebSocket 的双向能力；② SSE 基于 HTTP，穿透防火墙、负载均衡、鉴权都简单，WebSocket 的升级握手在某些中间件有坑；③ 实现简单，浏览器原生 EventSource 几行代码。WebSocket 更适合实时双向交互（聊天室、游戏），AI 流式用 SSE 性价比更高。

**关键点解析：**
- **面试官意图**：SSE vs WebSocket 是 AI 应用高频，考察你懂为什么选 SSE 而不是无脑上 WebSocket。
- **怎么答**：协议差异（单向 vs 双向、HTTP vs 升级、复杂度）+ AI 场景适配（单向够用、HTTP 简单、实现容易）+ WebSocket 适合什么。
- **深挖方向**：可能追问"SSE 单连接数限制怎么办"，答"浏览器对同域 SSE 连接数有限制（HTTP/1.1 是 6 个），可以用 HTTP/2 多路复用或分域；但单用户 AI 流式很少同时开多个，一般不是瓶颈"。

**扣分项：** "SSE 简单所以用"；说不出和 WebSocket 的协议差异。

### Q75：流式输出时，前端怎么知道生成结束了？

**参考回答：** 后端在生成完成时发送一个明确的完成事件（比如 `event: done, data: [DONE]` 或一个带结束标志的结构化事件），前端收到这个事件就知道这次生成结束、可以关闭连接或做后处理。不能靠"连接关闭"判断结束，因为异常关闭和正常结束都要区分——正常结束发 done 事件，异常发 error 事件，前端按事件类型走不同逻辑。这是 SSE 契约的一部分，前后端要约定好完成事件和错误事件的格式。

**关键点解析：**
- **面试官意图**：流式细节，考察你懂"完成要靠约定事件，不能靠连接状态"。这是很多人忽略的契约点。
- **怎么答**：完成事件约定 + 不能靠连接关闭判断 + 正常/异常事件区分。契约意识是关键。
- **深挖方向**：可能追问"连接中途断了算结束吗"，答"不算——断了可能是网络问题，前端应触发重连或补偿接口拉完整结果，而不是当结束处理；只有收到 done 事件才是正常结束"。

**扣分项：** "连接关了就结束了"；没有完成事件契约。

---

## 模块五：Java 基础与微服务协同（Q76–Q90）

> 你定位是 AI + Java 双栈，Java 后端基础和微服务协同能力必考。
> 覆盖：SpringCloud Alibaba、Nacos、OpenFeign、FastAPI 协同、Redis、消息队列、MySQL 等。

### Q76：SpringCloud Alibaba 你用过哪些组件？各自解决什么问题？

**参考回答：** Nacos 做服务注册发现和配置中心，解决服务间怎么找到对方 + 配置动态下发；OpenFeign 做声明式 HTTP 调用，解决跨服务调用的样板代码；Sentinel 做限流熔断，解决下游服务故障不级联；Seata 做分布式事务，解决跨服务数据一致性；RocketMQ 做消息异步，解决解耦削峰。我用得深的是 Nacos 和 OpenFeign——电商项目里 Java 微服务通过 Nacos 注册、通过 OpenFeign 调 Python 的 FastAPI。Sentinel、Seata 停留在了解原理层面。

**关键点解析：**
- **面试官意图**：考察组件广度 + 诚实度。重点看你能不能说清每个组件解决什么问题，以及哪些是真用过、哪些是了解。
- **怎么答**：每个组件一句话说职责 + 解决什么问题 + 自己的掌握深度边界。主动说"Sentinel/Seata 停留在了解"显诚实，不吹牛。
- **深挖方向**：可能追问"Nacos 怎么实现服务发现"，答"服务启动注册到 Nacos，消费方从 Nacos 拉服务列表并订阅变更，调用时从列表选实例负载均衡"。

**扣分项：** 把所有组件都说成"用过且精通"；说不出组件解决的具体问题。

### Q77：Nacos 既是注册中心又是配置中心——这两个职责怎么实现的？

**参考回答：** 注册中心：服务启动时把自己的地址注册到 Nacos，Nacos 维护服务名→实例列表的映射；消费方启动时拉取列表并长轮询/订阅变更，实例上下线时 Nacos 推送更新。配置中心：配置存 Nacos，客户端启动时拉取配置，并长轮询监听配置变更，配置改了 Nacos 推送，客户端热更新。两者底层都基于 Nacos 的数据存储 + 推送机制，但注册数据生命周期跟随服务，配置数据独立管理。本质都是"集中存储 + 变更推送 + 客户端监听"。

**关键点解析：**
- **面试官意图**：Nacos 是 SpringCloud Alibaba 核心，考察你是不是真懂两个职责的机制，而不是"它就是注册中心和配置中心"。
- **怎么答**：分别讲注册（注册+拉取+订阅推送）和配置（拉取+监听+热更新），再点出底层共通（集中存储+推送+监听）。
- **深挖方向**：可能追问"Nacos 怎么保证配置变更及时推送"，答"长轮询——客户端发起请求，Nacos 有变更立即返回，没变更 hold 住一段时间，平衡实时性和压力；不是纯推也不是纯拉"。

**扣分项：** "就是存数据"；说不出注册和配置的机制差异。

### Q78：Nacos 和 Eureka 区别？为什么项目选 Nacos？

**参考回答：** 几点区别：① 架构——Eureka 是 AP（高可用优先，数据最终一致），Nacos 默认 AP 也可切 CP（支持强一致）；② 功能——Eureka 只做注册，Nacos 注册+配置一体；③ 推送——Eureka 客户端定时拉取，Nacos 支持长轮询推送变更更及时；④ 生态——Nacos 是 Alibaba 系，和 Sentinel、Seata 等 Alibaba 组件集成更好。选 Nacos 主要因为注册+配置一体化减少组件数，且和 Alibaba 生态契合。Eureka 2.x 开源计划变动后社区用 Nacos 多。

**关键点解析：**
- **面试官意图**：注册中心选型是微服务高频，考察你是不是"项目用啥就用啥"，还是有对比思考。
- **怎么答**：架构（AP/CP）、功能（注册+配置）、推送机制、生态四个对比 + 选型理由。
- **深挖方向**：可能追问"AP 和 CP 怎么选"，答"注册中心一般选 AP——服务发现容忍短暂不一致但不能因为强一致导致注册不可用；Nacos 服务发现用 AP，配置用 CP 更稳"。

**扣分项：** "项目就用 Nacos"；说不出和 Eureka 的机制差异。

### Q79：OpenFeign 的原理是什么？它是怎么把接口调用变成 HTTP 请求的？

**参考回答：** OpenFeign 是声明式 HTTP 客户端。原理：你定义一个 Java 接口加 @FeignClient 注解，Feign 在运行时用动态代理生成这个接口的实例；调用接口方法时，代理拦截调用，根据方法上的注解（@RequestMapping 等）和参数，构造 HTTP 请求（URL、方法、headers、body），通过底层的 HTTP 客户端（默认用 JDK 或集成 OkHttp/Apache HttpClient）发送，响应反序列化成方法返回类型。配合负载均衡器（如 Spring Cloud LoadBalancer）做实例选择，配合 Sentinel 做熔断降级。

**关键点解析：**
- **面试官意图**：OpenFeign 是你电商项目的核心调用方式，必考原理。重点看动态代理 + 注解驱动的机制。
- **怎么答**：声明式接口 + 动态代理生成实例 + 代理拦截构造 HTTP 请求 + 底层客户端发送 + 集成负载均衡和熔断。
- **深挖方向**：可能追问"Feign 超时怎么配""为什么 Feign 调用有时第一次失败"，答"超时分 connect 和 read，在配置类或 yaml 配；第一次失败可能是连接建立或服务实例刚启动未就绪，配重试或预热"。

**扣分项：** "就是发 HTTP 请求"；不懂动态代理机制。

### Q80：OpenFeign 调用失败怎么降级？

**参考回答：** 两种方式：① fallback 类——实现 Feign 接口，每个方法写降级返回逻辑，在 @FeignClient 里指定 fallback 类，调用失败时框架走 fallback 返回兜底结果；② fallbackFactory——能拿到异常原因，按不同异常做不同降级，比 fallback 类灵活。降级触发条件由熔断器（Sentinel 或 Resilience4j）控制——超时、异常、熔断打开时走降级。降级返回要设计成业务可接受的兜底——比如返回空列表、默认值、提示信息，而不是抛错把调用方拖崩。

**关键点解析：**
- **面试官意图**：降级是微服务容错核心，考察你是不是只会"调不通就报错"，还是有降级设计。
- **怎么答**：两种方式（fallback 类 / fallbackFactory）+ 触发条件（熔断器控制）+ 降级返回设计（业务可接受兜底）。fallbackFactory 拿异常原因这点更显深度。
- **深挖方向**：可能追问"降级返回空列表，调用方怎么区分'真没数据'和'降级兜底'"，答"用特定的降级标志或响应码区分，调用方按场景处理——展示场景可静默兜底，关键决策场景要明确告知降级了"。

**扣分项：** "失败就抛异常"；不知道熔断器触发降级。

### Q81：FastAPI 为什么适合做 AI 服务？

**参考回答：** 几个原因：① 异步原生——基于 ASGI，async/await 不阻塞事件循环，AI 的 IO 密集（调模型、查向量库、HTTP 调用）在异步下吞吐高；② 流式友好——StreamingResponse 和 async generator 天然支持 SSE/流式输出，契合 LLM 流式生成；③ 类型校验——Pydantic 做请求/响应校验和序列化，接口契约清晰；④ 自动文档——OpenAPI schema 自动生成，便于和 Java 侧对契约；⑤ Python 生态——AI 库都在 Python，FastAPI 是 Python 栈里性能好的 Web 框架。本质是异步 + 流式 + 契约 + AI 生态契合。

**关键点解析：**
- **面试官意图**：考察你对 FastAPI 选型理由的理解，而不是"因为用 Python"。
- **怎么答**：五个点——异步、流式、Pydantic 校验、自动文档、AI 生态。前三个直接对应 AI 服务需求。
- **深挖方向**：可能追问"FastAPI 异步和同步混用会怎样"，答"在 async 路由里调同步阻塞函数（比如某些同步 SDK）会阻塞事件循环，要用 run_in_executor 丢到线程池，否则拖垮吞吐"。

**扣分项：** "因为快"；说不出和 AI 服务需求的契合点。

### Q82：FastAPI 和 Spring Boot 怎么协同？跨语言调用的坑有哪些？

**参考回答：** 协同靠 HTTP REST——FastAPI 暴露接口，Spring Boot 用 OpenFeign 调用。坑有：① 序列化——Java 对象和 JSON 的命名风格（驼峰 vs 蛇形）、空值（null vs 缺字段）、日期格式不一致，要约定命名策略和空值处理；② 超时——Python AI 服务延迟长，Feign 默认超时太短，要单独配长超时；③ 异常——HTTP 错误码和业务错误的映射，Python 抛的异常 Java 侧要能识别并降级；④ 契约维护——两边字段变更要同步，靠 OpenAPI 文档统一；⑤ 环境和依赖——Python 的 Conda/venv 和 Java 的 Maven 体系不同，部署要各自容器化。

**关键点解析：**
- **面试官意图**：跨语言协同是你简历"双栈"的核心，考察你是不是真踩过坑。
- **怎么答**：五个坑——序列化、超时、异常映射、契约维护、环境。序列化和超时是高频实际坑。
- **深挖方向**：可能追问"命名风格怎么统一"，答"Jackson 配 PropertyNamingStrategy 转 snake_case 对齐 Python，或 FastAPI 用 alias 统一驼峰；契约里约定死一种，别两边各自风格"。

**扣分项：** "就是 HTTP 调用没坑"；没考虑序列化和超时。

### Q83：Redis 在你的两个项目里分别做什么用？

**参考回答：** 农业项目：Redis 用得少，会话状态主要在 InMemorySaver（内存），Redis 可能做轻量缓存。电商项目：RedisStack 做向量检索（这是主要用途），同时 Redis 可做商品热点数据缓存、接口结果缓存。两个项目对 Redis 的用法不同——农业是会话/缓存，电商是向量库 + 缓存。共同点是都把 Redis 当"低延迟共享存储"，但向量检索是 RedisStack 扩展能力，普通 Redis 没有。

**关键点解析：**
- **面试官意图**：考察你 Redis 用法的场景化理解，而不是背 Redis 数据结构。
- **怎么答**：分项目说用途 + 共同定位（低延迟共享存储）+ RedisStack 和普通 Redis 的区别。
- **深挖方向**：可能追问"Redis 做向量库和专用向量库比什么劣势"，答"数据量上限、索引算法选择少、大规模运维不如 Milvus 专业；优势是复用 Redis 基础设施、小数据量够用、支持混合结构化过滤"。

**扣分项：** 把 RedisStack 和普通 Redis 混为一谈；"Redis 就是缓存"。

### Q84：Redis 持久化了解吗？你的项目怎么处理 Redis 数据丢失？

**参考回答：** Redis 持久化有 RDB（快照，全量周期性落盘，恢复快但可能丢最近数据）和 AOF（追加日志，更完整但文件大、恢复慢），生产常两者结合。我的项目里：农业项目会话用 InMemorySaver 本就是内存方案，丢就丢、重启清空，没强依赖 Redis 持久化；电商项目向量数据在 MySQL 有源头（SKU），向量库丢了可以从 MySQL 重建，所以 Redis 是"可重建的缓存/索引"而非"唯一事实源"。原则是：业务事实存 MySQL/数据库，Redis 存可重建的数据，丢了能恢复。

**关键点解析：**
- **面试官意图**：Redis 持久化是后端高频，考察你是不是"Redis 丢了就丢了"，还是有数据来源设计。
- **怎么答**：RDB/AOF 机制 + 两个项目的处理 + 核心原则（事实存 DB，Redis 存可重建数据）。原则那句是关键认知。
- **深挖方向**：可能追问"如果向量库必须不能丢呢"，答"那 Redis 配 AOF + 持久化，但更合理的是用专用向量库（Milvus）做持久化存储，Redis 只做热缓存层"。

**扣分项：** "Redis 数据丢了就丢了"；不知道 RDB/AOF；把 Redis 当唯一事实源。

### Q85：RocketMQ 你用在异步同步，它的核心概念有哪些？

**参考回答：** 核心概念：Producer（生产者）、Consumer（消费者）、Topic（主题，消息分类）、Message Queue（队列，Topic 分区，并行消费单元）、Broker（消息存储和转发）、NameServer（注册中心，Broker 路由）、Group（生产者/消费者分组）。消息模型：支持集群消费（一条消息一个消费者组里只有一个实例消费）和广播消费（所有实例都消费）。我用在电商是 Producer 发商品变更消息、Consumer 消费更新向量库，Topic 是商品变更，集群消费保证多实例不重复处理。

**关键点解析：**
- **面试官意图**：RocketMQ 是你项目用的 MQ，考察你是不是只会"发消息收消息"，还懂内部概念。
- **怎么答**：核心组件（Producer/Consumer/Topic/Queue/Broker/NameServer/Group）+ 消费模型（集群/广播）+ 项目应用。
- **深挖方向**：可能追问"NameServer 和 Zookeeper 区别""为什么 RocketMQ 不用 ZK"，答"NameServer 是无状态轻量注册中心，互不通信，AP 优先；ZK 强一致但重，RocketMQ 选 NameServer 简化部署"。

**扣分项：** "就是发消息收消息"；说不出 Topic/Queue/Group 概念。

### Q86：RocketMQ 怎么保证消息不丢？你的项目怎么处理重复消费？

**参考回答：** 不丢靠几层：Producer 端同步发送 + 重试，发送失败重试；Broker 端消息持久化到磁盘（CommitLog）+ 主从同步复制；Consumer 端消费成功才返回 ACK，失败重试，超次数进死信队列。重复消费处理靠消费端幂等——我用 MD5 指纹：同一条商品变更消息重复消费时，MD5 相同判断内容没变，跳过更新，天然防重。本质是 MQ 保证 at-least-once（至少一次），可能重复，业务幂等兜底，不指望 MQ 保证 exactly-once。

**关键点解析：**
- **面试官意图**：MQ 可靠性是后端必考，考察你是不是只会"发完不管"，还是有不丢 + 幂等的完整设计。
- **怎么答**：不丢三层（Producer 重试、Broker 持久化+主从、Consumer ACK+死信）+ 重复靠业务幂等（MD5 指纹）+ 本质（at-least-once + 业务幂等）。最后那句本质是亮点。
- **深挖方向**：可能追问"exactly-once 能做到吗"，答"严格 exactly-once 成本高，RocketMQ 事务消息能接近，但多数场景用 at-least-once + 业务幂等更实际"。

**扣分项：** "发完就不管了"；不知道要业务幂等防重复。

### Q87：Elastic-Job 怎么做定时任务？为什么选它而不是 Spring 的 @Scheduled？

**参考回答：** Elastic-Job 是分布式定时任务框架。和 @Scheduled 比：① 分布式——@Scheduled 是单机定时，多实例会重复执行，Elastic-Job 在多实例间分片执行，不重复；② 高可用——一个实例宕机其他实例接管；③ 动态分片——任务按分片分配给不同实例并行执行；④ 运维——支持任务配置、监控、失败重试。我用它做向量库全量校对——多实例分片跑、某实例挂了其他接管、定时低峰跑不抢资源。@Scheduled 适合单机简单定时，分布式场景要 Elastic-Job 或 XXL-JOB。

**关键点解析：**
- **面试官意图**：定时任务选型是后端常考，考察你是不是懂 @Scheduled 的局限和分布式方案。
- **怎么答**：Elastic-Job 四个优势（分布式不重复、高可用、分片、运维）+ 和 @Scheduled 对比 + 项目应用。
- **深挖方向**：可能追问"分片怎么分"，答"配置分片数，每个实例拿到分片号，任务里按分片号处理对应数据范围（比如按 ID 取模），实例数变时分片重新分配"。

**扣分项：** "用 @Scheduled 就行"；不知道 @Scheduled 多实例重复问题。

### Q88：MySQL 索引原理？你在项目里怎么用索引优化？

**参考回答：** MySQL InnoDB 索引是 B+ 树——数据有序存储，范围查询快，非主键索引存主键再回表。我用索引优化：电商项目 SKU 表，按商品名查询加索引、按品类+价格筛选建联合索引（最左前缀原则，把过滤性强的字段放前面）；避免 SELECT * 只查需要的列减少回表；深分页用游标（基于 ID where）代替 offset。索引不是越多越好——写多索引会拖慢写入、占空间，要按查询模式建。

**关键点解析：**
- **面试官意图**：MySQL 索引是 Java 后端必考，考察你是不是只会"加个索引"，还懂原理和优化。
- **怎么答**：B+ 树原理 + 项目优化（联合索引 + 最左前缀 + 避免回表 + 游标分页）+ 索引代价认知。
- **深挖方向**：可能追问"最左前缀怎么回事""索引失效场景"，答"联合索引 (a,b,c) 能用 a、a,b、a,b,c，不能跳过 a 直接用 b；失效：函数操作列、隐式类型转换、like 左模糊、or 条件等"。

**扣分项：** "加个索引就快"；不知道联合索引最左前缀；索引乱建。

### Q89：SQLAlchemy 怎么用？和 MyBatis 对比你的感受？

**参考回答：** SQLAlchemy 是 Python 的 ORM，我用来读 MySQL SKU。用法：定义模型类映射表，用 session 查询，支持原生 SQL 和 ORM 两种。和 MyBatis 比：SQLAlchemy 是全自动 ORM，对象操作多、写 SQL 少，开发快但复杂 SQL 控制力弱；MyBatis 是半自动，SQL 自己写、映射自动化，复杂 SQL 和性能调优更灵活。我的感受是：AI 侧读数据用 SQLAlchemy 够快够简单；复杂业务 SQL 和性能优化还是 MyBatis 更可控。两者不是好坏，是场景选择。

**关键点解析：**
- **面试官意图**：ORM 对比考察你的双栈视野——是不是能在 Java 和 Python 的 ORM 间对比。
- **怎么答**：SQLAlchemy 用法 + 和 MyBatis 对比（全自动 vs 半自动、开发快 vs SQL 控制）+ 场景选择感受。
- **深挖方向**：可能追问"SQLAlchemy 的 N+1 问题"，答"查列表后循环查关联对象会产生 N+1 次查询，用 eager loading（joinedload）一次性关联查询解决"。

**扣分项：** 拉踩某一个；说不出全自动和半自动的差异。

### Q90：你作为 AI 模块负责人，Java 侧和 Python 侧代码规范怎么统一？

**参考回答：** 代码规范不强求完全统一——Java 有 Java 的规范（阿里巴巴手册那套），Python 有 PEP 8，硬统一不现实。统一的是"协作层面"：① 接口契约——REST 接口的字段命名、错误码、分页规则两边统一（哪怕语言内风格不同，跨语言接口契约要一致）；② 注释和文档——接口文档、关键逻辑注释两边都写清楚；③ Git 工作流——分支管理、Code Review 流程两边一致；④ 错误处理约定——HTTP 状态码和业务错误码的映射统一。语言内的代码风格各自遵循各自规范，协作契约统一即可。

**关键点解析：**
- **面试官意图**：考察跨语言协作的工程规范意识，重点看你知道"统一什么、不统一什么"。
- **怎么答**：不强求语言内风格统一 + 统一协作层面（契约、文档、Git、错误码）+ 语言内各自规范。
- **深挖方向**：可能追问"Code Review 跨语言怎么做"，答"AI 侧我 review Python，Java 侧 Java 同学 review，但接口契约和跨语言交互的代码两边都看，关键联调点交叉 review"。

**扣分项：** "都要统一成一套"；完全不统一各搞各的。

---

## 模块六：工程实践与软素质（Q91–Q100）

> 覆盖你简历的"工程实践"部分——技术分享、压测排障、工程复盘、持续学习，以及应届生软素质高频题。

### Q91：20 余次并发压测，压出了哪 7 类问题？具体怎么定位和修复的？

**参考回答：** 7 类问题：① 向量召回超时——并发高时单次检索慢，定位到是向量库连接池和线程阻塞，修复是连接池复用 + 查询超时设置；② 工具调用失效——并发下工具偶发返回空，定位到共享状态被并发污染，修复是工具调用隔离；③ 会话状态异常——多用户并发时状态串号，定位到 thread_id 未严格隔离，修复是会话隔离；④ SSE 偶发断流——长连接被中间件或超时断开，修复是心跳保活 + 断线重连；⑤ 重复消息消费——MQ 重复投递，靠幂等防重；⑥ 缓存失效瞬间穿透——缓存过期瞬间高并发打到数据库，修复是缓存预热 + 互斥锁；⑦ 模型输出格式异常——并发下偶发不按 schema 输出，修复是解析失败重试 + 兜底。每类都从日志和压测数据定位根因，修复后补测试防回归。

**关键点解析：**
- **面试官意图**：这是简历最大的数据亮点，必考。面试官在验证：① 你是不是真压过；② 定位问题的方法论；③ 修复是否到根因。能说出 7 类问题各带根因和修复，是强验证。
- **怎么答**：7 类问题每类一句话"现象 + 根因 + 修复"。重点是"定位根因"——面试官想听你怎么从现象追到代码，而不是"压测发现慢我就优化了"。
- **深挖方向**：可能任选一类追问细节（"SSE 断流怎么排查的"），准备好每类的排查链路：日志 → 复现 → 定位 → 修复 → 回归。

**扣分项：** 只说"压出很多问题都修了"；说不出具体根因；7 类背不住但可以归纳核心几类（至少能说 4–5 类）。

### Q92：向量召回超时——怎么定位和优化？

**参考回答：** 定位：压测时观察单次检索耗时分布，发现 P99 明显恶化；加日志看慢在哪——发现是向量查询在并发下等待连接（连接池耗尽）、以及 embedding 调用串行等待。优化：① 连接池复用和容量调整，避免每个请求新建连接；② embedding 调用异步化或并发限制，避免串行阻塞；③ 给向量查询设超时，超时快速失败不拖垮整体；④ 结果缓存——热点 query 的召回结果缓存，命中不重复算。优化后对比 P99 和吞吐，确认改善而非凭感觉。

**关键点解析：**
- **面试官意图**：任选一类故障深挖，考察你是不是真做了性能定位。重点看"定位靠数据不靠猜 + 优化有对比验证"。
- **怎么答**：定位（耗时分布 + 日志找慢点）+ 优化（连接池、异步、超时、缓存）+ 验证（P99 和吞吐对比）。
- **深挖方向**：可能追问"缓存召回结果会不会导致商品下架后仍返回旧数据"，答"会——所以缓存要设 TTL 且商品变更时主动失效该商品的缓存，和业务状态联动"。

**扣分项：** "把超时调大"；"加机器"；没做优化前后对比。

### Q93：压测的工具和方法论是什么？

**参考回答：** 工具：用 JMeter 或 Locust 做并发压测，设计不同并发梯度（50/100/200…）逐步加压。方法论：① 明确目标——测吞吐、延迟还是正确性，先定指标；② 准备数据——可重复的商品和查询数据，避免数据依赖干扰；③ 隔离环境——压测环境和生产数据隔离，不污染真实数据；④ 逐步加压——观察 QPS、P50/P95/P99、错误率、连接池、线程池、GC 随并发的变化曲线；⑤ 关注压测后的状态——资源是否回收、数据是否一致，不只是看峰值；⑥ 复现和定位——压测暴露问题后，用日志复现、定位根因、修复后回归对比。

**关键点解析：**
- **面试官意图**：压测方法论是"结果导向"的验证，考察你是不是专业的（有目标、有环境、有指标、有回归）。
- **怎么答**：工具 + 六步方法论。重点是"逐步加压 + 关注分位延迟 + 压测后状态检查"——很多人只报峰值 QPS。
- **深挖方向**：可能追问"P95 和 P99 为什么重要"，答"P95/P99 反映长尾延迟——用户体验由最差的那批请求决定，平均延迟会被少数快请求拉低，掩盖慢请求"。

**扣分项：** "就是开个压测工具看 QPS"；不关注错误率和分位延迟。

### Q94：工具调用失效怎么排查的？

**参考回答：** 现象：并发压测下工具偶发返回空或异常。排查链路：① 先看日志——工具调用失败的报错是什么类型（超时、参数错误、还是异常），用 traceId 关联整次调用；② 复现——用压测脚本单复现或降低并发复现，确认是偶发还是必现；③ 定位——发现是工具内部共享状态（如会话数据被多线程并发读写）被污染，或外部服务偶发超时；④ 修复——共享状态按会话隔离、工具调用加超时和重试；⑤ 回归——修复后压测验证不再复现，并加日志监控工具调用成功率。

**关键点解析：**
- **面试官意图**：深入一类故障的排查，考察方法论（日志→复现→定位→修复→回归）而不是碰运气修。
- **怎么答**：五步排查链路，每步一个动作。重点是"共享状态污染"这个常见根因和"按会话隔离"的修复。
- **深挖方向**：可能追问"怎么发现是共享状态污染"，答"看报错是偶发的、和并发度正相关、单线程跑正常——这种特征基本指向共享可变状态，检查工具里有没有类级/全局变量被并发写"。

**扣分项：** "重试几次就好了"；"加日志看报错"（定位不到根因）。

### Q95：你组织过 3 场技术分享——怎么准备的？技术分享给你带来什么？

**参考回答：** 3 场是 MySQL 向量同步、LangChain 智能体、RAG 幻觉优化。准备方法：① 先讲清楚"要解决什么问题"再讲方案，听众才能跟得上；② 内容来自实战——都是我在项目里踩过的坑和验证过的方案，不是抄文档；③ 结构上按"背景 → 方案 → 坑和对比 → 效果"组织，控制时长；④ 讲完沉淀成文档，别人能复用。技术分享给我带来：① 逼我把半懂的东西讲明白——讲不清楚说明没真懂；② 团队知识的沉淀，减少重复踩坑；③ 跨角色沟通能力——能把 AI 侧的东西讲给 Java 同学听。

**关键点解析：**
- **面试官意图**：技术分享体现"输出闭环 + 团队贡献"，考察你是不是只顾自己做项目，还是有分享和沉淀意识。
- **怎么答**：准备方法（问题先行 + 实战内容 + 结构 + 沉淀文档）+ 三个收获（逼真懂、团队沉淀、跨角色沟通）。
- **深挖方向**：可能追问"分享里最有价值的一次是哪个"，答"RAG 幻觉优化——因为它把抽象问题讲成可复用的三层防控方法，Java 同学也能听懂，能直接指导后续项目"。

**扣分项：** "分享了"说不出内容；分享抄文档没有实战；无收获反思。

### Q96：你简历写"参与 20 余次检索并发压测"——压测在项目里的价值是什么？

**参考回答：** 价值三层：① 发现线上才会出现的问题——单测和功能测试覆盖不到并发、超时、资源竞争这些，压测能提前暴露；② 量化能力——给出 QPS、P95/P99 这些数据，让"检索快不快"从感觉变成可衡量、可对比的指标，也是以后优化效果的基线；③ 驱动优化方向——压测数据告诉我们瓶颈在哪（连接池、线程、GC、还是模型调用），优化有的放矢。比如我发现的 7 类问题，大多不在功能测试里出现，是靠压测暴露的。

**关键点解析：**
- **面试官意图**：考察你对"压测为什么值得做"的理解，而不是"简历上凑个亮点"。
- **怎么答**：三层价值——发现问题、量化能力、驱动优化。落到具体（7 类问题靠压测暴露）。
- **深挖方向**：可能追问"压测和线上真实流量区别"，答"压测是构造流量，覆盖不了所有真实场景（网络抖动、突发尖峰、业务分布），所以压测发现的问题不能代表全部，但能覆盖大部分并发类问题，是成本可控的手段"。

**扣分项：** "测试让系统更稳"（空泛）；说不清压测发现的具体问题。

### Q97：Git 分支管理和 CodeReview 你平时怎么做？

**参考回答：** Git 分支：用 Git Flow 或简化版——main 主干稳定，feature 分支开发新功能，命名规范（feature/xxx 或 fix/xxx），功能完成后合并回 main；合并用 PR/MR 走 review，不做直推主干。CodeReview：① 先看需求上下文再 review，不只看 diff；② 重点看正确性（边界、并发、异常处理）、可读性、是否有更简方案；③ 提意见具体、给建议不指责；④ 自己提交前自测 + 看一遍自己的 diff。我的习惯是每次提交小而聚焦，便于 review 和回滚。

**关键点解析：**
- **面试官意图**：协作规范是团队工程素养，考察你是不是一个人写代码惯了，还是懂团队协作。
- **怎么答**：分支策略（feature + PR/MR + 命名规范）+ CodeReview 方法（看上下文、重点维度、建设性意见）+ 提交习惯（小而聚焦）。
- **深挖方向**：可能追问"code review 时意见分歧怎么办"，答"以代码正确性和团队规范为准，说不清就找第三人仲裁，或小范围实验验证，不靠嗓门；分歧点本身往往说明需求或规范不清，值得一起对齐"。

**扣分项：** "直接 push 主干"；"review 就看一眼格式"；意见只说"这样不行"。

### Q98：接口契约在项目里怎么定的？遇到字段对不齐怎么处理？

**参考回答：** 定义：先写契约文档（或 FastAPI 自动生成的 OpenAPI），明确字段名、类型、必填/可空、错误码、分页规则，作为双方开发的唯一依据，联调前评审。处理对不齐：① 先判断是"契约理解偏差"还是"实现 bug"——两边都对照契约，谁不符合谁改；② 是契约本身不合理就改契约，走文档变更流程，两边同步更新，不口头改；③ 补充测试用例覆盖边界。原则是"契约是事实来源，改契约走流程，不临时加判断硬凑"。

**关键点解析：**
- **面试官意图**：契约是跨团队协作的核心，考察你是不是有"契约先行"意识而不是"对到能跑为止"。
- **怎么答**：定义（文档先行 + 明确语义）+ 对不齐的处理（先判类型 + 改契约走流程 + 补测试）+ 原则（契约是事实来源）。
- **深挖方向**：可能追问"契约改了一个字段，怎么保证两边都同步"，答"改契约文档/OpenAPI 为准，两边按文档各自改代码，改完跑联调用例验证，核心契约变更要评审"。

**扣分项：** "口头说一下"；"谁先改到跑通算谁的"；没有契约文档。

### Q99：作为应届生，你的核心竞争力是什么？凭什么让我们选你？

**参考回答：** 三点：① 双栈能力——既懂 Java 后端（SpringCloud、OpenFeign、微服务），又懂 Python AI（LangChain、RAG、Agent），能一个人打通"业务系统 ↔ AI 服务"的链路，这在需要 AI 和业务融合的团队是稀缺的；② 工程落地能力——不只是"模型能答对"，我会考虑幂等、数据一致、异常兜底、可观测，能把模型能力收敛成可测试可维护的业务接口，两个项目里都有数据验证（7 类故障修复、三层幻觉防控）；③ 自驱和学习能力——技术分享、工程复盘、持续跟进 LangGraph/MCP，证明我有输出闭环和成长性。我的优势是把 AI 从"demo"变成"能稳定运行的业务功能"。

**关键点解析：**
- **面试官意图**：这是总结陈词题，考察你能不能清晰地给自己定位，并匹配岗位需求。
- **怎么答**：三点——双栈稀缺、工程落地、自驱成长，每点带证据，最后一句总结定位（把 AI 从 demo 变业务功能）。
- **深挖方向**：可能追问"你最有信心的一行代码是什么"或"最自豪的 bug 修复"，准备好一个具体的代码/故障案例讲清 STAR。
- **扣分项**：泛泛"我学习能力强"；没有证据支撑；定位和岗位不匹配。

### Q100：如果入职后，前三个月你的计划是什么？

**参考回答：** 分三步：第一个月——快速融入，读团队代码库和文档，跑通项目本地环境，熟悉接口契约和 AI 链路的代码位置，有问题及时请教；第二个月——上手实战，从团队现有的 RAG/Agent 模块接小任务，用实习和实训积累的经验落地，同时补齐团队技术栈的细节；第三个月——产出价值，独立负责一个可交付的小模块，把压测、排障、文档沉淀的工程习惯带进来，并主动做一次技术分享回馈团队。整体原则：先对齐再动手，先学习再贡献。

**关键点解析：**
- **面试官意图**：考察你的入职心态——是"我先学三个月再说"还是"尽快产出"，以及是否务实。
- **怎么答**：三阶段（融入 → 实战 → 产出），每阶段一个具体动作，落脚"对齐再动手、学习再贡献"。主动提技术分享和文档沉淀，呼应你的简历习惯。
- **深挖方向**：可能追问"如果前三个月都是重复性工作怎么办"，答"重复工作也要做扎实，同时主动观察能不能优化、提炼成可复用工具或文档——把重复活做出增量价值，而不是抱怨"。

**扣分项：** "听领导安排"（被动）；"我学得很快"（空洞）。

---

## 附：复习路线与面试策略

### 一、按面试官视角优先级排序

| 优先级 | 模块 | 题号 | 理由 |
|--------|------|------|------|
| 🔥 必考 | 电商项目深挖 | Q31–Q55 | 你的最强项目（负责人 + 数据 + 全链路），面试官 50% 时间砸在这 |
| 🔥 必考 | 农业项目深挖 | Q11–Q30 | 第一个项目，考察基础 RAG/Agent 能力和个人独立交付 |
| ⭐ 高频 | AI 技术原理 | Q56–Q75 | 硬核原理题，区分"会用"和"懂" |
| ⭐ 高频 | Java 微服务协同 | Q76–Q90 | 双栈定位的第二支柱 |
| ⭐ 高频 | 自我介绍/动机 | Q1–Q10 | 开场决定印象分 |
| 💡 看情况 | 工程实践/软素质 | Q91–Q100 | 简历亮点，可能被深挖或一笔带过 |

### 二、必背的"数据弹药"（口述时自然带出）

- **7 类故障**（压测暴露）：召回超时、工具调用失效、会话串号、SSE 断流、重复消费、缓存穿透、格式异常 —— 每个配根因+修复
- **三层幻觉防控**：检索低分过滤 → Prompt 约束 → 实体校验（输入→生成→输出闭环）
- **两套工程机制**：MD5 指纹幂等入库；RocketMQ 增量 + Elastic-Job 定时对账
- **两个项目的演进主线**：单服务内存会话 → 微服务跨语言 + 数据一致性

### 三、容易被追问的"坑位"（提前准备）

1. **每个数字**：7 类故障具体是哪些？20 次压测怎么测的？—— 数字后面必须站得住
2. **每个名词**：InMemorySaver、MMR、RRF、LCEL、HNSW —— 自我介绍出现的名词都能展开 2 分钟
3. **每个选型**：为什么 RedisStack 不用 Milvus？为什么双模式？为什么 MQ 不用同步？—— 都要有"权衡"不是"碰巧"
4. **诚实边界**：Sentinel/Seata、LangGraph/MCP 哪些是了解哪些是实战 —— 主动划清，别被追问时露馅

### 四、答题节奏建议

- **项目题**（Q11–Q55）：30% 讲背景和方案，70% 讲"为什么"和"踩了什么坑"，技术经理最看重决策过程和复盘
- **原理题**（Q56–Q75）：结论先行 → 展开机制 → 落到你的项目，避免纯背八股
- **软素质题**（Q91–Q100）：给方法论 + 一个真实案例，避免空谈
- 每题控制在 2 分钟内，被打断说明讲多了，学会一句话结论 + 展开供追问

### 五、文档配套

- 本文档为详解版（含面试官意图 / 深挖方向），适合系统复习
- 如需轻量背诵版（每题仅问题 + 关键得分点），可基于本文档精简生成
- 建议配合 `4-interview/projects/agri-qa-assistant/` 与电商项目的项目深度面试文档交叉复习

---

> 生成说明：本文档基于 `D:\简历\纯AI\纯AI_简历_STAR优化版.docx` 提取的真实内容设计，覆盖简历全部亮点（双工具路由、InMemorySaver 会话隔离、SSE 流式、Pydantic 结构化、MD5 幂等、RedisStack+MMR、三层幻觉防控、OpenFeign 跨语言、RocketMQ+Elastic-Job 一致性、7 类故障排障、3 场技术分享），并按技术经理视角编排追问方向。

