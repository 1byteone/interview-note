# 纯 AI 简历 · 面试 100 问 · 背诵版

> 基于 `纯AI_简历_STAR优化版.docx`，从详解版精简。
> **每题仅保留：问题 + 关键得分点（3–5 条），供考前快速过目。**
> 详解版含完整参考回答和深挖方向，用于深入复习。

---

## 模块一：自我介绍 & 职业动机（Q1–Q10）

**Q1：请用 1 分钟做一个自我介绍。**
- 三段式：背景一句 → 两段经历各带差异化亮点 → 双栈定位句
- 农业项目钩子：双工具路由 + 幻觉拦截；电商项目钩子：RAG 全链路 + 跨服务协同
- 不堆框架名词，给面试官能追问的钩子
- 控制 1 分钟，不背诵、语速从容

**Q2：你为什么选择 AI 应用开发方向？**
- "AI 落地工程难点（检索质量/输出稳定性/异常兜底）匹配我的 Java 工程能力"
- 用电商项目三层幻觉防控作现实证据
- 要点：区分"调 API"和"工程落地"

**Q3：你觉得自己最大的优势和不足？**
- 优势：双栈协同 + 全链路意识（实例：幂等入库、跨语言联调、压测排障）
- 不足：RocketMQ 停留使用层，正在补源码级知识（NameServer/Broker/CommitLog/ConsumeQueue）
- 要点：不足必须"真实 + 可改进 + 正在改进"

**Q4：你的职业规划？**
- 短期：深耕 AI 应用开发 + 补中间件底层
- 中期：独立负责 AI 系统模块
- 长期：AI 与业务的技术桥梁
- 要点：绑定当前岗位，不跳槽信号

**Q5：你对加班怎么看？**
- "关注产出而非工时"；攻坚期没问题 + 效率思维减少返工
- 要点：用工程手段（文档/契约/测试覆盖）提效，不只是"能加班"

**Q6：期望薪资？**
- 看重成长空间和方向匹配；薪资开放，参考公司体系协商
- 要点：不报死数字，表达灵活性

**Q7：你还投了哪些公司/岗位？**
- AI 应用开发 + Java 后端方向，集中在能做 AI 工程落地的团队
- 要点：方向聚焦，不海投

**Q8：你有什么想问我们的？**
- ① 团队 AI 项目技术栈（混合检索？LangChain/LangGraph？）
- ② 新人前 3 个月核心期望
- ③ 技术分享和成长机制
- 要点：问团队/技术/成长，不问薪资福利（初面）

**Q9：陌生技术栈怎么上手？**
- 三步：官方文档 → 最小 Demo → 业务场景深入
- 用 LangChain 实战经历佐证
- 要点：向团队已有代码和规范学习

**Q10：平时怎么学习新技术？**
- 四步：官方文档 → Demo 验证 → 笔记/分享沉淀 → 项目应用反馈
- 要点：输出闭环（3 场技术分享就是证据）

---

## 模块二：农业知识库问答智能体（Q11–Q30）

**Q11：农业问答整体架构和数据流？**
- 入口 → Agent 调度 → 私有 PDF 检索 Tool → 通用知识 Tool → 模型生成 → Pydantic 校验 → SSE 流式
- InMemorySaver 按 thread_id 隔离会话
- 要点：每个环节点出组件和职责，讲清边界

**Q12：为什么双工具路由而不是一个工具？**
- 私有库权威但窄；通用广但编造
- 双工具互补：准确性 vs 覆盖面的权衡
- 要点：先说单工具缺陷，再说互补逻辑

**Q13：私有 PDF 检索 Tool 怎么实现？**
- PDF 解析 → 语义分块 + overlap → embedding → 向量库 → Top-K 召回
- Tool 接口对 Agent 隐藏底层细节
- 要点：语义切分优先于固定字符

**Q14：通用知识 Tool 是什么？**
- 本质是无 RAG 的模型裸调用，封装成 Tool
- 私有未命中时兜底常识问题
- 要点：诚实说"就是裸模型调用"

**Q15：System Prompt 约束知识库优先怎么写？**
- Prompt 明确优先级 + 触发条件 + 禁止编造
- 关键：代码层分数阈值强制路由（软约束+硬约束组合）
- 要点：承认 prompt 是软约束，有代码层兜底

**Q16：LangChain / LCEL 怎么用？**
- LCEL 用 `|` 管道语法串组件，原生支持流式/异步/fallback
- 要点：声明式组合、流式原生、fallback 三个实在好处

**Q17：InMemorySaver 为什么选它？局限是什么？**
- 实训场景单实例够用、开发快
- 局限：重启丢会话、多实例不共享
- 生产替代：Redis/数据库 Saver
- 要点：主动说局限 + 生产方案

**Q18：thread_id 隔离怎么保证不串话？**
- 隔离靠 thread_id 唯一性，不在组件
- 风险：thread_id 来源不可控（前端伪造）
- 生产：绑定鉴权、服务端生成
- 要点：不信任前端传入

**Q19：多轮上下文怎么传递？**
- 历史消息作为上下文回传给模型，模型自行维持指代
- 不是手动提取"作物=番茄"
- 要点：理解多轮本质是历史回传

**Q20：FastAPI 异步 astream 怎么封装？**
- async 路由 + async generator yield token + SSE 格式
- 全局异常捕获包住，异常时 yield 错误事件再关流
- 要点：async 不阻塞 + 流式异常处理

**Q21：SSE 流式 vs 一次性返回 JSON？**
- 首字延迟改善（几百毫秒 vs 10 秒）
- 模型没变快，是显示策略变了
- 要点：主动说代价（后处理受限、前端要拼接）

**Q22：流式异常怎么处理和恢复？**
- 异常 yield 错误事件 + 关流 + 记日志
- 恢复：前端记录已收 token + thread_id，调补偿接口续传
- 要点：续传用上 InMemorySaver 检查点

**Q23：打字机式展示前端怎么对接？**
- EventSource 监听 + 增量拼接 + 完成/错误事件处理 + 断线重连
- 要点：契约约定完成事件格式

**Q24：Pydantic 结构化输出模型不按格式输出怎么办？**
- 解析失败有限重试 → 仍失败用兜底默认值 → 关键字段缺失不放行
- 要点："尽力而为 + 校验兜底"不是强保证

**Q25：为什么选这四个字段（作物/病害/风险/方案）？**
- 覆盖"识别 → 评估 → 应对"决策链
- 不选价格/用量因地区差异大，强行结构化反而编造
- 要点：有取舍逻辑，不是随便定

**Q26：怎么发现"编造"问题的？**
- 测试时对照手册发现模型方案不符 → 用检索来源引用比对
- 要点：回答带引用，找不在引用里的内容即为编造

**Q27：检索不到内容怎么处理？**
- 私有未命中 → 通用兜底 → 都不行就告知未覆盖 + 标"未知"
- 宁可说不知道，不编造
- 要点：带来源标识区分有依据/兜底

**Q28：PDF 分块粒度怎么定？**
- 语义段落切分，保留 overlap，带元数据
- 粒度在测试集上对比调，块太大不精准、太小丢上下文
- 要点：不是一次定死，可配置可评估

**Q29：重新做会改进什么？**
- ① 会话持久化（重启恢复）② 检索效果可观测 ③ 引用溯源 ④ 容器化部署
- 要点：四个维度对应四个边界

**Q30：农业问答 vs 通用 ChatBot 的本质区别？**
- 知识来源（私有库 vs 参数知识）+ 准确性要求 + 输出形态（结构化 vs 自由）
- 本质：从自由对话收敛到有依据、可结构化的专业问答
- 要点：最后那句本质是区分度

---

## 模块三：电商自然语言 AI 智能检索系统（Q31–Q55）

**Q31：整体架构？Java 和 Python 怎么分工？**
- Java 业务微服务 ↔ OpenFeign ↔ Python FastAPI AI 检索服务
- 我是 AI 模块全权负责人，独立完成 Python 侧
- 要点：主动划清个人边界

**Q32：为什么 Python 做 AI 而不是 Java？**
- ① AI 生态在 Python ② 职责隔离+独立扩容 ③ 团队分工
- 要点：有取舍（跨语言开销 + 契约成本）

**Q33：向量入库怎么做幂等？**
- MD5 生成内容指纹 → 入库以 MD5 为唯一键
- 内容变 MD5 变才更新，未变跳过
- 要点：和 SKU ID 职责不同（业务主键 vs 内容版本）

**Q34：文本拼接和递归分片？**
- SKU 名+描述+属性拼接 → RecursiveCharacterTextSplitter（段落→句号→逗号递归）
- 要点：优先语义边界切，保完整性

**Q35：RedisStack 向量检索为什么选它？**
- 项目已有 Redis 复用基础设施 + 数据量够用 + 支持混合过滤
- 局限：百万级以上换专用库
- 要点：复用 vs 专用的权衡

**Q36：余弦相似度为什么不用欧氏？**
- 余弦看方向（语义相似）对长度不敏感；欧氏被向量模长干扰
- 取值 [-1,1] 便于设阈值
- 要点：工程上余弦更适合文本语义

**Q37：MMR 去重排序是什么？**
- 兼顾相关性和多样性，避免 Top-K 都是同质结果
- 选候选时惩罚与已选结果太接近的
- 要点：电商多样性影响用户体验

**Q38：三层幻觉防控每层做什么？**
- 输入侧：检索低分过滤（丢弃不相关片段）
- 生成侧：System Prompt 约束不编造
- 输出侧：实体校验回查商品库
- 要点：三层在数据流的不同位置，形成闭环

**Q39：Prompt 约束真的管用吗？**
- 软约束，降低频率但不能零
- 所以才有三层，不单靠 prompt
- 要点：承认 LLM 概率本质，多层叠加

**Q40：商品实体校验失败怎么处理？**
- 价格不一致：以商品库为准覆盖
- 商品名不存在：剔除或标未找到
- 规格不符：标以商品页为准
- 要点：事实以业务库为准，模型只组织语言

**Q41：多轮条件筛选怎么承接？**
- 历史条件累积成过滤条件 + 会话记忆承接
- 三轮例子："手机" → "+2000-3000" → "+华为"
- 要点：是累积不是每轮独立检索

**Q42：为什么保留 ES 和 RAG 双模式？**
- ES：精确匹配强、性能高；RAG：理解口语化模糊
- 一刀切用任一种都有缺陷
- 要点：各有所长，便于 A/B 对比

**Q43：ES + IK + DSL 多条件筛选？**
- text+IK（细粒度召回）+ keyword（精确过滤）+ bool（must/filter/sort）
- 深分页用 search_after
- 要点：ik_max_word 索引 / ik_smart 查询

**Q44：向量和 ES 融合排序？**
- RRF（Reciprocal Rank Fusion）：按排名融合，不直接加分数
- 要点：分数量纲不同，排名融合更稳

**Q45：embedding 怎么选？**
- 中文支持好 + 维度适中 + 查询和入库必须同模型
- 要点："同模型同预处理"是基本约束

**Q46：向量库和业务库一致性？**
- 增量：MQ 推送变更 + MD5 幂等更新
- 定时：Elastic-Job 全量对账兜底
- 要点：增量+定时双保险

**Q47：为什么用 MQ 不同步调用？**
- 解耦（不阻塞 Java）+ 削峰 + 容错（重试）
- 代价：最终一致延迟，用对账兜底
- 要点：对比同步适用场景

**Q48：OpenFeign 调 Python 接口注意什么？**
- ① 超时设够长 ② 异常降级返回兜底 ③ 契约对齐 ④ 序列化处理
- 要点：超时和降级是跨语言核心坑

**Q49：接口契约怎么对接？**
- 以 FastAPI 的 OpenAPI 为单一事实来源
- Java DTO 严格对齐 Pydantic 字段
- 要点：契约先行 + 联调以文档为准

**Q50：向量同步和智能推荐两个接口怎么划分？**
- 写（同步）关注幂等/一致性；读（推荐）关注延迟/召回质量
- 要点：读写分离，QPS/SLA 不同

**Q51：为什么不直接查全量 SKU？**
- 撑爆内存；分页/流式读取 + 内存恒定 + 断点续做
- 要点：游标分页避免深分页

**Q52：增量+定时一致性保障？**
- 增量：MQ + MD5 判变 → 删旧插新
- 定时：全量对账 → 多删少补
- 要点：增量保实时，定时保最终一致

**Q53："适合老人用的手机"怎么检索？**
- 关键词处理不了非属性语义 → 向量跨字面匹配
- 要点：语义检索解决关键词解决不了的问题

**Q54：跨语言协作怎么做？**
- 契约先行 → 并行开发 → 联调排障（改契约走文档不口头）
- 要点：字面不一致由 AI 侧主导统一

**Q55：两个项目的技术演进？**
- 延续：RAG+Agent+幻觉防控+结构化输出
- 差异：单服务→微服务跨语言+数据一致性
- 演进：从"能跑"到"稳定运行+业务协同"
- 要点：最后那句演进本质是亮点

---

## 模块四：RAG / Agent / LLM 技术原理（Q56–Q75）

**Q56：什么是 RAG？解决什么问题？**
- 检索增强生成 = 先检索外部知识 + 再生成
- 解决：知识时效 / 幻觉 / 私有知识
- 要点：参数记忆 → 外部检索

**Q57：RAG 完整链路？**
- 离线：文档加载 → 分块 → 向量化 → 入库
- 在线：查询 → 向量化 → 召回 → 重排 → 拼进 prompt → 生成
- 要点：每环节都影响效果

**Q58：分块策略？你选哪种？**
- 递归字符切分（语义边界优先）+ overlap + 元数据
- 要点：多种策略对比选型

**Q59：embedding 是什么？**
- 文本 → 稠密向量，语义相近则距离近
- 同模型查询和入库
- 要点：语义空间映射，不是简单编码

**Q60：向量相似度和索引算法？**
- 余弦（语义常用）/ 欧氏 / 内积
- FLAT（小数据）/ HNSW（中等）/ IVF+PQ（海量）
- 要点：相似度和索引是两个层面

**Q61：Agent vs Chain？**
- Agent：LLM 动态决策工具调用
- Chain：预定义固定流程
- 要点："固定流程" vs "动态路由"

**Q62：ReAct 模式？**
- Thought → Action → Observation 循环
- 推理显式化，可解释可纠错
- 要点：function calling 是它的工程升级

**Q63：LCEL Runnable 接口？**
- 统一 invoke/batch/stream/astream
- 用 `|` 组合，链也是 Runnable
- 要点：统一抽象带来组合性

**Q64：LangGraph 和 LCEL 关系？**
- LCEL 适合线性；LangGraph 适合图（循环/分支/多 Agent）
- 要点：进阶不是替代

**Q65：LangGraph 状态怎么传递？**
- 全局 State 对象 → 节点返回更新 → merge → 条件边跳转
- checkpointer 持久化，支持断点续跑
- 要点：条件边做循环

**Q66：MCP 是什么？**
- 标准化 LLM ↔ 工具/数据源的连接协议
- 写一次工具，多应用复用
- 要点：工具集成从造轮子到协议互通

**Q67：LLM 幻觉能完全消除吗？**
- 根因：概率生成非检索事实；完全不能消除
- 工程目标：降低频率 + 可追溯
- 要点：多层防控 + 引用溯源

**Q68：Prompt Engineering 核心原则？**
- 角色明确 / 上下文约束 / 结构化输出 / few-shot / 负面指令 / 版本化
- 要点：减少模型自由度

**Q69：few-shot vs zero-shot？**
- zero-shot 不给样例；few-shot 给样例引导格式/风格/逻辑
- few-shot 2–5 个，覆盖典型 + 多样
- 要点：多了贵且可能过拟合表面特征

**Q70：function calling / tool use？**
- 工具定义 → 模型输出结构化调用请求 → 执行 → 结果回传
- 比 ReAct 更结构化稳定
- 要点：模型决策标准化成结构化输出

**Q71：Agent 工具调用失败怎么处理？**
- ① 结构化错误回传给模型 ② 有限重试 ③ 降级 ④ 终止保护 ⑤ 可观测
- 要点：让模型参与决策恢复

**Q72：LLM 应用可观测性记什么？**
- 输入 → 检索（召回/分数）→ 模型调用（模型/token/耗时）→ 工具调用 → 输出
- traceId 串联全链路
- 要点：效果/性能/成本/幻觉追溯

**Q73：LLM 调用成本怎么控制？**
- 模型分级 / prompt 精简 / 结果缓存 / token 监控 / 早期终止
- 要点：按需用模型 + 能缓存就缓存

**Q74：SSE vs WebSocket？**
- SSE 单向推送（HTTP），WebSocket 双向全双工
- AI 流式：单向够 + HTTP 穿透好 + 实现简单
- 要点：WebSocket 适合聊天室/游戏，SSE 性价比更高

**Q75：前端怎么知道生成结束？**
- 后端发明确的 done 事件，不能靠连接关闭判断
- 要点：正常/异常事件要区分，前后端约定好

---

## 模块五：Java 基础与微服务协同（Q76–Q90）

**Q76：SpringCloud Alibaba 用过哪些组件？**
- Nacos（注册+配置）、OpenFeign（声明式调用）、Sentinel/Seata（了解）
- 要点：诚实说用过/了解的边界

**Q77：Nacos 两个职责怎么实现？**
- 注册中心：服务注册 → 拉取列表 → 订阅推送
- 配置中心：拉取 → 长轮询监听 → 热更新
- 底层共通：集中存储 + 变更推送

**Q78：Nacos vs Eureka？**
- Nacos 注册+配置一体 + 长轮询推送 + Alibaba 生态
- Eureka 只注册 + 定时拉取 + AP
- 要点：Nacos 功能更多，生态更契合

**Q79：OpenFeign 原理？**
- 动态代理 → 拦截调用 → 注解驱动构造 HTTP 请求 → 底层客户端发送
- 要点：动态代理是核心机制

**Q80：OpenFeign 降级怎么做？**
- fallback 类 / fallbackFactory（能拿异常原因）
- 由 Sentinel/Resilience4j 触发
- 要点：降级返回业务可接受兜底

**Q81：FastAPI 适合 AI 服务的原因？**
- ① 异步原生 ② 流式友好 ③ Pydantic 校验 ④ 自动文档 ⑤ AI 生态
- 要点：前三个直接对应 AI 服务需求

**Q82：FastAPI 和 SpringBoot 协同的坑？**
- 序列化风格 / 超时配置 / 异常映射 / 契约维护 / 环境隔离
- 要点：序列化和超时是高频坑

**Q83：Redis 在两个项目里分别做什么？**
- 农业：轻量缓存；电商：RedisStack 向量检索 + 缓存
- 要点：RedisStack ≠ 普通 Redis

**Q84：Redis 持久化和数据丢失？**
- RDB + AOF 两种持久化
- 原则：业务事实存 DB，Redis 存可重建数据
- 要点：不把 Redis 当唯一事实源

**Q85：RocketMQ 核心概念？**
- Producer/Consumer/Topic/Queue/Broker/NameServer/Group
- 集群消费保证不重复
- 要点：概念清晰不只会发消息

**Q86：消息不丢+重复消费怎么处理？**
- 不丢：Producer 重试 + Broker 持久化+主从 + Consumer ACK+死信
- 重复：业务幂等（MD5 指纹）
- 要点：at-least-once + 业务幂等

**Q87：Elastic-Job vs @Scheduled？**
- @Scheduled 单机重复；Elastic-Job 分布式分片 + 高可用
- 要点：多实例场景 @Scheduled 有重复问题

**Q88：MySQL 索引原理？**
- B+ 树 + 联合索引最左前缀 + 避免回表 + 游标分页
- 要点：索引不是越多越好，按查询模式建

**Q89：SQLAlchemy vs MyBatis？**
- SQLAlchemy 全自动 ORM 开发快；MyBatis 半自动 SQL 控制强
- 要点：场景选择不是好坏之分

**Q90：跨语言代码规范怎么统一？**
- 语言内各自规范（阿里手册/PEP 8）
- 协作层面统一：接口契约/Git 工作流/错误码/文档
- 要点：统一协作，不统一语言内风格

---

## 模块六：工程实践与软素质（Q91–Q100）

**Q91：20 次压测的 7 类问题？**
- ① 召回超时 ② 工具调用失效 ③ 会话串号 ④ SSE 断流 ⑤ 重复消费 ⑥ 缓存穿透 ⑦ 格式异常
- 每类配根因+修复
- 要点：至少能说 4–5 类

**Q92：向量召回超时怎么优化？**
- 定位：耗时分布 + 日志找慢点
- 优化：连接池复用 + 异步 + 超时快速失败 + 结果缓存
- 要点：优化后做 P99/吞吐对比验证

**Q93：压测方法论？**
- 明确目标 → 准备数据 → 隔离环境 → 逐步加压 → 关注分位延迟 → 压测后状态
- 要点：不只看峰值，看 P95/P99 和错误率

**Q94：工具调用失效怎么排查？**
- 日志(traceId) → 降低并发复现 → 共享状态隔离修复 → 回归验证
- 要点：偶发+并发相关 → 基本指向共享可变状态

**Q95：3 场技术分享怎么准备的？**
- 问题先行 + 实战内容 + "背景→方案→坑→效果"结构 + 沉淀文档
- 收获：逼真懂 + 团队沉淀 + 跨角色沟通
- 要点：分享内容来自项目实战

**Q96：压测在项目里的价值？**
- ① 发现并发类问题 ② 量化指标（QPS/P95） ③ 驱动优化方向
- 要点：7 类问题靠压测暴露

**Q97：Git 分支和 CodeReview？**
- feature 分支 + PR/MR review + 小而聚焦提交
- 要点：Review 看正确性/可读性/边界，意见建设性

**Q98：接口契约怎么定？字段对不齐怎么处理？**
- OpenAPI 文档为事实来源；对不齐先判类型再改契约走流程
- 要点：契约先行，改契约走文档不口头

**Q99：你的核心竞争力？**
- ① 双栈稀缺（业务↔AI 链路）② 工程落地（幂等/一致/异常/可观测）③ 自驱学习
- 要点：从 demo 到能稳定运行的业务功能

**Q100：入职前三个月计划？**
- 一月：读代码+环境+契约；二月：接小任务落地；三月：独立交付+技术分享
- 要点：对齐再动手，学习再贡献

---

> **使用建议**：考前 1–2 天通读一遍背诵版，只过"关键得分点"不读完整回答；
> 不熟的题回详解版看"面试官意图"和"深挖方向"，对着镜子口述。
