# 技术经理面试 100 问 · QA 全集

> 基于 `简历_模板覆盖版.docx`，从技术经理/技术面试官视角设计。
> 每题包含：**问题 → 参考回答 → 关键得分点 → 常见扣分项**。

---

## 模块一：自我介绍 & 职业动机（1-10）

### Q1：请用 1 分钟做一个自我介绍。
**参考回答：** 我是软件工程专业应届生，方向是 Java 后端 + AI 应用开发。核心经历两段：一是传习教育的 AI 工程实习，参与商品搜索接口开发、RAG 知识库治理、SSE 流式交互，召回效果提升 35%；二是独立完成灵犀智能写作平台，基于 Spring AI Alibaba 的 StateGraph 编排多智能体，SSE 毫秒级首字上屏，生成成功率 99%。技术上以 Spring Boot + Redis + MySQL 为底座，叠加 RAG / Agent / SSE 等 AI 能力。我的特点是能把模型能力稳定地接到业务链路里。
**关键得分点：** 有结构（时间控制+技术主线+数据量化），不背稿，语速从容，突出"工程+AI"复合定位。
**扣分项：** 超时 >2min，背诵感强，无数据支撑，泛泛而谈。

### Q2：你为什么选择 AI 应用开发方向？
**参考回答：** AI 落地不只是调 API，核心难点在于检索质量、输出稳定性、延迟控制和异常兜底。这些恰好和我的 Java 工程背景高度契合。我希望成为"懂业务的 AI 工程师"，而不是只写 prompt 的 API 使用者。实习中我验证了这条路径的可行性——通过工程手段把 RAG 召回从不稳定提升到 35% 以上改善。
**关键得分点：** 回答有深度（区分"调 API"和"工程落地"），结合自身经历论证。
**扣分项：** "觉得 AI 热门所以选"——无个人思考。

### Q3：你觉得自己最大的优势和不足分别是什么？
**参考回答：** 优势是"全链路意识"——不只写功能，还关注测试、异常、文档和可维护性。不足是：实习深度有限，某些中间件（如 RocketMQ）停留在使用层面，底层原理还需加强。目前在系统学习源码级知识。
**关键得分点：** 优势有实例支撑（实习/项目中全链路做法），不足真实且有改进计划。
**扣分项：** "我没什么不足"——缺乏自省；不足变优点（"我太追求完美了"）。

### Q4：你的职业规划是什么？
**参考回答：** 短期（1-2年）：在 AI 应用开发岗位上深耕，补齐中间件底层原理和分布式系统设计短板。中期（3-5年）：成为能独立负责 AI 系统架构的技术骨干，具备从需求分析到性能调优的全栈能力。长期：成为连接 AI 和业务的技术桥梁。
**关键得分点：** 有层次、可执行，与岗位匹配。
**扣分项：** 过于模糊（"想当架构师"），或过于功利（"想创业"）。

### Q5：你对加班怎么看？
**参考回答：** 我关注的是"产出"而非"工时"。项目攻坚期主动加班是应该的；但日常我会用工程手段（自动化、文档化、测试覆盖）减少返工，本质上也是提升效率。
**关键得分点：** 不回避，用"效率思维"回答，同时表达责任感。
**扣分项：** "接受加班"但无实质内容；"不接受加班"过于绝对。

### Q6：期望薪资是多少？
**参考回答：** 我对行业薪资有一定了解，更看重岗位成长空间和技术方向匹配度。薪资方面我持开放态度，愿意参考公司体系和岗位级别来协商。
**关键得分点：** 不报死数字，表达灵活性和学习意愿。
**扣分项：** 报价过高/过低，或"什么都行"。

### Q7：你还投了哪些公司/岗位？
**参考回答：** 主要投递 Java 后端和 AI 应用开发方向，集中在能做 AI 工程落地的团队。相比"面广"，我更倾向"面深"——选一个技术方向对口、团队氛围好的地方长期发展。
**关键得分点：** 聚焦技术方向，体现求职策略清晰。
**扣分项：** 投了大量不相关岗位，说明定位不清。

### Q8：你有什么想问我们的？
**参考回答（推荐 2-3 个）：** 1）团队目前 AI 项目的技术栈和架构是怎样的？ 2）对这个岗位的新人，前 3 个月的核心期望是什么？ 3）团队的技术分享和成长机制是怎样的？
**关键得分点：** 问"团队/技术/成长"，不问"薪资/加班"（初面阶段）。
**扣分项：** "没有问题"——缺乏兴趣。

### Q9：如果给你一个完全陌生的技术栈，你怎么做？
**参考回答：** 三步走：① 快速通读官方文档（入门+核心概念），② 跑通一个最小 Demo，③ 带着业务场景深入关键模块。同时用公司内已有代码做参照，避免从零踩坑。实习中用 LangChain 和 Spring AI 都是这么上手的。
**关键得分点：** 有方法论（文档→Demo→业务），且有实例佐证。
**扣分项：** "百度一下"——太随意。

### Q10：你平时怎么学习新技术？
**参考回答：** 分层学习：① 先看官方文档建立全景认知；② 找一个真实场景做 Demo 验证；③ 写笔记/博客沉淀；④ 在项目中应用并收集反馈。比如 RAG 技术，我先通读 LangChain 文档，再在实习中落地，最后整理了排查记录。
**关键得分点：** 有体系、有闭环（输入→实践→输出）。
**扣分项：** "看视频教程"——缺少主动性。

---

## 模块二：项目深挖 — 养老系统（11-25）

### Q11：养老系统整体架构是什么样的？你在其中负责什么？
**关键得分点：** 能说清 B 端 + C 端双端、核心模块（健康评估/IoT/鉴权）、团队分工。不用背架构图，但要能"说清楚边界"。

### Q12：健康评估从 8 秒降到 1 秒，具体做了哪些优化？
**关键得分点：** 说清"AI 调用耗时 → 缓存命中 → 结果复用"三层优化。强调：① 不随时间变化的结果缓存化；② 热点请求命中 Redis 一级缓存；③ 超时/失败降级。要有数据对比。

### Q13：Redis 多级缓存怎么设计的？为什么选多级而不是单级？
**关键得分点：** 本地缓存（Caffeine/ConcurrentHashMap） + Redis + DB 三级。本地缓存命中快（ns级）、Redis 共享缓存、DB 兜底。淘汰策略、一致性方案（先删缓存后更新 DB + 延迟双删）。

### Q14：缓存和数据库不一致怎么处理？
**关键得分点：** "先更新 DB，再删缓存" + 延迟双删；对高一致性场景用分布式锁；对容忍最终一致的场景用 TTL + 异步刷新。

### Q15：百度千帆大模型怎么集成的？
**关键得分点：** API Key + SecretKey 鉴权 → Spring Boot 封装 HTTP 调用 → 请求/响应格式处理 → 超时控制 + 重试 + 降级。说清楚：调用方式、请求参数构造、结果解析、异常兜底。

### Q16：AI 调用超时或返回异常怎么办？
**关键得分点：** ① 设置合理超时时间（如 5-10s）；② 重试策略（有限次 + 指数退避）；③ 降级方案（返回缓存的默认建议/兜底文案）；④ 异常日志记录 + 告警。

### Q17：IoTDA 设备同步是怎么做的？为什么选增量同步？
**关键得分点：** ① 设备量大（500+），全量同步耗时且浪费带宽；② 增量同步：用时间戳/版本号拉取变更；③ 异步线程池并发拉取，不阻塞主线程；④ 失败重试 + 对账兜底。

### Q18：异步线程池怎么配置的？为什么不用 @Async？
**关键得分点：** ① 自定义 ThreadPoolTaskExecutor，参数可调（核心线程/最大线程/队列/拒绝策略）；② @Async 基于 Spring AOP，粒度粗且参数难定制；③ 手动线程池能更好地监控、调优和拒绝。

### Q19：「设备-位置」绑定逻辑怎么保证数据一致？
**关键得分点：** ① 数据库唯一约束（设备ID + 位置ID）；② 更新时先查后更新 + 乐观锁/版本号；③ 定时对账脚本；④ 异常数据标记 + 人工补偿。

### Q20：ThreadLocal 在认证体系中怎么用的？如何防止内存泄漏？
**关键得分点：** 拦截器中 set，业务链路通过 ThreadLocal 获取用户信息，finally 中 remove。线程池场景下讨论 InheritableThreadLocal 的局限（复用串号）和 TransmittableThreadLocal 的优势。

### Q21：微信小程序无感登录流程？
**关键得分点：** 前端 wx.login → 获取 code → 后端调用微信 API 换 openid/session_key → 签发自定义 token → 前端后续请求带 token → 拦截器校验。强调 token 安全和过期处理。

### Q22：并发压测怎么做？压出了什么问题？
**关键得分点：** JMeter/Locust 压测工具、压测场景设计（库存扣减、重复提交）、发现问题（重复下单/超卖）、优化方案（Redisson + 幂等键 + MQ 异步）。

### Q23：Redisson 分布式锁的原理？和原生 SETNX 区别？
**关键得分点：** ① Redisson 基于 Lua 脚本保证原子性；② 有看门狗自动续期（避免锁过期但业务未完成）；③ 支持可重入；④ 原生 SETNX 无续期、无重入。

### Q24：订单链路中 RocketMQ 做了什么？
**关键得分点：** 异步解耦（下单 → MQ → 库存扣减/通知），削峰（高并发时缓冲），失败重试（消费失败 → 重试队列 → 死信队列），幂等消费（唯一业务键去重）。

### Q25：MySQL 事务在这个项目中用到了吗？遇到过死锁吗？
**关键得分点：** 下单/库存扣减场景的事务（@Transactional）、事务隔离级别选择、索引优化避免锁表。如遇死锁：用 `SHOW ENGINE INNODB STATUS` 分析，优化索引和 SQL 执行顺序。

---

## 模块三：项目深挖 — 灵犀写作（26-40）

### Q26：StateGraph 是什么？和普通的 if-else 编排有什么区别？
**关键得分点：** StateGraph 是有向图模型（节点 + 边 + 状态），支持串行/并行/条件分支。和 if-else 区别：状态在节点间显式传递、可扩展（加节点不改旧代码）、可视化可追溯。

### Q27：多智能体怎么编排？串行和并行怎么选？
**关键得分点：** 串行：有依赖关系（先大纲 → 再正文）。并行：无依赖（正文生成 + 配图生成可同时）。通过 StateGraph 的边定义依赖关系。

### Q28：新增一种生成模式，只需要配置新节点——这是什么意思？
**关键得分点：** 遵循开闭原则：新增节点 + 注册到 StateGraph + 配置边关系。不修改已有节点逻辑。用 Spring Bean 注入或配置化实现。

### Q29：SSE 流式输出的技术细节？
**关键得分点：** ① HTTP 长连接 + `text/event-stream` 格式；② 服务端逐 token 推送，前端 EventSource 接收；③ 断线自动重连（SSE 原生支持）；④ 与普通轮询/WebSocket 的区别和选型理由。

### Q30：毫秒级首字上屏怎么实现的？
**关键得分点：** ① 异步任务立即返回 SSE 连接；② 后台线程池中调用 LLM，第一个 token 到达即通过 SSE 推送；③ 不等生成完毕，首字延迟 = 网络 + LLM 首 token 延迟。

### Q31：异步任务体系怎么设计的？任务状态 tracking 怎么做？
**关键得分点：** ThreadPoolTaskExecutor + @Async 解耦耗时操作；任务状态用枚举（PENDING/RUNNING/SUCCESS/FAILED）+ 数据库/Redis 存储；回调机制（WebSocket/SSE/轮询）通知前端。

### Q32：吞吐提升 50% 是怎么衡量的？
**关键得分点：** 压测 QPS 对比：同步调用 vs 异步调用。同步时一个请求占一个线程；异步后请求立即返回，后台处理，相同资源下 QPS 翻倍。

### Q33：Prompt 模板引擎怎么封装的？
**关键得分点：** 定义模板变量（标题/主题/风格/字数），运行时填充参数，支持多模型适配。模板与业务解耦，可配置化管理。

### Q34：StructuredOutputConverter 是什么？怎么保证输出格式？
**关键得分点：** Spring AI 组件，把 LLM 自由文本输出转换为 Java Bean。"尽力而为"——不保证 LLM 必返回标准 JSON，需要校验 + 兜底。结合 System Prompt 约束格式。

### Q35：策略模式做多源配图，具体怎么实现？
**关键得分点：** 定义 ImageProvider 接口 → PexelsProvider / SVGProvider / 本地Provider 实现 → 运行时按配置/优先级选择 → 失败自动切换下一个。策略模式 + 工厂模式。

### Q36：配图源失败自动降级——降级逻辑怎么做？
**关键得分点：** try-catch 捕获 → 标记当前源失败 → 切换下一个源 → 所有源失败返回默认占位图。有监控和告警。

### Q37：生成成功率 99% 是怎么测的？
**关键得分点：** 用 N 组不同类型输入（标题/大纲/正文/完整文章）做端到端测试，统计成功（返回完整结构化 JSON）占比。1% 失败主要是模型超时或格式异常，通过重试和降级挽回。

### Q38：Redis 在灵犀项目中做什么？
**关键得分点：** ① 缓存热门模板/风格数据；② 分布式锁防止并发生成冲突；③ 任务状态缓存（高频读）。

### Q39：MyBatis Flex 和 MyBatis-Plus 区别？为什么选 Flex？
**关键得分点：** Flex 更轻量、API 更简洁、元数据驱动。适合独立项目快速开发。如果面试官追问，可以说两者思路相似（增强 CRUD），选型主要看团队习惯和生态成熟度。

### Q40：如果让你重新做灵犀项目，你会改进什么？
**关键得分点：** ① 用 Redis 持久化任务状态（当前方案重启丢失）；② 加入 LangSmith 做链路追踪；③ 用 LangGraph 替代手写 StateGraph 增强可观测性；④ 加入 token 用量监控和成本控制。体现反思能力。

---

## 模块四：实习深挖（41-50）

### Q41：实习期间做的最重要的事是什么？
**关键得分点：** 选一个最有深度的讲（建议：RAG + SSE 全链路联调），STAR 结构清晰，有数据。

### Q42：AI 商品搜索的完整链路是什么？
**关键得分点：** 用户输入 → 查询改写 → 多路召回（ES 关键词 + 向量语义）→ 排序融合 → 结果返回 → 前端渲染。说清每一层用了什么技术。

### Q43：接口契约是怎么定的？你怎么保证和前端/其他服务对齐？
**关键得分点：** Swagger/OpenAPI 文档 + 接口评审会 + 联调环境验证。强调契约先行、文档驱动。

### Q44：高并发压测具体怎么做？用了什么工具？
**关键得分点：** JMeter/Locust 设计场景（并发下单/搜索），压测环境搭建（数据准备、服务部署），监控指标（QPS/RT/P99/错误率），结果分析与优化。

### Q45：ES + IK 分词 + DSL 多条件筛选具体怎么实现？
**关键得分点：** 索引设计（字段类型/text/keyword）、IK 分词器配置（ik_smart 索引/ik_max_word 查询）、DSL 查询构建（bool/must/filter/sort）、性能优化（避免深分页、使用 scroll/search_after）。

### Q46：语义召回怎么接入的？和 ES 如何协同？
**关键得分点：** 文本 → embedding → 向量数据库存储 → 查询时 KNN 检索 → 与 ES 结果用 RRF 或加权合并。ES 强精确匹配，向量强语义理解。

### Q47：RAG 知识库的文档分块策略？
**关键得分点：** 按语义段落切分（非固定长度），设置 overlap 避免上下文断裂，按文档结构（标题层级）切分，后续可用滑动窗口二次优化。

### Q48：LangChain 和 LCEL 在实习中怎么用的？
**关键得分点：** LangChain 作为 AI 编排框架，LCEL 用管道符串联组件（Prompt → Model → Parser）。强调：定义调用链、注入 Tool、结构化输出、异常兜底。

### Q49：20 余组并发测试测出了什么？7 类问题分别是？
**关键得分点：** 检索超时、工具调用失败、会话状态异常、SSE 断流、重复消费、缓存击穿、模型输出非法。每类问题的定位手段（日志/监控）和修复方案。

### Q50：实习中最大的成长是什么？
**关键得分点：** ① 理解了"模型能力 ≠ 生产可用"，学会了从工程视角看 AI；② 提升了跨团队协作能力（接口联调/需求拆解）；③ 建立了"测试→修复→文档→复盘"的闭环习惯。

---

## 模块五：Java 基础 & 并发（51-65）

### Q51：HashMap 底层原理？JDK7 和 JDK8 区别？
**参考回答：** JDK7 用数组+链表，头插法，多线程扩容死循环。JDK8 改为数组+链表+红黑树（链表>8 时转树），尾插法，扩容时高低位拆分避免死循环。
**关键得分点：** 说清数据结构、扩容机制、线程安全问题。
**扣分项：** 只说"数组+链表"，不提 JDK8 红黑树。

### Q52：ConcurrentHashMap 怎么保证线程安全？
**参考回答：** JDK7 分段锁（Segment），JDK8 改为 CAS + synchronized（锁粒度细化到桶级别），配合 volatile 保证可见性。put 时 CAS 竞争，竞争失败再 synchronized 锁桶头节点。
**关键得分点：** 分清 JDK 版本差异，理解锁粒度演进。
**扣分项：** "它用了分段锁"然后就结束了——不够。

### Q53：线程池核心参数有哪些？怎么选型？
**参考回答：** corePoolSize、maximumPoolSize、workQueue、keepAliveTime、handler、threadFactory。CPU 密集型：core = N+1；IO 密集型：core = 2N 或更大。队列选有界 ArrayBlockingQueue 防止 OOM。
**关键得分点：** 六大参数 + 场景选型 + 有界队列意识。
**扣分项：** 只背参数不理解选型。

### Q54：线程池的拒绝策略有哪些？你们项目用的哪个？
**参考回答：** AbortPolicy（默认，抛异常）、CallerRunsPolicy（调用方执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢队列头）。项目用 CallerRunsPolicy——不丢任务，降速执行。
**关键得分点：** 四种策略 + 实际选型理由。

### Q55：synchronized 和 ReentrantLock 区别？
**参考回答：** synchronized：JVM 层面、自动释放、非公平、不可中断。ReentrantLock：API 层面、手动 unlock（finally）、支持公平/非公平、可中断、可超时、支持 Condition。
**关键得分点：** 从实现层面（JVM vs API）和功能层面（公平/中断/超时）对比。

### Q56：volatile 关键字的作用？
**参考回答：** ① 保证可见性（修改立即刷回主存）；② 禁止指令重排（内存屏障）。不保证原子性——自增仍需 AtomicInteger。
**关键得分点：** 可见性 + 有序性 + 不保证原子性，三句话说清楚。

### Q57：ThreadLocal 原理？为什么线程池下会出问题？
**参考回答：** 每个 Thread 持有一个 ThreadLocalMap，key 是 ThreadLocal 弱引用，value 是强引用。线程池复用线程，ThreadLocal 被回收后 key=null 但 value 仍在 map 中 → 内存泄漏。解决方案：finally 中 remove；跨线程传递用 TTL。
**关键得分点：** 弱引用 key + 强引用 value + 线程池复用 → 泄漏。

### Q58：AQS 原理了解吗？
**参考回答：** AbstractQueuedSynchronizer 是并发包基石。核心：state 变量 + CLH 双向队列。ReentrantLock/CountDownLatch/Semaphore 都基于它。独占模式（ReentrantLock）和共享模式（Semaphore）。
**关键得分点：** state + CLH 队列 + 独占/共享。

### Q59：乐观锁和悲观锁怎么选？
**参考回答：** 悲观锁：写多读少、冲突频繁（synchronized/Redisson）。乐观锁：读多写少、冲突少（CAS/版本号）。项目中库存扣减用悲观锁（Redisson），版本号更新用乐观锁。
**关键得分点：** 有场景对应，不空谈概念。

### Q60：CompletableFuture 怎么用？
**参考回答：** 异步编排利器。supplyAsync 启动异步任务，thenApply/thenAccept 链式处理，allOf 等待多个任务完成，exceptionally 处理异常。项目中可用于并行调用多个下游服务。
**关键得分点：** 知道 API + 实际使用场景。

### Q61：JVM 内存结构说一下？
**参考回答：** 堆（对象实例）、方法区/元空间（类信息、常量池）、虚拟机栈（栈帧：局部变量表、操作数栈）、本地方法栈（Native）、程序计数器。堆分新生代（Eden/S0/S1）和老年代。
**关键得分点：** 五大区域 + 堆的分代。

### Q62：垃圾回收算法有哪些？
**参考回答：** 标记-清除（碎片多）、标记-整理（无碎片但慢）、复制（新生代用，浪费空间）、分代收集（新生代复制 + 老年代标记整理）。CMS/G1/ZGC 各代演进。
**关键得分点：** 算法对比 + 收集器演进。

### Q63：线上 OOM 怎么排查？
**参考回答：** ① 看 GC 日志；② jmap dump 堆转储；③ MAT/VisualVM 分析大对象；④ jstack 看线程死锁；⑤ 代码层排查（大集合、未关闭流、ThreadLocal 泄漏）。
**关键得分点：** 工具链 + 排查步骤 + 常见原因。

### Q64：JVM 调优做过吗？
**参考回答：** 有基本实践：调整 -Xms/-Xmx 避免堆扩缩、设置 NewRatio 调整新生代比例、选择 G1 收集器做大堆场景。更深入的调优（GC 选择、Region 调整）还在学习中。
**关键得分点：** 有实际参数调整经验，坦诚说明不足。

### Q65：Java 8 的新特性用过哪些？
**参考回答：** Lambda、Stream API、Optional、CompletableFuture、接口默认方法。项目中 Stream 用在集合处理，Optional 用在空值防御。
**关键得分点：** 不只列特性，要关联到项目使用。

---

## 模块六：Spring & MyBatis-Plus（66-78）

### Q66：Spring IOC 和 AOP 分别是什么？
**关键得分点：** IOC：控制反转，依赖注入解耦。AOP：面向切面编程，用代理实现日志/事务/权限等横切关注点。说清代理方式（JDK 动态代理 vs CGLIB）。

### Q67：Spring Bean 的生命周期？
**关键得分点：** 实例化 → 属性注入 → Aware 回调 → BeanPostProcessor 前置 → InitializingBean/init-method → BeanPostProcessor 后置 → 使用 → DisposableBean/destroy-method。

### Q68：Spring 循环依赖怎么解决？
**关键得分点：** 三级缓存：singletonObjects（成品）→ earlySingletonObjects（半成品）→ singletonFactories（ObjectFactory）。构造器注入无法解决（提前暴露对象）。

### Q69：@Transactional 事务失效的场景？
**关键得分点：** ① 非 public 方法；② 同类内部调用（this 调用绕过代理）；③ 异常被 catch 未抛出；④ 抛出非 RuntimeException 未指定 rollbackFor；⑤ 多数据源未指定事务管理器。

### Q70：Spring Boot 自动配置原理？
**关键得分点：** @SpringBootApplication → @EnableAutoConfiguration → spring.factories/AutoConfiguration.imports 加载候选配置类 → @Conditional 条件装配。关键：spring-autoconfigure 机制。

### Q71：Spring AOP 用 JDK 还是 CGLIB 代理？
**关键得分点：** 默认：实现了接口用 JDK 代理，否则 CGLIB。Spring Boot 2.x 默认用 CGLIB（proxyTargetClass=true）。说清两种代理的区别和切换方式。

### Q72：MyBatis 工作原理？
**关键得分点：** 加载配置 → SqlSessionFactory → SqlSession → Executor → MappedStatement → JDBC。Mapper 接口通过动态代理（MapperProxy）执行 SQL。

### Q73：MyBatis 一级缓存和二级缓存？
**关键得分点：** 一级：SqlSession 级别，默认开启。二级：Mapper 级别，需手动开启，跨 SqlSession 共享。增删改会清空缓存。

### Q74：MyBatis-Plus 比 MyBatis 多了什么？
**关键得分点：** 通用 CRUD 封装、条件构造器（LambdaQueryWrapper）、分页插件、逻辑删除、自动填充、代码生成器。核心原理：MyBatis 插件机制扩展。

### Q75：MyBatis 分页插件原理？
**关键得分点：** 基于 MyBatis Interceptor 拦截 SQL → 拼接 LIMIT → 执行 COUNT 获取总数。物理分页而非内存分页。

### Q76：MyBatis 动态 SQL 有哪些标签？
**关键得分点：** if/choose/when/otherwise、where/set/trim、foreach、include。底层 OGNL 表达式解析。

### Q77：Spring Boot 3 和 Spring Boot 2 主要区别？
**关键得分点：** Java 17+ 最低要求、Jakarta EE 替代 javax、GraalVM Native Image 支持、HTTP 接口增强、Observability 改进。

### Q78：你项目中 AOP 具体用在哪些场景？
**关键得分点：** 日志记录、权限校验、事务管理、接口耗时监控、统一异常处理。结合项目实际说明。

---

## 模块七：MySQL（79-85）

### Q79：MySQL 事务隔离级别有哪些？InnoDB 默认哪个？
**关键得分点：** READ UNCOMMITTED → READ COMMITTED → REPEATABLE READ（默认）→ SERIALIZABLE。InnoDB 默认 RR，通过 MVCC 实现。

### Q80：MVCC 是什么？怎么实现的？
**关键得分点：** 多版本并发控制。每行有隐藏字段（trx_id/roll_pointer），通过 undo log 版本链 + ReadView 判断数据可见性。实现无锁读。

### Q81：B+ 树索引原理？为什么不用 B 树？
**关键得分点：** B+ 树：非叶子节点只存 key（更矮胖）、叶子节点有序链表（利于范围查询）。B 树：每个节点都存数据，树更高，磁盘 IO 更多。

### Q82：什么情况索引会失效？
**关键得分点：** ① 最左前缀违反；② 对索引列做函数/计算；③ LIKE '%xxx'；④ 类型隐式转换；⑤ OR 连接非索引列；⑥ NOT IN/NOT EXISTS 某些场景。

### Q83：EXPLAIN 的关键字段怎么看？
**关键得分点：** type（ALL/index/range/ref/const）、key（实际用的索引）、rows（扫描行数）、Extra（Using filesort/Using temporary 是优化重点）。

### Q84：慢 SQL 怎么优化？
**关键得分点：** ① 开启慢查询日志定位；② EXPLAIN 分析；③ 加/改索引；④ 优化 SQL 写法（避免 SELECT *、子查询改 JOIN）；⑤ 分表分库（大数据量场景）。

### Q85：MySQL 锁有哪些？什么时候会死锁？
**关键得分点：** 行锁（Record/Gap/Next-Key）、表锁、意向锁。死锁：两个事务互相等待对方持有的锁。解决：统一加锁顺序、缩短事务、设置锁等待超时。

---

## 模块八：Redis（86-90）

### Q86：Redis 五种基本数据类型及使用场景？
**关键得分点：** String（缓存/计数器）、Hash（对象存储）、List（消息队列/时间线）、Set（去重/交并差集）、ZSet（排行榜/延迟队列）。结合项目说。

### Q87：缓存穿透、击穿、雪崩怎么区分和解决？
**关键得分点：**
- 穿透：查不存在的数据→布隆过滤器 + 空值缓存
- 击穿：热点 key 过期→互斥锁（Redisson）+ 永不过期 + 后台刷新
- 雪崩：大量 key 同时过期→随机 TTL + 多级缓存 + 限流熔断

### Q88：Redis 持久化 RDB 和 AOF？
**关键得分点：** RDB：定时快照，恢复快但可能丢数据。AOF：追加写日志，数据安全但文件大。混合持久化：RDB + AOF 增量。

### Q89：Redis 集群方案了解吗？
**关键得分点：** 主从复制、哨兵模式（自动故障转移）、Cluster（数据分片 + 哈希槽）。Cluster 适合大数据量高可用场景。

### Q90：Redis 和 Memcached 区别？
**关键得分点：** Redis 数据结构更丰富、支持持久化、支持集群、单线程（6.0 多线程 IO）、原生支持 Lua 脚本。Memcached 更简单但功能少、多线程、只支持 String。

---

## 模块九：Elasticsearch & 消息队列（91-95）

### Q91：ES 倒排索引原理？
**关键得分点：** 正排：文档→词。倒排：词→文档列表。查询时先在倒排索引中找词，再合并文档 ID 列表。Term Dictionary + Posting List。

### Q92：IK 分词器 ik_smart 和 ik_max_word 区别？
**关键得分点：** ik_smart：粗粒度（最少切分），适合查询。ik_max_word：细粒度（最大切分），适合索引。最佳实践："索引用 max_word，查询用 smart"。

### Q93：RocketMQ 如何保证消息不丢失？
**关键得分点：** 生产端：同步发送 + 重试。Broker：同步刷盘 + 主从同步。消费端：手动 ACK + 重试 + 死信队列。三个环节都要保证。

### Q94：RocketMQ 事务消息原理？
**关键得分点：** 半消息（half message）→ 执行本地事务 → 根据结果 commit/rollback。Broker 回查机制：定时检查未决事务。实现分布式事务最终一致性。

### Q95：Kafka 和 RocketMQ 区别？
**关键得分点：** Kafka：高吞吐、日志/大数据场景、分区顺序。RocketMQ：事务消息、定时消息、消息过滤（Tag）、金融级可靠。选型看场景。

---

## 模块十：AI 框架 & IoT（96-100）

### Q96：RAG 的检索质量怎么评估？
**关键得分点：** 召回率（相关文档是否都被召回）、精确率（召回结果中相关占比）、F1-Score、MRR（平均倒数排名）、NDCG。用标注测试集做离线评估。

### Q97：LangChain 的核心组件有哪些？
**关键得分点：** Models（LLM/ChatModel）、Prompts、Chains（LCEL 管道）、Memory（会话记忆）、Agents（工具调用）、Retrievers（检索）、Callbacks（监控）。项目中用到哪些说清楚。

### Q98：SSE 和 WebSocket 怎么选？
**关键得分点：** SSE：单向推送、基于 HTTP、自动重连、适合 AI 逐 token 输出。WebSocket：双向通信、实时聊天/游戏。选型依据：通信方向 + 复杂度 + 浏览器兼容。

### Q99：华为云 IoTDA 的设备影子是什么？
**关键得分点：** 设备影子（Device Shadow）是云端维护的设备状态副本，包含"期望值"（应用下发）和"上报值"（设备上报）。设备离线时应用写期望值，上线后同步。解决异步通信和状态一致性。

### Q100：如果入职后给你一个从零开始的 AI 项目，你怎么规划？
**关键得分点：**
1. 需求分析：明确业务目标、用户场景、成功指标
2. 技术选型：模型选择、框架选型（LangChain/Spring AI）、存储方案
3. MVP 验证：最小可用原型，端到端跑通
4. 工程化：接口规范化、异常处理、日志监控、压力测试
5. 迭代优化：根据用户反馈和指标持续优化检索质量和生成质量
6. 上线运维：灰度发布、监控告警、成本控制

**关键得分点：** 有方法论、有工程思维、有全链路视野。

---

## 附录：面试答题框架（STAR-CE）

| 步骤 | 说明 |
|------|------|
| **S**ituation | 背景：什么项目/什么问题 |
| **T**ask | 任务：我负责什么 |
| **A**ction | 行动：具体做了什么（技术选型 + 实现细节） |
| **R**esult | 结果：量化数据 |
| **C**hallenge | 挑战：最大的技术难点是什么 |
| **E**volution | 演进：如果重新做，会怎么改进 |

> 每道项目追问题，建议控制在 2-3 分钟内讲完 STAR，预留时间给面试官追问细节。

---

> **使用建议：** 按模块分批复习，每批 10 题，对着镜子说一遍，控制在 2 分钟/题。重点关注「关键得分点」列，确保回答覆盖核心要点。
