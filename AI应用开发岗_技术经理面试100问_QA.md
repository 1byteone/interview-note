# 技术经理面试 100 问 · QA 全集

> 基于 `简历_模板覆盖版.docx`，从技术经理/技术面试官视角设计。
> 每题包含：**问题 → 参考回答 → 关键得分点 → 常见扣分项**。

---

## 模块一：自我介绍 & 职业动机（1-10）

### Q1：请用 1 分钟做一个自我介绍。
**参考回答：** 我是软件工程专业应届生，方向是 Java 后端 + AI 应用开发。核心经历两段：一是传习教育的 AI 工程实习，参与商品搜索接口开发、RAG 知识库治理、SSE 流式交互，召回效果提升 35%；二是独立完成灵犀智能写作平台，基于 Spring AI Alibaba 的 StateGraph 编排多智能体，SSE 毫秒级首字上屏，生成成功率 99%。技术上以 Spring Boot + Redis + MySQL 为底座，叠加 RAG / Agent / SSE 等 AI 能力。我的特点是能把模型能力稳定地接到业务链路里。
**关键得分点：** 有结构（时间控制+技术主线+数据量化），不背稿，语速从容，突出"工程+AI"复合定位。
**扣分项：** 超时 >2min，背诵感强，无数据支撑，泛泛而谈。

### Q2：你为什么选择 AI 应用开发方向？
**参考回答：** AI 落地不只是调 API，核心难点在于检索质量、输出稳定性、延迟控制和异常兜底。这些恰好和我的 Java 工程背景高度契合。我希望成为"懂业务的 AI 工程师"，而不是只写 prompt 的 API 使用者。实习中我验证了这条路径的可行性——通过工程手段把 RAG 召回从不稳定提升到 35% 以上改善。
**关键得分点：** 回答有深度（区分"调 API"和"工程落地"），结合自身经历论证。
**扣分项：** "觉得 AI 热门所以选"——无个人思考。

### Q3：你觉得自己最大的优势和不足分别是什么？
**参考回答：** 优势是"全链路意识"——不只写功能，还关注测试、异常、文档和可维护性。不足是：实习深度有限，某些中间件（如 RocketMQ）停留在使用层面，底层原理还需加强。目前在系统学习源码级知识。
**关键得分点：** 优势有实例支撑（实习/项目中全链路做法），不足真实且有改进计划。
**扣分项：** "我没什么不足"——缺乏自省；不足变优点（"我太追求完美了"）。

### Q4：你的职业规划是什么？
**参考回答：** 短期（1-2年）：在 AI 应用开发岗位上深耕，补齐中间件底层原理和分布式系统设计短板。中期（3-5年）：成为能独立负责 AI 系统架构的技术骨干，具备从需求分析到性能调优的全栈能力。长期：成为连接 AI 和业务的技术桥梁。
**关键得分点：** 有层次、可执行，与岗位匹配。
**扣分项：** 过于模糊（"想当架构师"），或过于功利（"想创业"）。

### Q5：你对加班怎么看？
**参考回答：** 我关注的是"产出"而非"工时"。项目攻坚期主动加班是应该的；但日常我会用工程手段（自动化、文档化、测试覆盖）减少返工，本质上也是提升效率。
**关键得分点：** 不回避，用"效率思维"回答，同时表达责任感。
**扣分项：** "接受加班"但无实质内容；"不接受加班"过于绝对。

### Q6：期望薪资是多少？
**参考回答：** 我对行业薪资有一定了解，更看重岗位成长空间和技术方向匹配度。薪资方面我持开放态度，愿意参考公司体系和岗位级别来协商。
**关键得分点：** 不报死数字，表达灵活性和学习意愿。
**扣分项：** 报价过高/过低，或"什么都行"。

### Q7：你还投了哪些公司/岗位？
**参考回答：** 主要投递 Java 后端和 AI 应用开发方向，集中在能做 AI 工程落地的团队。相比"面广"，我更倾向"面深"——选一个技术方向对口、团队氛围好的地方长期发展。
**关键得分点：** 聚焦技术方向，体现求职策略清晰。
**扣分项：** 投了大量不相关岗位，说明定位不清。

### Q8：你有什么想问我们的？
**参考回答：** 会结合岗位预期提问，例如：① 团队目前 AI 项目的技术栈和架构是怎样的？ ② 对这个岗位的新人，前 3 个月的核心期望是什么？ ③ 团队的技术分享和成长机制是怎样的？
**关键得分点：** 问"团队/技术/成长"，不问"薪资/加班"（初面阶段）。
**扣分项：** "没有问题"——缺乏兴趣。

### Q9：如果给你一个完全陌生的技术栈，你怎么做？
**参考回答：** 三步走：① 快速通读官方文档（入门+核心概念），② 跑通一个最小 Demo，③ 带着业务场景深入关键模块。同时用公司内已有代码做参照，避免从零踩坑。实习中用 LangChain 和 Spring AI 都是这么上手的。
**关键得分点：** 有方法论（文档→Demo→业务），且有实例佐证。
**扣分项：** "百度一下"——太随意。

### Q10：你平时怎么学习新技术？
**参考回答：** 分层学习：① 先看官方文档建立全景认知；② 找一个真实场景做 Demo 验证；③ 写笔记/博客沉淀；④ 在项目中应用并收集反馈。比如 RAG 技术，我先通读 LangChain 文档，再在实习中落地，最后整理了排查记录。
**关键得分点：** 有体系、有闭环（输入→实践→输出）。
**扣分项：** "看视频教程"——缺少主动性。

---

## 模块二：项目深挖 — 养老系统（11-25）

### Q11：养老系统整体架构是什么样的？你在其中负责什么？
**参考回答：** 系统由 Vue3 + Element Plus 的 B 端、微信小程序 C 端和 Spring Boot 后端组成，后端通过 MyBatis-Plus 访问 MySQL，Redis 承担热点缓存，IoTDA 负责设备接入，千帆模型负责健康报告语义分析。我负责健康评估、IoT 增量同步和统一鉴权，边界是把外部能力封装成稳定的业务接口。
**关键得分点：** 能说清 B/C 双端、核心基础设施和个人边界；不夸大团队成果。

### Q12：健康评估从 8 秒降到 1 秒，具体做了哪些优化？
**参考回答：** 先拆分耗时：数据库查询、模型调用和结果组装。对重复的健康建议按老人/评估版本建立缓存，优先命中本地缓存，再查 Redis，未命中才调用模型；同时限制模型调用超时并返回可用兜底。8 秒到 1 秒主要来自热点结果复用，而不是单纯调大线程池。
**关键得分点：** 有耗时拆解、缓存命中逻辑、数据依据和降级意识。

### Q13：Redis 多级缓存怎么设计的？为什么选多级而不是单级？
**参考回答：** 一级是进程内缓存，适合热点数据，访问最快；二级是 Redis，供多实例共享；最后回源 MySQL 或重新调用模型。读路径按 L1→L2→DB，写路径更新 DB 后删除缓存并设置 TTL。多级缓存是用少量本地内存换取低延迟，同时保留分布式共享能力。
**关键得分点：** 说清读写路径、TTL、一致性和多实例问题。

### Q14：缓存和数据库不一致怎么处理？
**参考回答：** 对健康建议这类允许最终一致的数据，采用“更新 DB 后删除缓存”，删除失败进入重试或延迟双删；热点数据设置 TTL。强一致场景不依赖缓存直接返回，或使用锁/版本号控制。关键是先定义一致性要求，再选择方案。
**关键得分点：** 不把延迟双删说成绝对可靠，能区分强一致和最终一致。

### Q15：百度千帆大模型怎么集成的？
**参考回答：** 在 Spring Boot 中封装独立的 AI Client，负责鉴权、请求参数、模型选择、响应解析和错误映射；业务层只传递结构化输入。外层配置连接/读取超时、有限重试和日志 traceId，密钥放配置中心或环境变量，不写入代码。
**关键得分点：** 封装边界、安全、超时、错误处理和可测试性。

### Q16：AI 调用超时或返回异常怎么办？
**参考回答：** 先设置基于业务 SLA 的读取超时；仅对连接失败或临时错误做有限次指数退避重试，避免重复提交型请求被盲目重试；模型不可用时优先返回已有缓存或规则兜底，并记录模型、耗时、错误码和 traceId，便于告警和复盘。
**关键得分点：** 重试要有条件、有上限、有幂等性；有降级和可观测性。

### Q17：IoTDA 设备同步是怎么做的？为什么选增量同步？
**参考回答：** 本地记录上次同步游标或时间点，按分页拉取 IoTDA 的变更数据，只处理新增、更新和下线设备；线程池负责有限并发，避免一次任务占满资源。同步结果记录成功/失败状态，失败重试，定期全量对账修复遗漏。500+ 设备时增量同步能显著减少请求和写库量。
**关键得分点：** 游标/分页、有限并发、失败恢复、全量兜底。

### Q18：异步线程池怎么配置的？为什么不用 @Async？
**参考回答：** 我会按 IO 密集型任务配置有界队列，设置核心/最大线程、存活时间、命名线程工厂和拒绝策略，并监控活跃数和队列长度。@Async 本身可以用，但默认配置容易失控；自定义线程池能做到任务隔离、参数可调和指标可观测，实际不是“绝不用 @Async”。
**关键得分点：** 纠正绝对化表述，体现容量评估和任务隔离。

### Q19：「设备-位置」绑定逻辑怎么保证数据一致？
**参考回答：** 设备 ID 和位置关系建立唯一约束，写入采用事务和版本号；更新时校验设备是否已绑定、位置是否被占用，避免并发覆盖。同步失败保留待处理状态，配合定时对账和人工补偿处理异常。
**关键得分点：** 数据库约束优先、并发更新、异常补偿。

### Q20：ThreadLocal 在认证体系中怎么用的？如何防止内存泄漏？
**参考回答：** 拦截器校验 token 后，把用户上下文放入 ThreadLocal，Controller/Service 在同一次请求内读取；请求结束无论成功失败都在 finally 中 remove。在线程池中不能直接假设上下文会自动传递，应优先显式传参，必要时使用 TTL 并清理快照。
**关键得分点：** 生命周期、finally 清理、线程池串号风险。

### Q21：微信小程序无感登录流程？
**参考回答：** 小程序调用 wx.login 得到 code，后端服务端调用微信接口换取 openid，创建或查询用户后签发短期 access token 和可轮换 refresh token；后续请求由拦截器校验 token 并写入上下文。session_key 只在服务端保存或使用，不能下发前端。
**关键得分点：** code 换身份、token 生命周期、敏感凭证不下发。

### Q22：并发压测怎么做？压出了什么问题？
**参考回答：** 先准备可重复的库存和用户数据，再用 JMeter/Locust压测重复提交、库存扣减和查询热点，观察 QPS、P95/P99、错误率、数据库连接池和 Redis 命中率。重点验证库存最终不为负、同一请求只生成一笔订单，并根据日志定位重复写入和锁竞争。
**关键得分点：** 场景、指标、数据正确性和定位手段完整。

### Q23：Redisson 分布式锁的原理？和原生 SETNX 区别？
**参考回答：** Redisson 用 Redis 键记录持有者和重入次数，通过 Lua 保证加锁、续期和释放的原子性，并提供看门狗续期。SETNX 只解决一次加锁，业务必须自己处理唯一 owner、过期时间和安全释放。锁不是库存一致性的全部，数据库条件更新和幂等仍要保留。
**关键得分点：** owner、Lua、看门狗、可重入，以及锁的边界。

### Q24：订单链路中 RocketMQ 做了什么？
**参考回答：** 将非核心或可异步流程从同步请求中拆出，通过消息削峰和解耦；消费者处理库存、索引或通知。消费失败返回重试，超过次数进入死信，消费端用业务唯一键实现幂等。下单核心结果仍需数据库事务确认，不能把 MQ 当作事务万能替代品。
**关键得分点：** 解耦削峰、重试死信、消费幂等、核心事务边界。

### Q25：MySQL 事务在这个项目中用到了吗？遇到过死锁吗？
**参考回答：** 下单、库存扣减和绑定关系更新需要事务，保证一次业务操作内部的原子性。事务尽量短，按固定顺序访问资源并依赖合适索引减少锁范围。若发生死锁，查看 InnoDB 状态和慢日志，确认互相等待的 SQL，统一加锁顺序并允许安全重试。
**关键得分点：** 事务边界、锁范围、死锁诊断与恢复。



---

## 模块三：项目深挖 — 灵犀写作（26-40）

### Q26：StateGraph 是什么？和普通的 if-else 编排有什么区别？
**参考回答：** StateGraph 可以把 AI 应用建模成“状态 + 节点 + 边”的有向图。节点负责一个明确步骤，边负责顺序、条件或并行关系，状态负责在节点间传递中间结果。if-else 适合简单流程，但复杂后容易把业务、模型调用和异常分支揉在一起；图模型更便于扩展、测试和追踪。
**关键得分点：** State、Node、Edge；明确说明为什么比线性代码更可维护。

### Q27：多智能体怎么编排？串行和并行怎么选？
**参考回答：** 我按任务依赖关系决定。标题生成、大纲生成、正文生成存在上下游依赖，采用串行；配图搜索和部分内容生成互不依赖，可以并行。并行后要在汇聚节点统一合并结果，并设置超时、失败降级和取消策略，不能只追求并发。
**关键得分点：** 依赖分析、并行汇聚、异常处理。

### Q28：新增一种生成模式，只需要配置新节点——这是什么意思？
**参考回答：** 把每种能力封装成独立节点，把流程连接关系从节点内部抽离。新增模式时实现或注册新节点，再配置入口、边和输出映射，不修改已有节点。这个说法的前提是节点契约统一，否则配置化只是把复杂度转移到配置文件。
**关键得分点：** 开闭原则、统一节点接口、说明边界而非夸大“零代码”。

### Q29：SSE 流式输出的技术细节？
**参考回答：** SSE 是基于 HTTP 的服务端单向事件流，响应类型为 `text/event-stream`，服务端按 `data`、`event`、`id` 等字段发送事件，浏览器可用 EventSource 接收。AI 场景中模型产生一个 token 或一个增量片段就发送一次，结束时发送明确的完成事件，异常时发送错误事件并关闭连接。
**关键得分点：** 协议格式、连接生命周期、完成/异常事件。

### Q30：毫秒级首字上屏怎么实现的？
**参考回答：** 请求到达后先完成鉴权和参数校验，立即建立 SSE 连接；后台异步任务调用模型的流式接口，拿到首个增量片段后马上写入事件流，而不是等待整篇文章生成。这里的“毫秒级”应明确是前端展示链路的首字延迟指标，不能等同于完整请求耗时。
**关键得分点：** 先建流、异步生成、首 token 即推送；指标口径准确。

### Q31：异步任务体系怎么设计的？任务状态 tracking 怎么做？
**参考回答：** 接口层创建任务并返回 taskId，后台线程池执行生成，任务状态至少包括 PENDING、RUNNING、SUCCESS、FAILED、CANCELLED。状态和结果可以放 Redis 或数据库，更新时带版本号防止旧任务覆盖新状态；SSE 负责实时进度，查询接口负责断线后的补偿读取。
**关键得分点：** taskId、状态机、持久化、断线补偿、并发更新。

### Q32：吞吐提升 50% 是怎么衡量的？
**参考回答：** 在相同机器、相同模型、相同输入集和相同并发度下，对同步版本和异步版本分别压测，比较吞吐、P95/P99、错误率和线程池队列长度。异步主要改善线程占用和请求排队，不能简单说“异步一定让模型生成更快”，所以我会同时报告完整任务耗时和接口响应耗时。
**关键得分点：** 对照实验、指标口径、避免把响应速度和模型速度混淆。

### Q33：Prompt 模板引擎怎么封装的？
**参考回答：** 将系统提示词、用户提示词和变量定义分离，模板只声明主题、风格、字数、受众等变量，运行时做参数校验和安全转义。模板按版本管理，调用日志记录模板版本和模型版本，便于回滚和效果对比。
**关键得分点：** 参数化、版本化、校验、安全和可追踪。

### Q34：StructuredOutputConverter 是什么？怎么保证输出格式？
**参考回答：** 它根据目标 Java 类型生成格式提示，并尝试把模型文本转换为结构化对象。它是“尽力而为”的转换，不代表模型一定遵守格式。因此我会组合使用结构化 schema、System Prompt、解析校验、有限重试和兜底对象；关键字段缺失时不能直接入库。
**关键得分点：** 明确转换器不是强保证；校验、重试、降级闭环。

### Q35：策略模式做多源配图，具体怎么实现？
**参考回答：** 定义统一的 `ImageProvider` 接口，每个数据源实现自己的搜索、下载和结果转换逻辑；由工厂或注册表根据配置得到策略列表，再按优先级或健康状态选择。调用方只依赖接口，不依赖 Pexels 或 SVG 的具体实现。
**关键得分点：** 抽象接口、策略替换、工厂/注册表、调用方解耦。

### Q36：配图源失败自动降级——降级逻辑怎么做？
**参考回答：** 对可重试的网络错误做有限重试和退避，对鉴权失败或参数错误直接切换策略；记录每个图源的耗时和失败原因，连续失败触发熔断。所有图源不可用时返回默认占位图或仅保留文章文本，并把任务标记为“部分成功”，不能假装完整成功。
**关键得分点：** 错误分类、有限重试、熔断、部分成功语义和监控。

### Q37：生成成功率 99% 是怎么测的？
**参考回答：** 先定义成功标准：结构化字段完整、正文可读、图片链接有效、任务状态正确。使用覆盖不同题材和长度的测试集，统计端到端成功任务数/总任务数，并区分首次成功、重试成功和降级成功。报告指标时说明样本量、时间范围和是否包含降级结果。
**关键得分点：** 成功定义、样本集、分母口径、区分重试与降级。

### Q38：Redis 在灵犀项目中做什么？
**参考回答：** 主要用于缓存热门模板和任务状态，降低重复查询；也可以用分布式锁限制同一用户或同一任务的重复生成。任务状态若只放 Redis，要接受重启丢失风险，生产应使用数据库或持久化方案，并通过 TTL 和容量策略控制内存。
**关键得分点：** 场景具体、说明 Redis 边界和持久化取舍。

### Q39：MyBatis Flex 和 MyBatis-Plus 区别？为什么选 Flex？
**参考回答：** 两者都增强了 MyBatis 的开发效率，提供条件构造和通用能力，但 API、代码生成、插件和生态习惯不同。个人项目选择 Flex 主要考虑查询表达力、开发效率和当时的技术实践；如果团队已有 MyBatis-Plus 规范，我会优先遵循团队统一栈，而不是为个人偏好增加维护成本。
**关键得分点：** 不拉踩，围绕团队成本、生态和实际场景回答。

### Q40：如果让你重新做灵犀项目，你会改进什么？
**参考回答：** 第一，将任务状态从纯内存迁移到持久化存储，支持服务重启恢复；第二，增加 prompt、模型、节点和 token 的可观测性；第三，为外部图源增加超时、熔断和配额控制；第四，建立离线评测集，分别评估结构完整性、内容质量、首字延迟和成本。
**关键得分点：** 改进具体、能对应当前方案边界、兼顾可靠性和成本。

---

## 模块四：实习深挖（41-50）

### Q41：实习期间做的最重要的事是什么？
**参考回答：** 我最看重的是打通“搜索 + RAG + SSE”的完整链路。面对关键词召回不稳、模型输出不可控、流式交互易断线三个问题，我从数据分块、检索融合、结构化输出和流式异常恢复四个点推进，最后在固定测试集上把召回提升约 35%，并把排查过程沉淀成文档。
**关键得分点：** 选一个有深度的场景、STAR 清晰、有数据、有闭环。

### Q42：AI 商品搜索的完整链路是什么？
**参考回答：** 用户输入先做预处理和查询改写，再进行多路召回——ES 负责关键词，语义向量负责近义表达；两路结果做融合排序后过滤、分页返回，同时记录查询和点击日志供效果评估。不同场景配置不同权重，避免“所有查询走同一套逻辑”。
**关键得分点：** 查询改写、多路召回、融合排序、日志评估。

### Q43：接口契约是怎么定的？你怎么保证和前端/其他服务对齐？
**参考回答：** 先定义数据结构和语义（字段类型、空值含义、错误码、分页规则），用 OpenAPI 或接口文档作为单一事实来源，联调前通过评审，联调时以文档为准。发现语义分歧时先改契约再改代码，并补充测试用例，而不是临时加判断。
**关键得分点：** 契约先行、文档驱动、语义对齐、测试补充。

### Q44：高并发压测具体怎么做？用了什么工具？
**参考回答：** 我先明确压测目标，是看接口吞吐、链路延迟还是数据正确性；准备可重复的测试数据和隔离环境，用 JMeter 或 Locust 设计并发场景，逐步加压观察 QPS、P50/P95/P99、错误率、线程池、连接池和 GC；压测不止看峰值，还要看压测后的资源回收和数据一致性。
**关键得分点：** 目标、环境、工具、指标、资源与一致性。

### Q45：ES + IK 分词 + DSL 多条件筛选具体怎么实现？
**参考回答：** 先设计索引：需要全文检索的字段用 text，需要精确匹配和排序的字段用 keyword；中文用 IK 分词，索引侧用更细的 ik_max_word，查询侧按召回需要选择。查询用 bool 组合 must（关键词）、filter（价格/品类等精确条件）和 sort 排序，避免深分页改用 search_after。
**关键得分点：** 字段类型、分词策略、bool 查询、分页性能。

### Q46：语义召回怎么接入的？和 ES 如何协同？
**参考回答：** 文本先经过 embedding 模型转成向量，写入向量索引；查询时把用户问题向量化后做近似近邻检索。ES 负责关键词精确匹配和过滤，语义召回负责近义表达；两路结果用 RRF 或加权融合，再按分数过滤。纯向量方案在精确 ID、品类、价格过滤上并不占优，所以采用双路。
**关键得分点：** 嵌入、向量召回、融合、双路互补的选型逻辑。

### Q47：RAG 知识库的文档分块策略？
**参考回答：** 优先按标题层级和语义段落切块，而不是固定字符数硬切；必要时保留少量 overlap 避免上下文被截断，并在块上保留来源、章节、更新时间等元数据用于引用和过滤。分块大小和 overlap 需要根据模型上下文和检索效果在测试集上调，不是一次定死。
**关键得分点：** 语义切分、overlap、元数据、可配置可评估。

### Q48：LangChain 和 LCEL 在实习中怎么用的？
**参考回答：** LangChain 提供组件和编排能力，我用它定义模型、提示词、工具和检索器，并用 LCEL 的管道语法把它们串联成可复用链；同时用结构化输出解析和回调记录调用。关键是了解“为什么这么组链、异常时链怎么降级”，而不只是能调通。
**关键得分点：** 组件理解、LCEL 组合、结构化输出、异常降级。

### Q49：20 余组并发测试测出了什么？7 类问题分别是？
**参考回答：** 我按功能做并发验证，覆盖检索、工具调用、会话、流式输出等链路，集中暴露了检索超时、工具调用偶发失败、会话状态在并发下互相污染、SSE 偶发断流、重复消息消费、缓存失效瞬间穿透、模型输出格式异常七类。每类问题我定位到具体代码和日志，修复后补充测试和降级策略，避免同类型问题回归。
**关键得分点：** 分类清晰、定位到根因、有修复和防回归。

### Q50：实习中最大的成长是什么？
**参考回答：** 最大的转变是从“能写代码”到“关注系统能不能稳定运行”。实习让我理解模型返回不可控、并发会放大问题、日志和可观测性决定排查效率，也让我建立了“测试 → 修复 → 文档 → 复盘”的习惯。这直接影响我现在设计接口时会主动考虑超时、幂等、降级和成本。
**关键得分点：** 有认知转变、落到具体工程实践、与后续行为关联。

---

## 模块五：Java 基础 & 并发（51-65）

### Q51：HashMap 底层原理？JDK7 和 JDK8 区别？
**参考回答：** JDK7 用数组+链表，头插法，多线程扩容死循环。JDK8 改为数组+链表+红黑树（链表>8 时转树），尾插法，扩容时高低位拆分避免死循环。
**关键得分点：** 说清数据结构、扩容机制、线程安全问题。
**扣分项：** 只说"数组+链表"，不提 JDK8 红黑树。

### Q52：ConcurrentHashMap 怎么保证线程安全？
**参考回答：** JDK7 分段锁（Segment），JDK8 改为 CAS + synchronized（锁粒度细化到桶级别），配合 volatile 保证可见性。put 时 CAS 竞争，竞争失败再 synchronized 锁桶头节点。
**关键得分点：** 分清 JDK 版本差异，理解锁粒度演进。
**扣分项：** "它用了分段锁"然后就结束了——不够。

### Q53：线程池核心参数有哪些？怎么选型？
**参考回答：** corePoolSize、maximumPoolSize、workQueue、keepAliveTime、handler、threadFactory。CPU 密集型：core = N+1；IO 密集型：core = 2N 或更大。队列选有界 ArrayBlockingQueue 防止 OOM。
**关键得分点：** 六大参数 + 场景选型 + 有界队列意识。
**扣分项：** 只背参数不理解选型。

### Q54：线程池的拒绝策略有哪些？你们项目用的哪个？
**参考回答：** AbortPolicy（默认，抛异常）、CallerRunsPolicy（调用方执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢队列头）。项目用 CallerRunsPolicy——不丢任务，降速执行。
**关键得分点：** 四种策略 + 实际选型理由。

### Q55：synchronized 和 ReentrantLock 区别？
**参考回答：** synchronized：JVM 层面、自动释放、非公平、不可中断。ReentrantLock：API 层面、手动 unlock（finally）、支持公平/非公平、可中断、可超时、支持 Condition。
**关键得分点：** 从实现层面（JVM vs API）和功能层面（公平/中断/超时）对比。

### Q56：volatile 关键字的作用？
**参考回答：** ① 保证可见性（修改立即刷回主存）；② 禁止指令重排（内存屏障）。不保证原子性——自增仍需 AtomicInteger。
**关键得分点：** 可见性 + 有序性 + 不保证原子性，三句话说清楚。

### Q57：ThreadLocal 原理？为什么线程池下会出问题？
**参考回答：** 每个 Thread 持有一个 ThreadLocalMap，key 是 ThreadLocal 弱引用，value 是强引用。线程池复用线程，ThreadLocal 被回收后 key=null 但 value 仍在 map 中 → 内存泄漏。解决方案：finally 中 remove；跨线程传递用 TTL。
**关键得分点：** 弱引用 key + 强引用 value + 线程池复用 → 泄漏。

### Q58：AQS 原理了解吗？
**参考回答：** AbstractQueuedSynchronizer 是并发包基石。核心：state 变量 + CLH 双向队列。ReentrantLock/CountDownLatch/Semaphore 都基于它。独占模式（ReentrantLock）和共享模式（Semaphore）。
**关键得分点：** state + CLH 队列 + 独占/共享。

### Q59：乐观锁和悲观锁怎么选？
**参考回答：** 悲观锁：写多读少、冲突频繁（synchronized/Redisson）。乐观锁：读多写少、冲突少（CAS/版本号）。项目中库存扣减用悲观锁（Redisson），版本号更新用乐观锁。
**关键得分点：** 有场景对应，不空谈概念。

### Q60：CompletableFuture 怎么用？
**参考回答：** 异步编排利器。supplyAsync 启动异步任务，thenApply/thenAccept 链式处理，allOf 等待多个任务完成，exceptionally 处理异常。项目中可用于并行调用多个下游服务。
**关键得分点：** 知道 API + 实际使用场景。

### Q61：JVM 内存结构说一下？
**参考回答：** 堆（对象实例）、方法区/元空间（类信息、常量池）、虚拟机栈（栈帧：局部变量表、操作数栈）、本地方法栈（Native）、程序计数器。堆分新生代（Eden/S0/S1）和老年代。
**关键得分点：** 五大区域 + 堆的分代。

### Q62：垃圾回收算法有哪些？
**参考回答：** 标记-清除（碎片多）、标记-整理（无碎片但慢）、复制（新生代用，浪费空间）、分代收集（新生代复制 + 老年代标记整理）。CMS/G1/ZGC 各代演进。
**关键得分点：** 算法对比 + 收集器演进。

### Q63：线上 OOM 怎么排查？
**参考回答：** ① 看 GC 日志；② jmap dump 堆转储；③ MAT/VisualVM 分析大对象；④ jstack 看线程死锁；⑤ 代码层排查（大集合、未关闭流、ThreadLocal 泄漏）。
**关键得分点：** 工具链 + 排查步骤 + 常见原因。

### Q64：JVM 调优做过吗？
**参考回答：** 有基本实践：调整 -Xms/-Xmx 避免堆扩缩、设置 NewRatio 调整新生代比例、选择 G1 收集器做大堆场景。更深入的调优（GC 选择、Region 调整）还在学习中。
**关键得分点：** 有实际参数调整经验，坦诚说明不足。

### Q65：Java 8 的新特性用过哪些？
**参考回答：** Lambda、Stream API、Optional、CompletableFuture、接口默认方法。项目中 Stream 用在集合处理，Optional 用在空值防御。
**关键得分点：** 不只列特性，要关联到项目使用。

---

## 模块六：Spring & MyBatis-Plus（66-78）

### Q66：Spring IOC 和 AOP 分别是什么？
**参考回答：** IOC 是控制反转，把对象创建和依赖关系交给容器管理，我通过构造器或字段注入而不是主动 new，最终目的是解耦和易测试。AOP 是面向切面，把日志、事务、校验等横切逻辑从业务代码抽离；运行时通过 JDK 动态代理或 CGLIB 生成代理对象。两者是 Spring 让代码可维护的核心基础。
**关键得分点：** 解耦/可测试 + 横切关注点 + 两种代理。

### Q67：Spring Bean 的生命周期？
**参考回答：** 大致是：容器扫描并实例化 → 属性填充（依赖注入）→ Aware 回调（BeanName/BeanFactory/ApplicationContext）→ BeanPostProcessor 前置处理 → 初始化（@PostConstruct / InitializingBean / init-method）→ BeanPostProcessor 后置处理 → 可供使用 → 容器关闭时销毁（@PreDestroy / DisposableBean / destroy-method）。AOP 代理就是在 BeanPostProcessor 后置阶段生成的。
**关键得分点：** 初始化前中后、Aware、BeanPostProcessor、销毁。

### Q68：Spring 循环依赖怎么解决？
**参考回答：** 通过三级缓存：一级存已完成实例，二级存提前暴露的半成品，三级存 ObjectFactory。Bean A 依赖 B 时，B 还未创建完，容器把 A 的早期引用放入二级缓存供 B 引用，B 完成后 A 再注入完整的 B。只有单例 + setter 注入能解决，构造器注入会在实例化前就缺对方，无法提前暴露。
**关键得分点：** 三级缓存各司其职、仅单例 setter 可解、构造器不行。

### Q69：@Transactional 事务失效的场景？
**参考回答：** 常见五类：① 方法不是 public（JDK 代理无法拦截）；② 同类内部调用，this 调用绕过代理；③ 异常被 catch，事务无法感知回滚；④ 默认只回滚 RuntimeException/Error，checked 异常需 rollbackFor；⑤ 方法自调用或代理未生效、多数据源未指定事务管理器。项目里我会审查“是不是真的走代理、异常有没有抛出去”。
**关键得分点：** 代理生效条件、异常类型、内部调用，及排查手段。

### Q70：Spring Boot 自动配置原理？
**参考回答：** 入口是 @SpringBootApplication 组合注解读到的 @EnableAutoConfiguration，它通过 AutoConfiguration.imports 加载候选自动配置类，每个候选类再用 @Conditional 判断条件（类是否存在、属性是否配置、Bean 是否缺省）后生效。因此我不需要写一堆 xml，而是“约定大于配置 + 按需装配”。
**关键得分点：** 入口注解、imports 加载、@Conditional 条件装配。

### Q71：Spring AOP 用 JDK 还是 CGLIB 代理？
**参考回答：** 目标类实现了接口，Spring 默认用 JDK 动态代理（基于接口），否则用 CGLIB（生成子类）。Spring Boot 2.x 起默认 proxyTargetClass=true，统一走 CGLIB。CGLIB 对 final 方法和非 public 方法有限制；类上没有接口时用 CGLIB，实现类必须保留可覆写空间。
**关键得分点：** 默认策略、Boot 2.x 默认 CGLIB、final 限制。

### Q72：MyBatis 工作原理？
**参考回答：** 配置（XML/注解）解析成 Configuration，SqlSessionFactory 构建；每次操作由 SqlSession 交给 Executor，Executor 通过 MappedStatement 拿到 SQL 参数映射，再经 JDBC 执行并完成结果映射。Mapper 接口是动态代理（MapperProxy）——调用方法时按全限定名找到 MappedStatement 执行 SQL。
**关键得分点：** SqlSessionFactory → Executor → MappedStatement → JDBC，Mapper 动态代理。

### Q73：MyBatis 一级缓存和二级缓存？
**参考回答：** 一级缓存是 SqlSession 级别，默认开启，同一次会话内相同查询直接复用；增删改会清空。二级缓存是 Mapper 命名空间级别，跨 SqlSession，需手动开启并配置序列化。二级缓存适合读多写少且不依赖实时数据的数据，分布式环境要谨慎，避免读到脏数据。
**关键得分点：** 缓存范围、默认行为、清空时机、分布式注意。

### Q74：MyBatis-Plus 比 MyBatis 多了什么？
**参考回答：** 增强不是替换：通用 mapper 提供单表 CRUD，条件构造器（LambdaQueryWrapper）做类型安全查询，内置分页插件、逻辑删除、字段自动填充、乐观锁插件和代码生成器。底层基于 MyBatis 的插件机制扩展 Executor/SqlSource，复杂多表 SQL 仍走手写 XML。
**关键得分点：** 通用 CRUD、Lambda 条件、插件机制、仍支持 XML。

### Q75：MyBatis 分页插件原理？
**参考回答：** 本质是 MyBatis 拦截器。分页拦截器包住 Executor 的查询方法，解析目标 SQL，先改写为 COUNT 查询拿总数，再在原 SQL 上拼接 LIMIT（不同方言不同语法）。它做的是物理分页而不是全表查回内存过滤，这也是 PageHelper 类插件的通用做法。
**关键得分点：** Interceptor、COUNT + LIMIT 改写、物理分页、方言。

### Q76：MyBatis 动态 SQL 有哪些标签？
**参考回答：** if/where 处理条件拼接，choose/when/otherwise 做分支，set 处理 update 逗号，trim 做前后缀裁剪，foreach 处理集合遍历，include 复用 SQL 片段。底层用 OGNL 表达式解析条件。核心价值是用标签代替字符串拼接，避免 SQL 语法错误和注入风险（配合 #{} 占位）。
**关键得分点：** 常用标签用途、OGNL、#{} 防注入。

### Q77：Spring Boot 3 和 Spring Boot 2 主要区别？
**参考回答：** ① 基础设施：Java 17 起为基线，去除大量 deprecated 模块；② 命名空间：javax 迁移到 jakarta；③ 原生支持：GraalVM Native Image 和 AOT 编译；④ 可观测性：Actuator/Micrometer 体系增强；⑤ 依赖基线整体升级，例如 Spring Framework 6。迁移时要重点检查第三方库的 jakarta 兼容。
**关键得分点：** Java 17、Jakarta、Native、Observability 迁移注意。

### Q78：你项目中 AOP 具体用在哪些场景？
**参考回答：** ① 统一日志和接口耗时埋点；② 登录态校验和权限控制；③ 事务管理（声明式）; ④ 统一异常处理入口；⑤ 在 AI 项目中可以统一记录模型调用参数、返回结果和耗时。使用时机是“横切关注点”，避免用它包裹强业务逻辑导致流程不透明。
**关键得分点：** 场景真实、说明边界、与 AI 项目结合。

---

## 模块七：MySQL（79-85）

### Q79：MySQL 事务隔离级别有哪些？InnoDB 默认哪个？
**参考回答：** 四级隔离：读未提交（脏读）、读已提交（不可重复读）、可重复读（InnoDB 默认）、串行化（无并发）。InnoDB 默认 RR，并通过 MVCC 让普通读无锁，避免退化到串行化。业务选隔离级别要看并发和一致性要求，不能只背定义。
**关键得分点：** 四级定义、默认 RR、MVCC 让 RR 实用化。

### Q80：MVCC 是什么？怎么实现的？
**参考回答：** 多版本并发控制，让读写不互相阻塞。每行有隐藏字段 trx_id（创建事务）和 roll_pointer（指向前版本 undo log）；读操作通过 ReadView（活跃事务列表 + 上限）判断哪些版本可见。RR 和 RC 的主要区别在 ReadView 的生成时机：RR 事务内固定，RC 每次读都重新生成。
**关键得分点：** 隐藏字段、undo 版本链、ReadView、RR vs RC 差异。

### Q81：B+ 树索引原理？为什么不用 B 树？
**参考回答：** B+ 树非叶子节点只存 key 和指针，叶子节点存储整行数据地址且用双向链表串起来。同样的页大小能装更多 key，树更矮、磁盘 IO 更少；叶子链让范围查询和排序非常高效。B 树每个节点都存数据，树更高、叶子没有链表，范围扫描要多次回溯。
**关键得分点：** 矮胖、叶子链表、范围查询、IO 次数。

### Q82：什么情况索引会失效？
**参考回答：** ① 不满足最左前缀；② 对索引列使用函数或计算，例如 WHERE LENGTH(name)>5、WHERE id+1=10；③ LIKE 以 % 开头；④ 隐式类型转换，如字符串列与数字比较；⑤ OR 两侧存在非索引列；⑥ 优化器认为全表扫更快（数据量小或选择性差）。排查用 EXPLAIN 看 key 和 type。
**关键得分点：** 能列举明确失效场景，并强调用 EXPLAIN 验证。

### Q83：EXPLAIN 的关键字段怎么看？
**参考回答：** 重点看 type（const/eq_ref/ref/range/index/ALL，越靠左越好）、key（实际选用索引）、rows（预估扫描行数）、 Extra（Using filesort、Using temporary、Using index 覆盖索引）。Extra 出现 filesort/temporary 通常是排序或分组没吃到索引，需要结合索引设计看。
**关键得分点：** type 层级、rows、Extra 告警信号。

### Q84：慢 SQL 怎么优化？
**参考回答：** 先开慢查询日志拿到实际慢语句；EXPLAIN 看是否走索引、有没有 filesort/temporary；再针对性建复合索引、改写查询（避免不必要的 SELECT *、子查询改 JOIN、函数包索引列）。如果数据量大造成结构性瓶颈，再考虑分库分表或读写分离，而不是一味堆索引。
**关键得分点：** 定位（慢日志）+ 分析（EXPLAIN）+ 索引/SQL 优化 + 分层的取舍。

### Q85：MySQL 锁有哪些？什么时候会死锁？
**参考回答：** 粒度上分表锁和行锁（InnoDB），行锁又可细分为 Record Lock、Gap Lock、Next-Key Lock 和插入意向锁，另有意向锁做表锁与行锁的快速检查。死锁常发生在两个事务按相反顺序加锁，或间隙锁争夺插入位置。解决是统一加锁顺序、让事务更短、必要索引缩小锁范围，死锁后捕获异常并安全重试。
**关键得分点：** 行锁细分、死锁成因、加锁顺序与重试。

---

## 模块八：Redis（86-90）

### Q86：Redis 五种基本数据类型及使用场景？
**参考回答：** String 用于缓存和计数器；Hash 存对象字段，比如用户资料；List 做简单队列和时间线；Set 做去重、交集差集（共同关注）；ZSet 做排行榜、延迟队列（score 作到期时间）。选型核心是“通过操作直接满足需求，而不是先取出再算”，比如排行榜用 ZRANGEBYSCORE 而不是先查后排。
**关键得分点：** 各类型典型场景 + 操作即结果的选型思维。

### Q87：缓存穿透、击穿、雪崩怎么区分和解决？
**参考回答：**
- 穿透：查询的数据本身不存在，缓存永远不命中。解决：布隆过滤器拦截 + 空值缓存短 TTL。
- 击穿：热点 key 过期瞬间大量请求打库。解决：互斥锁重建（Redisson）、逻辑过期式“永不过期” + 后台刷新。
- 雪崩：大量 key 同时过期或 Redis 整体不可用。解决：过期时间加随机 jitter、多级缓存、限流熔断 + 降级。
对健康建议这类数据，我采用的是“短 TTL + 后台刷新”组合，避免高峰重建风暴。
**关键得分点：** 三者成因不同，解决方案能对应，并结合项目场景说明选择。

### Q88：Redis 持久化 RDB 和 AOF？
**参考回答：** RDB 是定时全量快照，恢复快、文件紧凑，但两次快照间的数据可能丢失；AOF 是追加写命令日志，可配置 everysec/always，数据更安全但文件大。生产常用 RDB + AOF 混合：用 RDB 打底快速恢复，AOF 补增量。持久化选型要结合可容忍丢失量和恢复速度权衡。
**关键得分点：** 两者差异、混合持久化、按可容忍丢失量选型。

### Q89：Redis 集群方案了解吗？
**参考回答：** 主从复制解决读扩展，哨兵解决自动故障转移（高可用），Cluster 通过 16384 个哈希槽分片解决容量和写扩展，客户端根据槽位路由。取模 vs 一致性哈希 vs 哈希槽：哈希槽由集群统一管理，迁移友好。超过单机容量或写压力大时用 Cluster，规模小时主从 + 哨兵已足够。
**关键得分点：** 三种方案的定位、哈希槽、按规模选型。

### Q90：Redis 和 Memcached 区别？
**参考回答：** Redis 数据结构丰富、支持持久化、支持主从/哨兵/Cluster、单线程模型（6.0 起 IO 多线程）+ Lua 脚本保证原子性；Memcached 只存 String、多线程、无持久化。Redis 可扩展性明显更强，大多数缓存和分布式需求选 Redis；Memcached 在纯 KV 大规模场景还有内存利用优势，但生态和 Web 端兼容性弱。项目里缓存、锁、排行榜都基于 Redis。
**关键得分点：** 数据结构/持久化/集群差异，结合项目选型理由。

---

## 模块九：Elasticsearch & 消息队列（91-95）

### Q91：ES 倒排索引原理？
**参考回答：** 正排索引是“文档→词”，倒排索引是“词→包含该词的文档列表”。建立时对文档分词，生成 term dictionary（词典）和 posting list（文档 ID 列表）；查询时先按词典找词，再合并对应文档 ID，配合 FST 压缩词典和跳表/位图加速合并。这就是 ES 全文检索快于数据库 LIKE 的原因。
**关键得分点：** 正倒排对比、词典+倒排表、为什么快。

### Q92：IK 分词器 ik_smart 和 ik_max_word 区别？
**参考回答：** ik_smart 做最少切分，粒度粗、词更少，适合查询侧减少噪音；ik_max_word 做最大切分，把词切到最细，召回更高，适合索引侧。最佳实践是“索引用 max_word，查询用 smart”——保证召回又不至于查询词过碎。专业领域可扩展自定义词典避免专有名词被拆错。
**关键得分点：** 两者粒度差异、索引/查询分离、自定义词典。

### Q93：RocketMQ 如何保证消息不丢失？
**参考回答：** 分三段：生产端用同步发送并处理 SendResult，失败走重试或本地补偿；Broker 侧设置同步刷盘（SYNC_FLUSH）和主从同步（SYNC_MASTER）降低宕机丢失；消费端采用消费确认 + 失败重试，最后进入死信队列。每一段都要独立保证，只保证一段不算数。
**关键得分点：** 生产/Broker/消费三段分别回答，突出“环环相扣”。

### Q94：RocketMQ 事务消息原理？
**参考回答：** 先发送半消息（消费者不可见），Broker 返回半消息已存储，应用执行本地事务，根据结果 commit/rollback 半消息。如果本地事务执行期间进程挂了，Broker 会定期回查，应用通过回查逻辑决定最终提交或回滚。它实现的不是强一致，而是最终一致性，用于下单后异步扣库存等场景。
**关键得分点：** 半消息、本地事务、回查机制、最终一致性定位。

### Q95：Kafka 和 RocketMQ 区别？
**参考回答：** 两者都是高吞吐 MQ，但定位不同。Kafka 生态强、吞吐极高，多用于日志和流处理，顺序性按分区保证；RocketMQ 提供事务消息、定时/延时消息、按 Tag 过滤、更完善的重试与死信，金融场景可靠性更好。选型依据是场景：日志流处理选 Kafka，需要事务消息和丰富特性的业务消息选 RocketMQ。
**关键得分点：** 定位差异、事务/定时特性、按场景选型而非一味比较性能。

---

## 模块十：AI 框架 & IoT（96-100）

### Q96：RAG 的检索质量怎么评估？
**参考回答：** 用带标注的测试集做离线评估，指标包括召回率（相关文档是否被召回）、精确率（召回结果的相关占比）、F1、MRR（第一个正确结果的排名）和 NDCG（考虑排序质量）。同时看两类错误：该召回的没召回（漏检），不该召回的召回太多（噪音）。检索评估要和“最终回答质量”分开，回答质量还要额外看是否忠实、有无引用。
**关键得分点：** 离线测试集、召回/精确/MRR/NDCG、检索与生成分开评估。

### Q97：LangChain 的核心组件有哪些？
**参考回答：** 核心有 Models（LLM/ChatModel）、Prompts、Chains（用 LCEL 组合）、Memory（会话记忆）、Agents 与 Tools（工具调用）、Retrievers（检索）、Callbacks（可观测性）和 Output Parsers（结构化输出）。实习中我用到了 Models、Retrievers、Agents/Tools、Output Parsers 和 LCEL，重点是把它们组合成可降级的链路。
**关键得分点：** 能枚举组件，并落到“项目里用了哪些、为什么这么组”。

### Q98：SSE 和 WebSocket 怎么选？
**参考回答：** SSE 是服务端到客户端的单向 HTTP 事件流，浏览器原生 EventSource 支持、自动重连、实现简单，非常适合 AI 逐 token 输出和实时通知；WebSocket 是双向长连接，适合聊天、协同编辑、游戏这类需要双向实时通信的场景。AI 流式输出只有“服务端→前端”一个方向，SSE 更轻、更省资源，所以我选 SSE；需要双向交互时才考虑 WebSocket。
**关键得分点：** 单向 vs 双向、HTTP vs TCP、自动重连、按通信方向选型。

### Q99：华为云 IoTDA 的设备影子是什么？
**参考回答：** 设备影子是云端为设备维护的一份状态副本，包含“期望值”（应用侧下发、设备待执行）和“上报值”（设备实际上报）。设备离线时应用仍能写入期望值，设备上线后同步执行；应用读取的是最近上报值，无需实时连线设备。它解耦了应用与设备，解决了弱网/离线下的状态一致性问题。项目里用于设备状态同步和离线缓存。
**关键得分点：** 期望值 vs 上报值、离线解耦、状态一致性。

### Q100：如果入职后给你一个从零开始的 AI 项目，你怎么规划？
**参考回答：** 我按六步推进：
1. 需求澄清：业务目标、用户场景、成功指标和边界，先回答“做出来要解决什么问题、如何衡量”。
2. 技术选型：模型选择、框架（LangChain / Spring AI）、向量与存储、缓存与异步，列出取舍。
3. MVP 验证：端到端跑通最小可用版本，尽早暴露模型效果和链路问题。
4. 工程化：接口规范、结构化输出、异常与降级、日志监控、压测。
5. 迭代优化：基于评测集和用户反馈调检索质量、生成质量和成本。
6. 上线运维：灰度、监控告警、token/成本控制、版本可回滚。
关键是我会把“可衡量指标”和“可回滚”前置，而不是先堆功能。
**关键得分点：** 有方法论、指标先行、工程化与运维闭环、体现全链路视野。

---

## 附录：面试答题框架（STAR-CE）

| 步骤 | 说明 |
|------|------|
| **S**ituation | 背景：什么项目/什么问题 |
| **T**ask | 任务：我负责什么 |
| **A**ction | 行动：具体做了什么（技术选型 + 实现细节） |
| **R**esult | 结果：量化数据 |
| **C**hallenge | 挑战：最大的技术难点是什么 |
| **E**volution | 演进：如果重新做，会怎么改进 |

> 每道项目追问题，建议控制在 2-3 分钟内讲完 STAR，预留时间给面试官追问细节。

---

> **使用建议：** 按模块分批复习，每批 10 题，对着镜子说一遍，控制在 2 分钟/题。重点关注「关键得分点」列，确保回答覆盖核心要点。
